/* ==========================================================================
   错题本 / 数据结构 · 绪论与存储结构
   —— 一张卡 = 一个丢过分的点，不是一整道题。
   ========================================================================== */

KM.page({
  path: 'review/ds/intro',
  title: '错题卡 · 绪论与存储结构',
  subtitle: '卡面原文。要一张一张翻着练，去[卡片练习](#/review/index/deck?at=deck)',
  tags: ['错题', '数据结构'],
  updated: '2026-08-22',

  blocks: [

    /* ------------------------------------------------------------------ */
    { t: 'h', id: 'storage', c: '存储结构' },

    { t: 'card',
      id: 'c-chain-not-only-list',
      date: '2026-08-22',
      where: '存储结构 · 审题',
      q: String.raw`
        集合 $S=\{\texttt{"do"},\texttt{"for"},\texttt{"repeat"},\texttt{"while"}\}$，
        查找概率依次为 $0.35,\ 0.15,\ 0.15,\ 0.35$。
        已知存在长度为 4 的顺序表中用折半查找时 $\mathrm{ASL}=2.2$。

        **若采用链式存储结构保存 $S$**，且要求平均查找长度更短，
        元素应如何排列？用何种查找方法？$\mathrm{ASL}$ 是多少？
      `,
      wrong: String.raw`
        把限制写死成了"==第二问一定是链式存储的**单链表**=="，
        于是只能按概率降序排成一条链、用顺序查找，得到 $\mathrm{ASL}=2.1$。
      `,
      right: String.raw`
        ==按字典序构造**最优二叉排序树**（用二叉链表存），$\mathrm{ASL}=2.0$。==

        - 根：$\texttt{for}(0.15)$
        - 左孩子：$\texttt{do}(0.35)$，右孩子：$\texttt{while}(0.35)$
        - $\texttt{while}$ 的左孩子：$\texttt{repeat}(0.15)$

        $$\mathrm{ASL}=0.15\times 1+0.35\times 2+0.35\times 2+0.15\times 3=2.0$$

        中序为 $\texttt{do},\texttt{for},\texttt{repeat},\texttt{while}$，==字典序成立==；
        两个高概率元素都被提到了第 2 层。
      `,
      why: String.raw`
        **根因有两层**：

        1. ==把题干的"顺序表"当成了贯穿全题的总要求==。
           那句"保存在长度为 4 的顺序表中，采用折半查找"只是**初始状态**，
           两问开头的"**若**"字==已经把它作废了==。
        2. ==把"链式存储"等同于"单链表"==。
           二叉链表、十字链表、邻接表全都是链式存储，
           ==题目只限制了物理存储方式，没限制逻辑结构必须是线性的==。

        **两句可执行的自查**：
        - ==看到"若采用……"，先把前面的假设划掉==；
        - ==看到"链式存储"，先问一句"能不能上树"==。
      `,
      link: 'ds/intro/adt?at=ex-asl' },

    { t: 'card',
      id: 'c-hash-not-sequential',
      date: '2026-08-22',
      where: '存储结构 · 概念辨析',
      q: String.raw`判断：**用一维数组实现的散列表，属于顺序存储结构。**`,
      options: [
        { k: 'A', c: String.raw`对` },
        { k: 'B', c: String.raw`错` },
      ],
      answer: 'B',
      right: String.raw`
        ==错。散列存储与顺序存储是四大物理存储结构中**平级且互斥**的两类。==

        虽然开放定址法的底层确实是一块连续数组、也不用指针，但：

        1. ==地址映射的自变量不同==
           $$\text{顺序：地址}=f(\text{逻辑位置}),\qquad \text{散列：地址}=H(\text{关键字})$$
           顺序存储里"存在哪"只跟排第几有关；散列里只跟值有关。
        2. ==目的相反==：顺序存储要**保存**拓扑关系，散列存储**故意抛弃**关系换 $O(1)$ 寻址。
        3. 散列存储在概念上必然包含[拉链法](#/ds/search/hash?at=chaining)，==那里用了大量显式指针==。
      `,
      why: String.raw`
        **根因**：把"底层是不是数组"当成了判定标准。
        真正的判定标准是==这块连续空间被用来干什么== ——
        是"按位置刻画关系"，还是"按值计算地址"。

        **一句话自查**：==问"元素存在哪，取决于它排第几，还是取决于它是多少"==。
        取决于排第几 → 顺序；取决于是多少 → 散列。

        **顺带记住**：顺序存储能承载的远不止顺序表 ——
        [顺序栈、循环队列、堆、并查集、邻接矩阵](#/ds/intro/adt?at=what-seq-can-hold)全都是。
      `,
      link: 'ds/intro/adt?at=hash-not-seq' },

    { t: 'card',
      id: 'c-orthogonal-limit',
      date: '2026-08-22',
      where: '存储结构 · 正交性',
      q: String.raw`
        "任何一种逻辑结构都可以由任意一种存储结构来实现"——
        这句话严格来说成立吗？如果不成立，问题出在哪？
      `,
      right: String.raw`
        ==不严格成立。问题出在**散列**（以及索引）上。==

        - ==纯散列存储只能无损表达四种逻辑结构中的"**集合**"==
          —— 因为集合是唯一不需要任何拓扑关系的逻辑结构；
        - 散列打乱了相对位置又不用指针，==根本没有手段表达"前驱后继""父子"==；
        - 给结点加 $\texttt{Next\_ID}$ 之类的字段？==那个字段就是虚拟指针==，
          此时已经变成"散列（结点池）+ 静态链式（表达关系）"的**复合**结构。

        **更严谨的分层**：

        | 类别 | 成员 | 与逻辑结构 |
        |---|---|---|
        | 拓扑组织基元 | ==顺序、链式== | ==完全正交== |
        | 检索优化策略 | ==索引、散列== | ==须依附于前两者== |
      `,
      why: String.raw`
        **根因**：教材那句"同一逻辑结构可采用不同存储结构"==是对的，但被过度外推了==。
        它想说的是"逻辑与物理解耦"，==不是"四种存储任意换都行"==。

        **一句话记住**：==真正能互相模拟、与逻辑结构完全正交的，只有顺序和链式两种基元==
        （顺序用下标模拟指针 = 静态链表；链式用指针串出顺序 = 单链表）。

        ==**但考试仍然按教材口径答**==：
        "顺序存储只能用于线性结构"必须答**错**，
        这条严谨边界是拿来理解的，==不要写进选择题的答案里==。
      `,
      link: 'ds/intro/adt?at=orthogonal-limit' },

  ],
});
