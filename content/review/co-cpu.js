/* ==========================================================================
   错题本 / 组成原理 · 中央处理器
   —— 一张卡 = 一个丢过分的点，不是一整道题。
      卡片写在这里（这一页是"卡面原文"，方便读和搜），
      练习用 review/index/deck 的翻面模式，汇总看 review/index/all。
   ========================================================================== */

KM.page({
  path: 'review/co/cpu',
  title: '错题卡 · 中央处理器',
  subtitle: '卡面原文。要一张一张翻着练，去[卡片练习](#/review/index/deck?at=deck)',
  tags: ['错题', '组成原理'],
  updated: '2026-08-15',

  blocks: [

    /* ------------------------------------------------------------------ */
    { t: 'h', id: 'registers', c: '寄存器位数' },

    { t: 'card',
      id: 'c-pc-align',
      date: '2026-08-15',
      where: '寄存器位数 · PC 与 IR',
      q: String.raw`
        某计算机主存空间 $4\ \text{GB}$，字长 $32$ 位，==按字节编址==，
        采用 $32$ 位定长指令字格式，若==指令按字边界对齐存放==，
        则 $\texttt{PC}$ 和 $\texttt{IR}$ 的位数至少分别是？
      `,
      options: [
        { k: 'A', c: '$30$、$30$' },
        { k: 'B', c: '$30$、$32$' },
        { k: 'C', c: '$32$、$30$' },
        { k: 'D', c: '$32$、$32$' },
      ],
      answer: 'B',
      mine: 'D',
      wrong: String.raw`
        $\texttt{PC}$ 写成了 $32$ 位：==看到"按字节编址"就惯性地按字节地址算==，
        漏掉了题面最后那句"指令按字边界对齐存放"。
      `,
      right: String.raw`
        ==$\texttt{PC}=30$ 位，$\texttt{IR}=32$ 位，选 B。==

        - $\texttt{IR}$ 装==指令本身==，$32$ 位定长指令 $\Rightarrow$ $32$ 位，一位都压不掉；
        - $\texttt{PC}$ 装==指令的地址==。$4\ \text{GB}$ 按字节编址是 $32$ 位字节地址，
          但对齐后指令只能从 $4$ 的倍数开始，==最低 2 位恒为 $0$==，
          真正要区分的位置只有 $2^{30}$ 个 $\Rightarrow$ $30$ 位。
      `,
      why: String.raw`
        **根因**：把"按字节编址"当成了终点，==没意识到"对齐"是一句会改答案的约束==。
        编址单位决定的是 $\texttt{MAR}$，==对齐决定的是 $\texttt{PC}$ 比 $\texttt{MAR}$ 少几位==。

        **一句话记住**：==看到"对齐"就算一句"低几位恒为 0"，那几位不进 $\texttt{PC}$。==
        字边界对齐、$4$ 字节一字 $\Rightarrow$ 少 $2$ 位。

        **分开问两个空**：==$\texttt{IR}$ 量"指令多长"，$\texttt{PC}$ 量"指令能落在几个位置"==。
        $\texttt{IR}$ 永远不受对齐影响 —— 只要看到答案里 $\texttt{IR}\ne$ 指令字长，
        那个选项直接划掉（本题的 A、C 就是这样掉的）。

        **读题动作**：题面每多一句限定，就问一句"==这句我用上了吗=="。
        这题的最后一句正是答案本身。
      `,
      link: 'co/cpu/datapath?at=ex-pc-ir-bits' },

    /* ------------------------------------------------------------------ */
    { t: 'h', id: 'control-flow', c: '指令与执行顺序' },

    { t: 'card',
      id: 'c-change-order',
      date: '2026-08-15',
      where: '执行顺序 · 转移 vs 恢复',
      q: String.raw`
        哪些指令==可能不改变==程序下一条指令的地址？

        Ⅰ. 条件转移　　Ⅱ. 过程调用　　Ⅲ. 陷入指令　　Ⅳ. 返回
      `,
      options: [
        { k: 'A', c: 'Ⅰ、Ⅱ' },
        { k: 'B', c: 'Ⅰ、Ⅳ' },
        { k: 'C', c: 'Ⅱ、Ⅲ' },
        { k: 'D', c: 'Ⅱ、Ⅳ' },
      ],
      answer: 'B',
      mine: 'A',
      wrong: String.raw`
        把 Ⅱ 也算了进去，理由是==过程调用有可能调用的正好是紧跟其后的那段程序==
        （那样下一条指令的地址就没变）。
      `,
      right: String.raw`
        ==Ⅰ、Ⅳ，选 B。==

        | | 会不会改变 | 理由 |
        |---|---|---|
        | Ⅰ 条件转移 | ==可能不改变== | 条件不成立就顺序执行 |
        | Ⅱ 过程调用 | 一定改变 | ==无条件==转到过程入口 |
        | Ⅲ 陷入指令 | 一定改变 | ==无条件==转到陷入处理程序 |
        | Ⅳ 返回 | ==不改变== | 送回 $\texttt{PC}$ 的==正是主程序本来就该执行的那一条== |

        **秒杀**：Ⅱ、Ⅲ 是无条件转移，==含它们的 A / C / D 全部划掉，只剩 B==。
      `,
      why: String.raw`
        **根因**：==把"能不能构造出一个特例"当成了"指令语义里留没留这个可能"==。

        "调用紧跟其后的那段程序"这个念头==本身没错==，
        $\texttt{x86}$ 里就有这种写法（借压栈弹栈拿到当前 $\texttt{PC}$）。
        但过程调用的语义==永远是"无条件转到目标地址"==，
        这次目标碰巧是下一条，==是操作数的巧合，不是指令的性质==。
        照这个逻辑推下去，$\texttt{JMP}$ 跳到下一条也"不改变顺序"，四个选项就全对了。

        **一句话记住**：==问"某类指令可不可能……"，看的是指令定义，不是操作数的巧合。==

        **顺带记住 Ⅳ 为什么算不改变**：==转移是"指定新去处"，返回是"回到本来该去的地方"==。
        硬件上返回指令照样强制写 $\texttt{PC}$，但它写进去的是断点 ——
        ==主程序本来就该执行的那一条==。

        **这题措辞是有毛病的**：写"改变下一条指令的地址"（硬件口径），
        考的却是"改变执行顺序"（控制流口径）。
        ==选项里没有"仅 Ⅰ"，这本身就是在提示该用控制流口径==。
      `,
      link: 'co/cpu/datapath?at=ex-change-order' },

  ],
});
