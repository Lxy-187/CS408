/* ==========================================================================
   数据结构 / 3 栈、队列与数组 / 栈的应用
   ========================================================================== */

KM.page({
  path: 'ds/stack-queue/stack',
  title: '栈及其应用',
  subtitle: '只在一端进出的线性表 —— 括号匹配、表达式求值、递归转非递归，本质都是"把待办事项倒着记下来"',
  tags: ['高频', '必考', '手算', '代码题'],
  updated: '2026-08-21',

  blocks: [

    /* ================================================================== */
    { t: 'h', id: 'def', c: '一、定义与两种存储' },

    { t: 'key', id: 'basic', title: '栈：后进先出（LIFO）', c: String.raw`
      **栈**是==只允许在一端（栈顶）进行插入和删除==的线性表。

      - **栈顶（top）**：允许操作的那一端；
      - **栈底（bottom）**：固定不动的那一端；
      - **特性**：==后进先出（LIFO, Last In First Out）==。

      ==栈是一种"操作受限的线性表"==，它的逻辑结构仍然是线性结构，
      只是把可用的操作砍到只剩两个。==这一点判断题爱考==。

      **基本操作**：$\mathrm{Push}$（进栈）、$\mathrm{Pop}$（出栈）、
      $\mathrm{GetTop}$（读栈顶但不删）、$\mathrm{StackEmpty}$。
    ` },

    { t: 'key', id: 'two-top', title: '★ 顺序栈的两种 top 约定（一定要先声明用哪种）', c: String.raw`
      | | 约定 A：**top 指向栈顶元素** | 约定 B：**top 指向栈顶的下一个位置** |
      |---|---|---|
      | 初始化 | ==$\texttt{top = -1}$== | ==$\texttt{top = 0}$== |
      | 判空 | $\texttt{top == -1}$ | $\texttt{top == 0}$ |
      | 判满 | $\texttt{top == MaxSize - 1}$ | $\texttt{top == MaxSize}$ |
      | 进栈 | ==$\texttt{data[++top] = x}$== | ==$\texttt{data[top++] = x}$== |
      | 出栈 | ==$\texttt{x = data[top--]}$== | ==$\texttt{x = data[--top]}$== |
      | 元素个数 | $\texttt{top + 1}$ | $\texttt{top}$ |

      ==两套只能用一套，混用必错==。408 教材（王道）默认用**约定 A**，
      但真题题面有时会写 $\texttt{top = 0}$ 表示空栈 —— ==读题时先确认==。

      **口诀**：==指向元素就"先加后用"，指向空位就"先用后加"==。
    ` },

    { t: 'diagram', id: 'top-demo', title: '两种约定下 top 指哪',
      note: '栈里已有 a、b、c 三个元素',
      caption: String.raw`==同样是装了 3 个元素的栈，两种约定下 $\texttt{top}$ 的值差 1==。
      所以判满条件、进栈语句、元素个数的算法全都不一样。
      ==做题前先在草稿上写一行"本题 top 指向栈顶元素"==，能省掉一半的错。`,
      svg: String.raw`
<svg class="dg" viewBox="0 0 700 208" role="img" aria-label="顺序栈两种 top 约定的对比">
  <text class="cap" x="60" y="20">约定 A：top 指向栈顶元素</text>
  <g class="n m"><rect x="90" y="34" width="90" height="32" rx="4"/><text class="bt xs" x="135" y="50" text-anchor="middle" dominant-baseline="central">3　空</text></g>
  <g class="n a"><rect x="90" y="66" width="90" height="32" rx="4"/><text class="bt xs" x="135" y="82" text-anchor="middle" dominant-baseline="central">2　c</text></g>
  <g class="n k"><rect x="90" y="98" width="90" height="32" rx="4"/><text class="bt xs" x="135" y="114" text-anchor="middle" dominant-baseline="central">1　b</text></g>
  <g class="n k"><rect x="90" y="130" width="90" height="32" rx="4"/><text class="bt xs" x="135" y="146" text-anchor="middle" dominant-baseline="central">0　a</text></g>
  <path class="ar em" d="M226,82 H186"/>
  <text class="lb em" x="232" y="86">top = 2</text>
  <text class="cap" x="60" y="184">元素个数 = top + 1 = 3</text>
  <path class="sep" d="M350,24 V190"/>
  <text class="cap" x="410" y="20">约定 B：top 指向下一个空位</text>
  <g class="n a"><rect x="440" y="34" width="90" height="32" rx="4"/><text class="bt xs" x="485" y="50" text-anchor="middle" dominant-baseline="central">3　空</text></g>
  <g class="n k"><rect x="440" y="66" width="90" height="32" rx="4"/><text class="bt xs" x="485" y="82" text-anchor="middle" dominant-baseline="central">2　c</text></g>
  <g class="n k"><rect x="440" y="98" width="90" height="32" rx="4"/><text class="bt xs" x="485" y="114" text-anchor="middle" dominant-baseline="central">1　b</text></g>
  <g class="n k"><rect x="440" y="130" width="90" height="32" rx="4"/><text class="bt xs" x="485" y="146" text-anchor="middle" dominant-baseline="central">0　a</text></g>
  <path class="ar em" d="M576,50 H536"/>
  <text class="lb em" x="582" y="54">top = 3</text>
  <text class="cap" x="410" y="184">元素个数 = top = 3</text>
</svg>
` },

    { t: 'key', id: 'shared-stack', title: '共享栈：两个栈共用一个数组', c: String.raw`
      两个栈分别从数组的==两端向中间生长==：

      - $\texttt{top0}$ 初值 $-1$，向右增长；
      - $\texttt{top1}$ 初值 $\texttt{MaxSize}$，向左增长。

      $$\text{判满}:\ \boxed{\texttt{top0 + 1 == top1}},\qquad
      \text{栈 0 空}:\ \texttt{top0 == -1},\quad \text{栈 1 空}:\ \texttt{top1 == MaxSize}$$

      **好处**：==只有当两个栈的元素总数超过 $\texttt{MaxSize}$ 时才会溢出==，
      而两个独立的栈各自可能一个满了另一个还空着。
      ==这是"用一块空间服务两个用途"的典型手法==。

      **链栈**：用链表实现，==通常不带头结点，头指针就是栈顶指针==，
      进栈就是头插、出栈就是删头结点，都是 $O(1)$，==而且不会栈满==。
    ` },

    /* ================================================================== */
    { t: 'h', id: 'pop-seq', c: '二、出栈序列' },

    { t: 'key', id: 'catalan', title: '合法出栈序列的个数与判定', c: String.raw`
      $n$ 个元素==按 $1,2,\dots,n$ 的顺序进栈==，进出栈可以交替进行，
      则不同的出栈序列共有

      $$\frac{1}{n+1}\binom{2n}{n}$$

      这就是[卡特兰数](#/ds/tree/properties?at=catalan-def)。

      前几项：$1,\ 2,\ 5,\ 14,\ 42,\ 132$（对应 $n=1..6$）。

      **判定某个序列是否合法**，有两种方法：

      **① 模拟法（万无一失）**：拿一个栈按题意走一遍，走得通就合法。

      **② 判据法（快）**：==不存在 $i<j<k$ 使得出栈序列中出现 $a_k<a_i<a_j$ 的模式==。
      口语版：==若某个元素先出栈，那么在它之后出栈的、比它小的那些元素，
      必须是**递减**的==。

      **最常用的一条特例**：==$1$ 若第一个出栈，则 $3$ 不可能在 $2$ 之前出栈==
      —— 因为 $2,3$ 都还在栈里，$3$ 压在 $2$ 上面时 $3$ 先出，
      但那样 $2$ 就必须紧跟其后。
    ` },

    /* ================================================================== */
    { t: 'h', id: 'apply', c: '三、三大应用' },

    { t: 'key', id: 'bracket', title: '应用一：括号匹配', c: String.raw`
      **算法**：从左到右扫描

      - 遇到==左括号==就==入栈==；
      - 遇到==右括号==就==出栈一个==，检查是否与之配对：
        - 栈空 → ==右括号多了==，失败；
        - 类型不匹配 → 失败。
      - 扫描结束后==栈必须为空==；不空说明==左括号多了==。

      ==三个失败条件缺一不可==：右括号多、类型不配、扫完栈非空。
      只写前两个是这道题最常见的丢分点。

      **为什么用栈**：括号的嵌套是==后出现的必须先闭合==，
      正是 LIFO 的定义。
    ` },

    { t: 'key', id: 'infix-postfix', title: '应用二：中缀 → 后缀（逆波兰式）', c: String.raw`
      **为什么要转**：后缀表达式==不需要括号、不需要优先级==，
      从左到右扫一遍就能求值，==正好适合机器执行==。

      **转换算法**（一个运算符栈，一个输出序列）：

      | 扫描到 | 动作 |
      |---|---|
      | **操作数** | ==直接输出== |
      | **左括号 $($** | ==入栈== |
      | **右括号 $)$** | ==反复弹栈并输出，直到遇到 $($ 为止；$($ 弹出但不输出== |
      | **运算符 $\theta$** | ==反复弹出栈顶那些优先级 $\ge \theta$ 的运算符并输出==，
        遇到 $($ 或栈空则停；然后把 $\theta$ 入栈 |
      | **扫描结束** | ==把栈中剩下的运算符全部弹出并输出== |

      ==左括号一旦入栈，就变成一堵"墙"==：它之上的运算符只能被右括号或它自己弹出。
      这就是括号如何被"消化掉"的。

      **求值后缀表达式**：遇操作数入栈；遇运算符==弹出两个==，
      ==先弹出的是右操作数==（这一点对减法、除法至关重要），算完把结果压回去。
    ` },

    { t: 'example', id: 'ex-infix',
      title: '★ 中缀转后缀 + 后缀求值',
      source: '必考题型',
      level: 3,
      problem: String.raw`
        (1) 把中缀表达式 $\texttt{A+B*(C-D)-E/F}$ 转换成后缀表达式，写出转换过程；
        (2) 设 $A=6,B=2,C=5,D=3,E=8,F=4$，用栈求出该后缀表达式的值。
      `,
      idea: String.raw`
        (1) ==画一张三列表：扫描到的字符 / 栈内容 / 已输出==，一格一格填。
        千万不要在脑子里做，运算符栈的状态一乱就全乱了。

        **优先级约定**：$\texttt{*}\ \texttt{/}$ 高于 $\texttt{+}\ \texttt{-}$；
        ==同级运算符也要弹出==（保证左结合，即 $\texttt{A-B-C}$ 算成 $\texttt{(A-B)-C}$）。

        (2) 求值时==注意减法和除法的操作数顺序==：
        先弹出的是**右**操作数，后弹出的是**左**操作数。
      `,
      solution: String.raw`
        **(1) 转换过程**

        | 扫描 | 动作 | 栈（底→顶） | 输出 |
        |---|---|---|---|
        | A | 输出 | | A |
        | + | 栈空，入栈 | + | A |
        | B | 输出 | + | AB |
        | * | $\texttt{*}$ 优先级高于 $\texttt{+}$，不弹，入栈 | + * | AB |
        | ( | 入栈 | + * ( | AB |
        | C | 输出 | + * ( | ABC |
        | − | 栈顶是 $($，停，入栈 | + * ( − | ABC |
        | D | 输出 | + * ( − | ABCD |
        | ) | 弹到 $($ 为止 | + * | ABCD− |
        | − | 弹出 $\texttt{*}$、$\texttt{+}$（都 $\ge$ $\texttt{-}$），再入栈 | − | ABCD−*+ |
        | E | 输出 | − | ABCD−*+E |
        | / | $\texttt{/}$ 高于 $\texttt{-}$，不弹，入栈 | − / | ABCD−*+E |
        | F | 输出 | − / | ABCD−*+EF |
        | 结束 | 弹出 $\texttt{/}$、$\texttt{-}$ | | ==ABCD−*+EF/−== |

        $$\boxed{\text{后缀式}=\texttt{ABCD-*+EF/-}}$$

        **(2) 求值**（$A=6,B=2,C=5,D=3,E=8,F=4$）

        | 扫描 | 动作 | 栈（底→顶） |
        |---|---|---|
        | 6 2 5 3 | 依次入栈 | 6 2 5 3 |
        | − | 弹 3（右）、5（左）→ $5-3=2$ | 6 2 2 |
        | * | 弹 2、2 → $2\times 2=4$ | 6 4 |
        | + | 弹 4、6 → $6+4=10$ | 10 |
        | 8 4 | 入栈 | 10 8 4 |
        | / | 弹 4（右）、8（左）→ $8/4=2$ | 10 2 |
        | − | 弹 2（右）、10（左）→ $10-2=8$ | ==8== |

        $$\text{结果}=\boxed{8}$$

        验算原式：$6+2\times(5-3)-8/4=6+4-2=8$ ✓
      `,
      comment: String.raw`
        **最容易错的三处**：

        1. ==遇到同级运算符时忘了弹出==。
           如果不弹，$\texttt{A-B-C}$ 会转成 $\texttt{ABC--}$，求值时算成 $A-(B-C)$，==结果错==。
        2. ==右括号把左括号也输出了==。$($ 只是标记，==弹出后丢弃==。
        3. ==求值时把操作数顺序搞反==。
           $\texttt{53-}$ 是 $5-3=2$ 不是 $3-5=-2$，==先弹出的是右操作数==。

        **一个快速验算法**：后缀表达式里==操作数的相对顺序与中缀中完全相同==
        （本题都是 A B C D E F）。顺序变了就一定抄错了。

        **前缀（波兰式）**：把中缀==从右往左==扫、括号规则对称处理即可，
        本题的前缀式是 $\texttt{-+A*B-CD/EF}$。408 考后缀居多。
      `,
    },

    { t: 'key', id: 'recursion', title: '应用三：递归与函数调用', c: String.raw`
      **函数调用栈**：每次调用一个函数，系统就压入一个==栈帧（活动记录）==，
      里面装着==返回地址、实参、局部变量==。函数返回时弹出栈帧。

      ==这解释了为什么[递归算法的空间复杂度 = 最大递归深度](#/ds/intro/complexity?at=space-def)==。

      **递归转非递归**：把系统栈搬到明面上自己维护。
      典型例子是[二叉树的非递归遍历](#/ds/tree/traversal?at=why-stack) ——
      那里的显式栈装的正是"还没处理完右子树的祖先"，
      和系统栈装的东西一模一样。

      **递归的两个缺点**（简答题答点）：
      1. ==时间开销大==（每次调用都要保存 / 恢复现场）；
      2. ==空间开销大且有栈溢出风险==（深度过大时爆栈）。

      ==但递归的可读性通常远好于非递归==，所以不是所有递归都值得改写。
    ` },

    /* ================================================================== */
    { t: 'h', id: 'examples', c: '四、例题' },

    { t: 'example', id: 'ex-pop-seq',
      title: '★ 出栈序列的合法性',
      source: '选择题高频',
      level: 3,
      problem: String.raw`
        元素 $1,2,3,4$ 依次进栈，进栈过程中可以出栈。

        (1) 共有多少种不同的出栈序列？
        (2) 判断下列序列哪些是合法的出栈序列：
        $\texttt{1234}$、$\texttt{4321}$、$\texttt{3142}$、$\texttt{1342}$、$\texttt{4123}$。
        (3) 若某个出栈序列以 $3$ 开头，共有多少种可能？
      `,
      idea: String.raw`
        (1) 直接套[卡特兰数](#/ds/tree/properties?at=catalan-def)。

        (2) ==用模拟法，逐个走一遍最稳==。
        草稿上写两行：一行"还没进栈的"，一行"栈里的"，逐个匹配目标序列。

        (3) =="3 第一个出栈"意味着 1、2 此刻都已在栈中且顺序是 1 在下、2 在上==，
        所以之后 2 一定排在 1 前面。剩下的自由度只在 4 的位置。
      `,
      solution: String.raw`
        **(1)** $n=4$：
        $$\frac{1}{5}\binom{8}{4}=\frac{70}{5}=\boxed{14}\ \text{种}$$

        **(2)** 逐个模拟：

        | 序列 | 模拟 | 结论 |
        |---|---|---|
        | $\texttt{1234}$ | 每个进了就出 | ==合法== |
        | $\texttt{4321}$ | 全进栈再全出 | ==合法== |
        | $\texttt{3142}$ | 1,2,3 进→出 3；出 1？==栈顶是 2 不是 1== | ==非法== |
        | $\texttt{1342}$ | 1 进出；2,3 进→出 3；4 进出；出 2 | ==合法== |
        | $\texttt{4123}$ | 1,2,3,4 全进→出 4；出 1？==栈顶是 3== | ==非法== |

        **(3)** $3$ 先出栈 $\Rightarrow$ $1,2,3$ 都已进栈、$3$ 已出，此时栈内自底向上是 $1,2$。
        剩下要出的是 $1,2,4$，其中==$2$ 必须在 $1$ 之前==（$2$ 压在 $1$ 上面）。
        $4$ 可以在任意时刻进出，即插在 $2,1$ 这个序列的三个空位之一：

        $$\texttt{3421},\quad \texttt{3241},\quad \texttt{3214}$$

        共 $\boxed{3}$ 种。
      `,
      comment: String.raw`
        **(2) 的通用判据**：==若 $a$ 先于 $b$ 出栈且 $a>b$，
        则所有介于 $b$ 与 $a$ 之间的数必须在 $b$ 之前、按**递减**次序出栈==。

        用它秒判 $\texttt{3142}$：$3$ 先出、$1$ 后出且 $1<3$，
        那么介于 $1$ 和 $3$ 之间的 $2$ 必须排在 $1$ 之前 —— 但实际 $2$ 在 $1$ 之后，==非法== ✓

        用它秒判 $\texttt{4123}$：$4$ 先出、$1$ 后出，介于中间的 $2,3$
        必须在 $1$ 之前且递减（即 $3,2$）—— 实际它们在 $1$ 之后，==非法== ✓

        **考场建议**：==选项少于 5 个时直接模拟，快且不会错==；
        判据法用来做验算。
      `,
    },

    /* ================================================================== */
    { t: 'h', id: 'pitfalls', c: '五、易错清单' },

    { t: 'warn', id: 'pitfall-list', title: '这一节的固定失分点', c: String.raw`
      1. **两种 top 约定混用** —— 判满条件、进栈语句全都不同。
      2. **共享栈的判满条件写成 $\texttt{top0 == top1}$** —— ==是 $\texttt{top0 + 1 == top1}$==。
      3. **括号匹配漏了"扫完栈非空"这个失败条件**。
      4. **中缀转后缀时同级运算符不弹出** —— 会破坏左结合性。
      5. **右括号把左括号也输出了** —— $($ 弹出后==丢弃==。
      6. **后缀求值时操作数顺序反了** —— ==先弹出的是右操作数==。
      7. **出栈序列个数记成 $n!$** —— ==是卡特兰数==，$n=4$ 时是 14 不是 24。
      8. **认为栈不是线性表** —— ==栈是操作受限的线性表==，逻辑结构仍是线性的。
      9. **链栈用带头结点还去操作头结点的 data** —— 链栈通常==不带头结点==。
      10. **认为递归一定比非递归慢很多** —— 慢，但==复杂度量级通常相同==，
          差的是常数因子和空间。
    ` },

    /* ================================================================== */
    { t: 'h', id: 'notes', c: '六、我的思路记录' },

    { t: 'insight', id: 'note-slot', title: '这里放你自己的话', c: String.raw`
      栈的所有应用可以归到同一句话：
      ==当"当前的事"必须先暂停、去处理"更内层的事"，处理完再回来时，就需要栈==。

      - 括号匹配：遇到新的左括号，就得先把它闭合了才能回头管外面的；
      - 表达式求值：遇到高优先级运算符，得先算它；
      - 函数调用：调用子函数，得先执行完它才能回来。

      =="嵌套"这个词本身就等价于"需要栈"==。
    ` },

  ],
});
