/* ==========================================================================
   数据结构 / 1 绪论与算法分析 / 抽象数据类型与存储结构
   ========================================================================== */

KM.page({
  path: 'ds/intro/adt',
  title: '抽象数据类型与存储结构',
  subtitle: '逻辑结构、存储结构、运算 —— 三要素分清楚了，这一章的判断题就送分了',
  tags: ['概念辨析', '必考'],
  updated: '2026-08-21',

  blocks: [

    /* ================================================================== */
    { t: 'h', id: 'terms', c: '一、四个层层嵌套的名词' },

    { t: 'key', id: 'data-terms', title: '数据 → 数据元素 → 数据项', c: String.raw`
      | 名词 | 定义 | 例子（学生表） |
      |---|---|---|
      | **数据** | 信息的载体，能被计算机识别、存储和加工 | 整张学生表 |
      | **数据对象** | ==性质相同==的数据元素的集合 | 全体学生记录 |
      | **数据元素** | 数据的==基本单位==，通常作为整体考虑 | 一条学生记录 |
      | **数据项** | ==构成数据元素的不可分割的最小单位== | 学号、姓名、成绩 |

      =="数据元素"是基本单位，"数据项"是最小单位== —— 这两个词的定语不一样，
      判断题经常互换来考。

      **数据类型**：一个==值的集合==和定义在此集合上的==一组操作==的总称。
      - **原子类型**：值不可再分（$\texttt{int}$、$\texttt{char}$）；
      - **结构类型**：值可以再分解（数组、结构体）。
    ` },

    { t: 'key', id: 'adt-def', title: '抽象数据类型（ADT）', c: String.raw`
      **ADT** $=$ ==数据对象 $+$ 数据关系 $+$ 基本操作==，
      通常写成三元组 $(D,\ S,\ P)$。

      **"抽象"抽掉了什么**：==只描述"能做什么"，不涉及"怎么做"==。
      ADT 定义一个栈时只说"有 $\mathrm{Push}$、$\mathrm{Pop}$、后进先出"，
      ==不规定它是用数组还是链表实现的==。

      **这就是 ADT 的意义**：使用者只需要看接口，==实现可以随时替换==。
      这也是"逻辑结构与存储结构相互独立"这一原则在语言层面的体现。
    ` },

    /* ================================================================== */
    { t: 'h', id: 'three', c: '二、★ 数据结构三要素' },

    { t: 'key', id: 'three-elements', title: '逻辑结构、存储结构、数据的运算', c: String.raw`
      **① 逻辑结构** —— 数据元素之间的==逻辑关系==，==与存储无关==。

      | 大类 | 小类 | 关系 |
      |---|---|---|
      | **线性结构** | 线性表、栈、队列、串、数组 | ==一对一==（除首尾外各有一个前驱和后继） |
      | **非线性结构** | **集合** | 除"同属一个集合"外==无任何关系== |
      | | **树形结构** | ==一对多== |
      | | **图状（网状）结构** | ==多对多== |

      **② 存储结构（物理结构）** —— 数据结构在==计算机中的表示==，共四种（见下节）。

      **③ 数据的运算** —— 包括运算的==定义==和==实现==：
      - ==运算的**定义**针对**逻辑结构**==，指出运算的功能；
      - ==运算的**实现**针对**存储结构**==，给出具体步骤。

      ==第 ③ 条这句话几乎每年都以判断题形式出现，要能一字不差地复述==。
    ` },

    { t: 'diagram', id: 'orthogonal', title: '逻辑结构与存储结构是"正交"的',
      note: '任何逻辑结构都可以用任何存储结构实现',
      caption: String.raw`左边是==数据"长什么样"==，右边是==数据"怎么放"==，两者==可以任意组合==。
      树可以用[顺序存储](#/ds/tree/properties?at=index-rule)（堆），也可以用链式存储（二叉链表）；
      线性表可以顺序存（数组），也可以链式存（链表）。
      ==所以"某种逻辑结构只能用某种存储结构"这类说法一律是错的==。`,
      svg: String.raw`
<svg class="dg" viewBox="0 0 700 236" role="img" aria-label="逻辑结构与存储结构之间的多对多关系">
  <text class="cap" x="20" y="18">逻辑结构（数据长什么样）</text>
  <g class="n k"><rect x="20" y="30" width="150" height="34" rx="6"/><text class="bt sm" x="95" y="47" text-anchor="middle" dominant-baseline="central">集合</text></g>
  <g class="n k"><rect x="20" y="76" width="150" height="34" rx="6"/><text class="bt sm" x="95" y="93" text-anchor="middle" dominant-baseline="central">线性结构（一对一）</text></g>
  <g class="n k"><rect x="20" y="122" width="150" height="34" rx="6"/><text class="bt sm" x="95" y="139" text-anchor="middle" dominant-baseline="central">树形结构（一对多）</text></g>
  <g class="n k"><rect x="20" y="168" width="150" height="34" rx="6"/><text class="bt sm" x="95" y="185" text-anchor="middle" dominant-baseline="central">图状结构（多对多）</text></g>
  <path class="ar plain" d="M170,47 L306,104"/>
  <path class="ar plain" d="M170,93 L306,110"/>
  <path class="ar plain" d="M170,139 L306,122"/>
  <path class="ar plain" d="M170,185 L306,128"/>
  <g class="n a"><rect x="306" y="98" width="88" height="36" rx="7"/><text class="bt sm" x="350" y="116" text-anchor="middle" dominant-baseline="central">任意组合</text></g>
  <path class="ar plain" d="M394,104 L530,47"/>
  <path class="ar plain" d="M394,110 L530,93"/>
  <path class="ar plain" d="M394,122 L530,139"/>
  <path class="ar plain" d="M394,128 L530,185"/>
  <text class="cap" x="530" y="18">存储结构（数据怎么放）</text>
  <g class="n g"><rect x="530" y="30" width="150" height="34" rx="6"/><text class="bt sm" x="605" y="47" text-anchor="middle" dominant-baseline="central">顺序存储</text></g>
  <g class="n g"><rect x="530" y="76" width="150" height="34" rx="6"/><text class="bt sm" x="605" y="93" text-anchor="middle" dominant-baseline="central">链式存储</text></g>
  <g class="n g"><rect x="530" y="122" width="150" height="34" rx="6"/><text class="bt sm" x="605" y="139" text-anchor="middle" dominant-baseline="central">索引存储</text></g>
  <g class="n g"><rect x="530" y="168" width="150" height="34" rx="6"/><text class="bt sm" x="605" y="185" text-anchor="middle" dominant-baseline="central">散列存储</text></g>
  <text class="cap" x="20" y="224">例：树 + 顺序存储 = 堆　　线性表 + 散列存储 = 散列表</text>
</svg>
` },

    /* ================================================================== */
    { t: 'h', id: 'storage', c: '三、四种存储结构' },

    { t: 'compare', id: 'four-storage', title: '四种存储结构对照',
      cols: ['', '怎么放', '优点', '缺点'],
      rows: [
        ['**顺序存储**', '==逻辑相邻的元素物理上也相邻==，用相对位置表示关系',
         '==可随机存取==，每个元素占空间最少', '==只能用整块连续空间==，会产生外部碎片'],
        ['**链式存储**', '各元素可放在任意位置，用==指针==表示关系',
         '==不要求连续空间==，插入删除灵活', '==只能顺序存取==；指针占额外空间'],
        ['**索引存储**', '存数据的同时另建==索引表==（关键字 + 地址）',
         '==检索速度快==', '索引表占额外空间；==增删时还要改索引表=='],
        ['**散列存储**', '==由关键字直接计算出存储地址==',
         '增删改查都很快', '可能[冲突](#/ds/search/hash?at=basic)，处理冲突增加时空开销'],
      ] },

    { t: 'key', id: 'sequential-key', title: '顺序存储的本质：用"位置"编码"关系"', c: String.raw`
      顺序存储==不需要任何额外的字段来记录"谁是谁的后继"==
      —— 后继就是"下一个格子"，这个信息由==地址本身==承载。

      $$\mathrm{LOC}(a_i)=\mathrm{LOC}(a_1)+(i-1)\times L$$

      正因为地址可以直接算出来，==顺序存储支持随机存取（$O(1)$ 取第 $i$ 个）==。

      **代价**是"物理相邻"这个约束太硬：
      - 必须一次性拿到==一整块==连续空间；
      - ==插入 / 删除要挪动大量元素==来维持相邻关系。

      链式存储把关系写进了指针里，于是摆脱了这个约束，
      但也==失去了"算出地址"的能力，只能顺着链一个个走==。
    ` },

    /* ================================================================== */
    { t: 'h', id: 'examples', c: '四、例题' },

    { t: 'example', id: 'ex-judge',
      title: '★ 五个判断题（这一章的全部考法）',
      source: '选择/判断',
      level: 2,
      problem: String.raw`
        判断下列说法是否正确并说明理由：

        (1) 数据的逻辑结构是指数据的各数据项之间的逻辑关系；
        (2) 数据的存储结构是数据的逻辑结构在计算机中的表示；
        (3) 数据结构的基本运算的实现只与逻辑结构有关；
        (4) 顺序存储方式只能用于线性结构，不能用于非线性结构；
        (5) 顺序存储的元素之间的关系由存储单元的邻接关系来体现。
      `,
      idea: String.raw`
        这五道题全都围绕==三要素的边界==，逐个对照定义即可：

        - "逻辑结构"讲的是==数据元素==之间的关系，不是数据项；
        - "运算的定义"看逻辑结构，"运算的实现"看存储结构；
        - ==逻辑结构和存储结构是正交的==，不存在"只能用"。
      `,
      solution: String.raw`
        **(1) 错。** 逻辑结构描述的是==数据元素==之间的关系，
        ==不是"数据项"之间的关系==。数据项是数据元素内部的最小单位。

        **(2) 对。** 这就是存储结构（物理结构）的定义原句。

        **(3) 错。** ==运算的**定义**针对逻辑结构，运算的**实现**针对存储结构==。
        同一个"插入"运算，在顺序表上要移动元素，在链表上只改指针，实现完全不同。

        **(4) 错。** ==非线性结构同样可以顺序存储==。
        典型例子：[完全二叉树用数组存](#/ds/tree/properties?at=index-rule)（堆），
        [图用邻接矩阵存](#/ds/graph/storage?at=matrix-def)。

        **(5) 对。** 顺序存储==正是靠"物理相邻"来表达"逻辑相邻"==的，
        不需要额外的指针字段。
      `,
      comment: String.raw`
        **(3) 是最容易被"想当然"坑掉的一题**。
        记一句口诀：=="定义看逻辑，实现看存储"==。

        **(4) 的变形问法**："链式存储只能用于线性结构吗？"
        同样是==错==（二叉链表、邻接表都是非线性结构的链式存储）。
        ==凡是出现"只能"两个字的，先怀疑它是错的==。
      `,
    },

    { t: 'example', id: 'ex-choose-storage',
      title: '给场景选存储结构',
      source: '概念应用',
      level: 2,
      problem: String.raw`
        为下列场景选择最合适的存储结构并说明理由：

        (1) 一个学生成绩表，建立后基本不变，需要频繁地按序号取出第 $k$ 个学生；
        (2) 一个任务队列，任务不断到达和完成，数量在几十到几万之间剧烈波动；
        (3) 一个用户表，需要根据用户 ID 快速定位到该用户的记录；
        (4) 一本书的正文，需要能快速跳到某一章。
      `,
      idea: String.raw`
        对每个场景问三件事：
        1. ==要不要随机存取==？要 → 倾向顺序 / 散列；
        2. ==增删频不频繁==？频繁 → 倾向链式；
        3. ==是不是"按关键字找"而不是"按位置找"==？是 → 散列或索引。
      `,
      solution: String.raw`
        **(1) 顺序存储。** =="基本不变" + "按序号取第 $k$ 个"== 正是顺序存储的强项：
        随机存取 $O(1)$，且没有插入删除的移动开销。

        **(2) 链式存储。** ==数量剧烈波动==意味着无法预先分配合适的连续空间，
        而且频繁增删在顺序表上要大量移动元素。链式存储==按需分配、增删只改指针==。

        **(3) 散列存储。** ==按关键字（用户 ID）定位==是散列表的定义性场景，
        平均 $O(1)$。若还需要按 ID 范围查询，则应改用
        [B+ 树式的索引](#/ds/search/btree?at=bplus-why)。

        **(4) 索引存储。** 正文本身顺序存放，另建一张==章节索引表==（章号 → 起始位置）。
        这正是[分块查找](#/ds/search/binary?at=block-def)的思想。
      `,
      comment: String.raw`
        **(3) 与 (4) 的分界**是这类题的关键：
        ==只要"精确匹配单个关键字"就用散列；要"范围 / 有序"就用索引或树==。
        散列把顺序彻底打散了，换不回来。

        **一个补充**：现实系统常常==多种结构叠用==
        （例如数据库表用顺序存储数据页 + B+ 树索引 + 散列做缓存），
        考试问"最合适"时按主要矛盾答即可。
      `,
    },

    /* ================================================================== */
    { t: 'h', id: 'pitfalls', c: '五、易错清单' },

    { t: 'warn', id: 'pitfall-list', title: '这一节的固定失分点', c: String.raw`
      1. **逻辑结构说成"数据项之间的关系"** —— 是==数据元素==之间的关系。
      2. **"数据元素是最小单位"** —— 数据元素是==基本单位==，==数据项才是最小单位==。
      3. **"运算的实现只与逻辑结构有关"** —— ==定义看逻辑，实现看存储==。
      4. **"顺序存储只能存线性结构"** —— ==非线性也能==（堆、邻接矩阵）。
      5. **"链式存储只能存线性结构"** —— 同样错。
      6. **把"数据类型"和"抽象数据类型"当同义词** —— ==ADT 强调"与实现无关"==。
      7. **认为存储结构变了逻辑结构就变了** —— ==逻辑结构独立于存储结构==。
      8. **索引存储 = 散列存储** —— ==索引要另建表并保持有序，散列直接算地址==。
      9. **认为顺序存储一定比链式省空间** —— 顺序表==预分配可能大量闲置==，
         而链表虽有指针开销却按需分配。
      10. **算法的"输出"可以是 0 个** —— [至少 1 个](#/ds/intro/complexity?at=five-features)。
    ` },

    /* ================================================================== */
    { t: 'h', id: 'notes', c: '六、我的思路记录' },

    { t: 'insight', id: 'note-slot', title: '这里放你自己的话', c: String.raw`
      整章的判断题都可以用一句话过滤：
      ==凡是把"逻辑"和"存储"绑死的说法，几乎都是错的==。

      这两层之所以要分开，是因为它们回答的是不同的问题：
      逻辑结构回答"==这些数据之间是什么关系=="，
      存储结构回答"==这些关系用什么手段在内存里表达出来=="。
      一个关系可以有很多种表达手段 —— 这就是"正交"的含义。
    ` },

  ],
});
