/* ==========================================================================
   数据结构 / 1 绪论与算法分析 / 抽象数据类型与存储结构
   ========================================================================== */

KM.page({
  path: 'ds/intro/adt',
  title: '抽象数据类型与存储结构',
  subtitle: '逻辑结构、存储结构、运算 —— 以及「顺序表」和「顺序存储结构」这两个必须分开的概念层级',
  tags: ['概念辨析', '必考', '易错'],
  updated: '2026-08-22',

  blocks: [

    /* ================================================================== */
    { t: 'h', id: 'terms', c: '一、四个层层嵌套的名词' },

    { t: 'key', id: 'data-terms', title: '数据 → 数据元素 → 数据项', c: String.raw`
      | 名词 | 定义 | 例子（学生表） |
      |---|---|---|
      | **数据** | 信息的载体，能被计算机识别、存储和加工 | 整张学生表 |
      | **数据对象** | ==性质相同==的数据元素的集合 | 全体学生记录 |
      | **数据元素** | 数据的==基本单位==，通常作为整体考虑 | 一条学生记录 |
      | **数据项** | ==构成数据元素的不可分割的最小单位== | 学号、姓名、成绩 |

      =="数据元素"是基本单位，"数据项"是最小单位== —— 这两个词的定语不一样，
      判断题经常互换来考。

      **数据类型**：一个==值的集合==和定义在此集合上的==一组操作==的总称。
      - **原子类型**：值不可再分（$\texttt{int}$、$\texttt{char}$）；
      - **结构类型**：值可以再分解（数组、结构体）。
    ` },

    { t: 'key', id: 'adt-def', title: '抽象数据类型（ADT）', c: String.raw`
      **ADT** $=$ ==数据对象 $+$ 数据关系 $+$ 基本操作==，
      通常写成三元组 $(D,\ S,\ P)$。

      **"抽象"抽掉了什么**：==只描述"能做什么"，不涉及"怎么做"==。
      ADT 定义一个栈时只说"有 $\mathrm{Push}$、$\mathrm{Pop}$、后进先出"，
      ==不规定它是用数组还是链表实现的==。

      **这就是 ADT 的意义**：使用者只需要看接口，==实现可以随时替换==。
      这也是"逻辑结构与存储结构相互独立"这一原则在语言层面的体现。
    ` },

    /* ================================================================== */
    { t: 'h', id: 'three', c: '二、★ 数据结构三要素' },

    { t: 'key', id: 'three-elements', title: '逻辑结构、存储结构、数据的运算', c: String.raw`
      **① 逻辑结构** —— 数据元素之间的==逻辑关系==，==与存储无关==。

      | 大类 | 小类 | 关系 |
      |---|---|---|
      | **线性结构** | 线性表、栈、队列、串、数组 | ==一对一==（除首尾外各有一个前驱和后继） |
      | **非线性结构** | **集合** | 除"同属一个集合"外==无任何关系== |
      | | **树形结构** | ==一对多== |
      | | **图状（网状）结构** | ==多对多== |

      **② 存储结构（物理结构）** —— 数据结构在==计算机中的表示==，共四种（见[第四节](#/ds/intro/adt?at=storage)）。

      **③ 数据的运算** —— 包括运算的==定义==和==实现==：
      - ==运算的**定义**针对**逻辑结构**==，指出运算的功能；
      - ==运算的**实现**针对**存储结构**==，给出具体步骤。

      ==第 ③ 条这句话几乎每年都以判断题形式出现，要能一字不差地复述==。
    ` },

    { t: 'diagram', id: 'orthogonal', title: '逻辑结构与存储结构大体是"正交"的',
      note: '实线 = 可独立承载；虚线 = 必须依附顺序或链式',
      caption: String.raw`左边是==数据"长什么样"==，右边是==数据"怎么放"==。
      树可以用[顺序存储](#/ds/tree/properties?at=index-rule)（堆），也可以用链式存储（二叉链表）；
      线性表可以顺序存（数组），也可以链式存（链表）。
      ==所以"某种逻辑结构只能用某种存储结构"这类说法一律是错的==。

      但==虚线那两条要打个折扣==：索引和散列==无法独立表达"前驱后继""父子"这类拓扑关系==，
      必须借助顺序或链式才能撑起非集合的逻辑结构。详见[下一节](#/ds/intro/adt?at=orthogonal-limit)。`,
      svg: String.raw`
<svg class="dg" viewBox="0 0 700 244" role="img" aria-label="逻辑结构与存储结构之间的多对多关系，以及索引与散列的局限">
  <text class="cap" x="20" y="18">逻辑结构（数据长什么样）</text>
  <g class="n k"><rect x="20" y="30" width="150" height="34" rx="6"/><text class="bt sm" x="95" y="47" text-anchor="middle" dominant-baseline="central">集合</text></g>
  <g class="n k"><rect x="20" y="76" width="150" height="34" rx="6"/><text class="bt sm" x="95" y="93" text-anchor="middle" dominant-baseline="central">线性结构（一对一）</text></g>
  <g class="n k"><rect x="20" y="122" width="150" height="34" rx="6"/><text class="bt sm" x="95" y="139" text-anchor="middle" dominant-baseline="central">树形结构（一对多）</text></g>
  <g class="n k"><rect x="20" y="168" width="150" height="34" rx="6"/><text class="bt sm" x="95" y="185" text-anchor="middle" dominant-baseline="central">图状结构（多对多）</text></g>
  <path class="ar plain" d="M170,47 L306,104"/>
  <path class="ar plain" d="M170,93 L306,110"/>
  <path class="ar plain" d="M170,139 L306,122"/>
  <path class="ar plain" d="M170,185 L306,128"/>
  <g class="n a"><rect x="306" y="98" width="88" height="36" rx="7"/><text class="bt sm" x="350" y="116" text-anchor="middle" dominant-baseline="central">组合</text></g>
  <path class="ar plain" d="M394,104 L530,47"/>
  <path class="ar plain" d="M394,110 L530,93"/>
  <path class="ar dash plain" d="M394,122 L530,139"/>
  <path class="ar dash plain" d="M394,128 L530,185"/>
  <text class="cap" x="530" y="18">存储结构（数据怎么放）</text>
  <g class="n g"><rect x="530" y="30" width="150" height="34" rx="6"/><text class="bt sm" x="605" y="47" text-anchor="middle" dominant-baseline="central">顺序存储</text></g>
  <g class="n g"><rect x="530" y="76" width="150" height="34" rx="6"/><text class="bt sm" x="605" y="93" text-anchor="middle" dominant-baseline="central">链式存储</text></g>
  <g class="n m"><rect x="530" y="122" width="150" height="34" rx="6"/><text class="bt sm" x="605" y="139" text-anchor="middle" dominant-baseline="central">索引存储</text></g>
  <g class="n m"><rect x="530" y="168" width="150" height="34" rx="6"/><text class="bt sm" x="605" y="185" text-anchor="middle" dominant-baseline="central">散列存储</text></g>
  <text class="cap" x="20" y="224">例：树 + 顺序存储 = 堆　　线性表 + 链式存储 = 单链表</text>
  <text class="lb em" x="20" y="240">虚线：索引 / 散列不能独立表达拓扑关系，须依附顺序或链式</text>
</svg>
` },

    /* ================================================================== */
    { t: 'h', id: 'seq-vs-seqlist', c: '三、★★ 顺序表 ≠ 顺序存储结构' },

    { t: 'key', id: 'level-confusion', title: '先把三组概念的层级摆正', c: String.raw`
      这一节是整章最容易出事的地方，==根源在于"顺序表"和"顺序存储结构"被当成了同义词==。

      $$\underbrace{\text{线性表}}_{\text{逻辑结构}}\ \xrightarrow{\ \text{用顺序存储实现}\ }\ \textbf{顺序表}
      \qquad
      \underbrace{\text{线性表}}_{\text{逻辑结构}}\ \xrightarrow{\ \text{用链式存储实现}\ }\ \textbf{单链表}$$

      **由此得到三条结论**：

      1. ==顺序表和单链表是**平级**的==，都是"线性表"这个逻辑结构的物理实现，
         ==两者在物理存储上互斥，单链表绝不属于顺序表==；
      2. ==顺序表 = "顺序存储结构"这一机制在"线性逻辑"下的一个具体应用==，
         而不是这个机制的全部；
      3. ==链式存储结构 $\ne$ 单链表==。
         二叉链表、十字链表、邻接表、邻接多重表==全都是链式存储结构==。

      **考场上的直接后果**：题目说"采用链式存储结构"时，
      ==你可以用二叉链表去存一棵[二叉排序树](#/ds/tree/bst?at=def)==，
      而不是被迫只用单链表。见[本页的例题](#/ds/intro/adt?at=ex-asl)。
    ` },

    { t: 'key', id: 'seqlist-def', title: '顺序表的准确含义', c: String.raw`
      **顺序表**特指"用==一段地址连续的存储单元==，依次存储线性表的数据元素"。

      两个核心特征：

      1. ==物理位置连续==：逻辑上相邻的两个元素，在内存中的地址也必然相邻；
      2. ==支持随机存取==：
         $$\mathrm{LOC}(a_i)=\mathrm{LOC}(a_1)+(i-1)\times L$$
         知道基地址就能 $O(1)$ 算出任意第 $i$ 个元素的地址。

      在 C/C++/Java 里，顺序表的典型实现就是==一维数组==。
      （详见[顺序表那一页](#/ds/list/seq-list?at=random-access)。）
    ` },

    { t: 'key', id: 'seq-storage-broad', title: '★ 顺序存储结构的**广义**定义（两个条件）', c: String.raw`
      作为四大物理存储机制之一，顺序存储结构的通用定义==只有两条==：

      1. **载体连续** —— 使用一段==地址连续==的物理存储单元存放数据；
      2. **关系隐式化（核心）** —— 数据元素之间的逻辑关系，
         ==完全依靠数据在这段连续空间里的物理位置（下标 / 相对地址）来隐式表达，
         绝不借助任何显式指针==（$\texttt{*next}$、$\texttt{*lchild}$ 之类）。

      $$\boxed{\text{顺序存储}=\text{空间连续}\ +\ \text{靠下标映射维系逻辑关系}}$$

      ==注意第 2 条没有要求"逻辑相邻的元素在物理上必须挨着"==。
      挨不挨着，取决于上层是什么逻辑结构 —— 这正是下一块要说的事。
    ` },

    { t: 'warn', id: 'narrow-vs-broad', title: '★★ "逻辑相邻 ⟹ 物理相邻"只是**线性逻辑**下的特例', c: String.raw`
      很多资料（也包括很多老师的口头表述）会说：

      > "顺序存储结构的存放位置取决于元素在序列中的逻辑序号；
      > 元素 $A$ 和 $B$ 如果逻辑上相邻，物理上也必然相邻。"

      ==这句话是**顺序表**的描述，不是**顺序存储结构**的通用定义==。
      把它当成通用定义，就会在遇到堆、并查集、邻接矩阵时自相矛盾。

      | | 承载**线性**结构（顺序表） | 承载**非线性**结构（堆 / 并查集 / 邻接矩阵） |
      |---|---|---|
      | 逻辑关系 | 一维的前驱后继 | ==二维的父子 / 邻接== |
      | 用什么表达 | ==物理上严格挨着==，$\mathrm{LOC}(a_{i+1})=\mathrm{LOC}(a_i)+L$ | ==一套基于下标的数学映射公式== |
      | "逻辑相邻 ⟹ 物理相邻" | ==成立== | ==不成立== |

      **为什么非线性时不成立**：一维的连续内存==不可能让所有父子对都物理相邻==
      （一个父亲有两个孩子，最多只能挨着一个）。
      于是它退而求其次 —— ==既然挨不着，那就用公式算==，
      比如完全二叉树的 $2i$ / $2i+1$。==没用一根指针，所以仍然是顺序存储==。

      **一句话收束**：
      - ==狭义（专指顺序表）==：顺序存储 $=$ 逻辑相邻 $\Rightarrow$ 物理相邻；
      - ==广义（底层机制）==：顺序存储 $=$ 空间连续 $+$ 靠位置下标映射逻辑关系，==不限于相邻==。
    ` },

    { t: 'compare', id: 'what-seq-can-hold', title: '★ 顺序存储结构能承载哪些逻辑结构（远不止顺序表）',
      cols: ['逻辑结构', '顺序存储的实现', '靠什么维系逻辑关系'],
      rows: [
        ['线性表', '**顺序表**', '$\\mathrm{LOC}(a_1)+(i-1)L$，==物理相邻=='],
        ['栈', '**[顺序栈](#/ds/stack-queue/stack?at=two-top)**', '数组 + 一个 `top` 下标'],
        ['队列', '**[循环队列](#/ds/stack-queue/queue?at=circular-demo)**', '数组 + `front`/`rear` 下标，靠==取模==首尾相接'],
        ['串', '**定长顺序串**', '字符数组，下标即位置'],
        ['树（完全二叉树）', '**[堆](#/ds/sort/select-heap?at=heap-def)**', '==父 $\\lfloor i/2\\rfloor$、左 $2i$、右 $2i+1$=='],
        ['树 / 森林', '**[双亲表示法、并查集](#/ds/tree/union-find?at=forest-rep)**', '==数组的"值"记录父结点的下标=='],
        ['图', '**[邻接矩阵](#/ds/graph/storage?at=matrix-def)**', '二维数组按行主序连续存放，$A[i][j]$ 表示边'],
        ['特殊矩阵', '**[压缩存储](#/ds/stack-queue/special-matrix?at=derive-any)**', '一套下标映射公式，如 $k=2i+j-3$'],
      ] },

    { t: 'key', id: 'why-level-order', title: '为什么完全二叉树的顺序存储必须用"层序"，中序不行', c: String.raw`
      标准做法确实是==把层序遍历中相邻的结点，放进物理上相邻的存储单元==。
      但==选层序不是因为"只有层序才叫顺序存储"，而是因为只有它能提供推导公式==。

      **反证一下**：假设改用==中序==把二叉树塞进连续数组。

      - 物理空间仍然连续 ✓
      - 也没有用任何指针 ✓
      - 但是 —— ==给定下标 $i$，你无法用一个通用公式算出它的左右孩子在哪==。

      失去了"由位置推导关系"的能力，这个数组就==退化成了一个普通的有序序列==，
      ==不再是"树的顺序存储"，只是把树的一个遍历结果存了下来==。

      $$\boxed{\text{关键不在于"用了哪种顺序"，而在于"这种顺序能不能反推出逻辑关系"}}$$

      **对照并查集**：它的父子结点在数组里==通常并不相邻==
      （$\texttt{parent[5]=1}$ 时，下标 5 和 1 隔得很远），
      但它依然是顺序存储 —— 因为"谁是谁的父亲"这件事，
      ==完全由"下标 $\to$ 值"这个机制表达，一根指针都没用==。
    ` },

    /* ================================================================== */
    { t: 'h', id: 'why-hash-separate', c: '四、★★ 正交性的边界与散列存储的定位' },

    { t: 'key', id: 'textbook-view', title: '先说考试口径：逻辑结构与存储结构相互独立', c: String.raw`
      教材和考纲的标准说法是：

      > ==同一种逻辑结构可以采用不同的存储结构来实现；
      > 存储结构的改变不影响逻辑结构。==

      ==选择题、判断题一律按这句话作答==，
      例如"顺序存储只能用于线性结构"必须答==错==（堆、邻接矩阵都是反例）。

      下面几块是对这句话的==边界补充==，属于超出考纲的深入辨析。
      ==理解它们能让你不被绕进去，但答题时不要把它们写成"教材错了"==。
    ` },

    { t: 'key', id: 'orthogonal-limit', title: '★ 严格地说，真正与逻辑结构正交的只有两种存储基元', c: String.raw`
      "任何逻辑结构都能用任何存储结构实现"这句话，==如果一字不改地当成定理，是站不住的==。

      **问题出在散列上**：散列存储的机制是 $\text{地址}=H(key)$，它==故意打乱了元素间的相对位置==，
      而且==不使用任何指针==。于是它==根本没有任何手段去表达"谁是谁的前驱""谁是谁的父亲"==。

      $$\boxed{\text{纯散列存储只能无损地表达四种逻辑结构中的**集合**}}$$

      因为==集合恰好是唯一一种"不需要任何拓扑关系"的逻辑结构==
      —— 元素只要"同属一个整体"且"无序"，散列表完全能胜任。
      而线性表、树、图都要求维系拓扑，散列表做不到。

      **由此得到一个更严谨的分层**：

      | 类别 | 成员 | 回答的问题 | 与逻辑结构的关系 |
      |---|---|---|---|
      | **拓扑组织基元** | ==顺序存储、链式存储== | "如何在内存中**构建**元素间的关系" | ==完全正交，任意组合均成立== |
      | **检索优化策略** | ==索引存储、散列存储== | "如何快速通过**内容**定位数据" | ==不具备正交普适性，须依附于前两者== |

      **为什么顺序和链式真的能"任意组合"**：物理内存本身是一维线性的，
      所有非线性结构最终都要被"降维"映射进去。而这两种基元互相都能模拟对方 ——
      ==顺序可以用下标模拟指针（[静态链表](#/ds/list/linked-list?at=static-list)），
      链式可以用指针串出线性顺序==。只要"关系"能被记录下来，任何拓扑都能还原。
    ` },

    { t: 'warn', id: 'virtual-pointer', title: '★ 为什么"给散列表加个 Next 字段"是偷换概念', c: String.raw`
      有人会说：散列表也能表达树啊 —— 每个结点里存一个 $\texttt{Parent\_ID}$ 不就行了？

      ==这个反驳不成立==。虽然 $\texttt{Parent\_ID}$ 不是 C 语言层面的真实内存指针（$\texttt{*parent}$），
      但它==在逻辑上就是一个"虚拟指针"==，原理和[静态链表的游标](#/ds/list/linked-list?at=static-list)一模一样。

      一旦加了这种字段，底层实际上已经变成了

      $$\underbrace{\text{散列结构}}_{\text{当结点池 / 负责定位}}\ +\ \underbrace{\text{静态链式结构}}_{\text{负责表达关系}}$$

      ==这是一个**复合**存储结构，不再是"纯散列"==。
      把它论证成"散列存储实现了树"，==等于把复合概念降维成单一概念==，逻辑上是循环论证。

      **同样的道理适用于索引存储**：索引表本身要==按关键字有序排列或另建结构==，
      它解决的是"快速定位"，==不解决"元素之间是什么关系"==。
    ` },

    { t: 'key', id: 'hash-not-seq', title: '底层都是数组，凭什么不算顺序存储', c: String.raw`
      **一个很自然的疑问**：用开放定址法实现的散列表，底层就是一个连续数组、
      也没用任何指针，==按上面的广义定义，它似乎完全符合"顺序存储"==。

      在工程语境下这么说没问题。但在数据结构的理论分类里，
      ==顺序、链式、索引、散列是**四种平级且互斥**的物理存储结构==，
      散列必须单列。理由有两条，==第一条是本质的==：

      **① 地址映射的"自变量"根本不同**

      $$\text{顺序存储：}\ \text{物理地址}=f(\ \underbrace{\text{逻辑位置}}_{\text{排第几}}\ )$$
      $$\text{散列存储：}\ \text{物理地址}=H(\ \underbrace{\text{关键字}}_{\text{值是多少}}\ )$$

      顺序存储里，==元素存在哪只跟"它在逻辑结构中的位置编号"有关，跟它的值毫无关系==；
      散列存储里恰好反过来 —— ==存在哪完全由值决定，跟任何逻辑排位无关==。

      **② 两者的目的相反**

      - ==顺序存储的目的是"**保存**结构"==：
        必须能从相对位置准确还原出前驱后继、父子等拓扑关系；
      - ==散列存储的目的是"**抛弃**结构，换取 $O(1)$ 寻址"==：
        它==故意打乱了元素之间的一切逻辑关系==，散列表里的元素退化成一个无序集合。

      **③ 一条补充理由**：散列存储在概念上==必然包含[拉链法](#/ds/search/hash?at=chaining)==，
      而拉链法是"数组 + 链表"，==用了大量显式指针==，
      已经突破了顺序存储"不用指针"的底线。
    ` },

    { t: 'diagram', id: 'mapping-var', title: '两种映射的自变量',
      note: '这是散列存储必须单列的根本原因',
      caption: String.raw`==同样是往一块连续内存里放数据，"根据什么决定放哪"完全不同==。
      顺序存储问的是"你排第几"，散列存储问的是"你的值是多少"。
      前者==保住了逻辑关系==（所以能还原出前驱后继、父子），
      后者==把逻辑关系彻底抹掉了==，换来了 $O(1)$ 的直接寻址。`,
      svg: String.raw`
<svg class="dg" viewBox="0 0 700 212" role="img" aria-label="顺序存储与散列存储在地址映射自变量上的差别">
  <text class="cap" x="20" y="20">顺序存储</text>
  <g class="n k"><rect x="20" y="30" width="120" height="38" rx="6"/><text class="bt sm" x="80" y="49" text-anchor="middle" dominant-baseline="central">逻辑位置 i</text></g>
  <path class="ar" d="M140,49 H172"/>
  <g class="n p"><rect x="172" y="30" width="196" height="38" rx="6"/><text class="bt sm" x="270" y="49" text-anchor="middle" dominant-baseline="central">LOC(a₁) + (i−1)×L</text></g>
  <path class="ar" d="M368,49 H400"/>
  <g class="n g"><rect x="400" y="30" width="110" height="38" rx="6"/><text class="bt sm" x="455" y="49" text-anchor="middle" dominant-baseline="central">物理地址</text></g>
  <text class="lb em" x="524" y="44">自变量 = 排第几</text>
  <text class="lb" x="524" y="62">与元素的值无关</text>
  <path class="sep" d="M20,90 H680"/>
  <text class="cap" x="20" y="114">散列存储</text>
  <g class="n a"><rect x="20" y="124" width="120" height="38" rx="6"/><text class="bt sm" x="80" y="143" text-anchor="middle" dominant-baseline="central">关键字 key</text></g>
  <path class="ar" d="M140,143 H172"/>
  <g class="n p"><rect x="172" y="124" width="196" height="38" rx="6"/><text class="bt sm" x="270" y="143" text-anchor="middle" dominant-baseline="central">H(key) = key mod p</text></g>
  <path class="ar" d="M368,143 H400"/>
  <g class="n g"><rect x="400" y="124" width="110" height="38" rx="6"/><text class="bt sm" x="455" y="143" text-anchor="middle" dominant-baseline="central">物理地址</text></g>
  <text class="lb em" x="524" y="138">自变量 = 值是多少</text>
  <text class="lb" x="524" y="156">与逻辑排位无关</text>
  <text class="cap" x="20" y="192">前者保住逻辑关系（能还原前驱后继、父子）　后者抹掉逻辑关系（换来 O(1) 寻址）</text>
</svg>
` },

    /* ================================================================== */
    { t: 'h', id: 'storage', c: '五、四种存储结构' },

    { t: 'compare', id: 'four-storage', title: '四种存储结构对照',
      cols: ['', '怎么放', '优点', '缺点'],
      rows: [
        ['**顺序存储**', '连续空间，==靠位置下标隐式表达逻辑关系==',
         '==可随机存取==，每个元素占空间最少', '==只能用整块连续空间==，会产生外部碎片'],
        ['**链式存储**', '各元素可放在任意位置，用==显式指针==表示关系',
         '==不要求连续空间==，插入删除灵活', '==只能顺序存取==；指针占额外空间'],
        ['**索引存储**', '存数据的同时另建==索引表==（关键字 + 地址）',
         '==检索速度快==', '索引表占额外空间；==增删时还要改索引表=='],
        ['**散列存储**', '==由关键字直接计算出存储地址==',
         '增删改查都很快', '可能[冲突](#/ds/search/hash?at=basic)；==逻辑关系被抹掉，不能范围查找=='],
      ] },

    /* ================================================================== */
    { t: 'h', id: 'examples', c: '六、例题' },

    { t: 'example', id: 'ex-avl-in-array',
      title: '★★ 非要用顺序存储实现一棵平衡二叉搜索树，能做到吗',
      source: '概念深挖',
      level: 4,
      problem: String.raw`
        [AVL 树](#/ds/tree/bst?at=avl-def)通常用二叉链表实现。
        如果==严格只允许使用一段连续内存（一维数组）==，还能实现平衡二叉搜索树吗？
        若能，代价是什么？
      `,
      idea: String.raw`
        =="能不能"和"划不划算"是两个问题，要分开答==。

        关键在于问自己：==顺序存储用什么表达父子关系？==
        [前面说过](#/ds/intro/adt?at=seq-storage-broad)有两条路 ——

        - **纯粹派**：完全不存任何下标，==只靠物理位置算==（$2i$ / $2i+1$，即堆的做法）；
        - **务实派**：==在数组里存下标，用下标当"指针"用==（[静态链表](#/ds/list/linked-list?at=static-list)）。

        两条路都能做出来，但==代价截然不同==，而且==概念纯度也不一样==。
      `,
      solution: String.raw`
        **结论：能实现，且有两种方案。**

        ---

        **方案一：纯物理位置映射（原教旨的顺序存储）**

        - **做法**：借鉴[堆](#/ds/sort/select-heap?at=heap-def)，把二叉树按完全二叉树骨架塞进数组：
          结点在下标 $i$，则左孩子固定在 $2i$、右孩子固定在 $2i+1$，==没有结点的位置留空==。
          ==全程不存任何指针或下标字段==，父子关系纯靠算术推出。

        - **怎么做旋转**：不能改指针，==只能物理搬移数组元素==。
          一次旋转要把整棵子树按新的 $2i/2i+1$ 规则重算下标、逐个复制到新位置、再清空旧位置。

        - **时间代价**：链式结构里 $O(1)$ 就能完成的[旋转](#/ds/tree/bst?at=rot-code)，
          这里要搬动多达 $O(n)$ 个结点。
          $$\text{插入 / 删除：}O(\log n)\ \longrightarrow\ \boxed{O(n)}$$

        - **空间代价（更致命）**：AVL 树==平衡但通常不是完全二叉树==，
          而数组必须按==树高==开满。由 $h<1.44\log_2(n+2)$ 得
          $$\text{数组容量}=2^h-1\approx 2^{1.44\log_2 n}=\boxed{n^{1.44}}$$
          **代入 $n=1000$**：AVL 的最大高度是 $14$（因为 $N_{14}=986\le 1000<1596=N_{15}$，
          见[最少结点数表](#/ds/tree/bst?at=avl-min-nodes)），
          于是数组要开 $2^{14}-1=16383$ 个格子 ——
          ==装 1000 个结点，开了 1.6 万个空间，超过 93% 是空洞==。

        ---

        **方案二：静态链表式（工程上的常用手法）**

        - **做法**：连续开一块 $\texttt{Node tree[MAXSIZE]}$，每个元素是一个结构体
          $$[\ \texttt{data},\ \texttt{left},\ \texttt{right},\ \texttt{bf}\ ]$$
          其中 $\texttt{left}$、$\texttt{right}$ 存的是==数组下标==，用来代替指针；
          另外维护一个"空闲结点池"来实现 $O(1)$ 的分配与回收。

        - **怎么做旋转**：==只修改 $\texttt{left}$ / $\texttt{right}$ 两个整数==，
          ==数据本身在数组里的物理位置完全不动==。

        - **代价**：时间和空间都保持理想的 $O(\log n)$ 与 $O(n)$，==没有任何退化==。

        - **额外好处**：所有结点挤在一块连续内存里，
          ==空间局部性（Cache 命中率）远好于在堆上四处分散的真实指针结构==。

        ---

        **两种方案的定性**：

        | | 方案一 | 方案二 |
        |---|---|---|
        | 物理内存连续 | ✅ | ✅ |
        | 是否存了"下标字段" | ==❌ 完全不存== | ==✅ 存了 left / right== |
        | 概念纯度 | ==纯顺序存储== | 物理上是顺序存储，==逻辑内核借用了链式思想== |
        | 插入 / 删除 | ==$O(n)$== | ==$O(\log n)$== |
        | 空间 | ==$O(n^{1.44})$== | $O(n)$ |
      `,
      comment: String.raw`
        **这道题和[下一道 ASL 真题](#/ds/intro/adt?at=ex-asl)是同源的**。
        那道题第 (1) 问里"最优 BST 映射到数组需要 6 个格子（$\texttt{repeat}$ 落在下标 6）"，
        ==正是方案一空间膨胀的最小实例== —— 4 个结点就要 6 个格子，膨胀率已经 1.5 倍了。

        **它也回答了[为什么完全二叉树的顺序存储必须用层序](#/ds/intro/adt?at=why-level-order)**：
        方案一之所以还能算"树的顺序存储"，==全靠 $2i/2i+1$ 这个可推导的映射==；
        一旦树不再是完全二叉树，这个映射就开始浪费空间；
        一旦换成中序之类的映射，连推导能力都没了。

        **一个必须留意的定性问题**：==方案二（静态链表）到底算顺序存储还是链式存储？==
        - 408 教材通常把[静态链表归为顺序存储](#/ds/list/linked-list?at=static-list)（因为用数组、空间连续）；
        - 但它的逻辑内核==确实是链式的==（靠"游标"而不是"位置"表达关系）。

        ==考试按教材答"顺序存储"==；但要知道这是一个==处在两类之间的混合体==，
        它恰好说明了[顺序 / 链式这两个基元可以互相模拟](#/ds/intro/adt?at=orthogonal-limit)。
      `,
    },

    { t: 'example', id: 'ex-asl',
      title: '★★ 同一组数据，顺序存储与链式存储各能做到多短的 ASL',
      source: '统考真题（概念陷阱题）',
      level: 4,
      problem: String.raw`
        设包含 4 个数据元素的集合 $S=\{\texttt{"do"},\ \texttt{"for"},\ \texttt{"repeat"},\ \texttt{"while"}\}$，
        各元素的查找概率依次为 $p_1=0.35$、$p_2=0.15$、$p_3=0.15$、$p_4=0.35$。
        将 $S$ 保存在一个长度为 $4$ 的顺序表中，采用折半查找法，查找成功时的平均查找长度为 $2.2$。

        (1) 若采用**顺序存储结构**保存 $S$，且要求平均查找长度更短，
        则元素应如何排列？应使用何种查找方法？$\mathrm{ASL}$ 是多少？

        (2) 若采用**链式存储结构**保存 $S$，且要求平均查找长度更短，
        则元素应如何排列？应使用何种查找方法？$\mathrm{ASL}$ 是多少？
      `,
      idea: String.raw`
        ==这道题的全部难点在审题，不在计算==。

        题干里"保存在一个长度为 4 的顺序表中，采用折半查找"是**初始已知状态**；
        而两问开头的那个"**若**"字，意味着==打破初始状态、另立新的限定条件==。
        所以：

        - 第 (1) 问==只受"顺序存储结构"约束==；
        - 第 (2) 问==只受"链式存储结构"约束== —— ==不是"只能用单链表"==。

        一旦想通"链式存储包含二叉链表"，第 (2) 问就从"线性查找"打开到了"树上查找"，
        可以构造==最优二叉排序树==。

        **构造最优 BST 的两条约束**（缺一不可）：
        1. ==中序遍历必须是字典序==（$\texttt{do}<\texttt{for}<\texttt{repeat}<\texttt{while}$）；
        2. ==概率大的元素要尽量靠近根==。
        在这两条的夹缝里找形态，就是这类题的做法。
      `,
      solution: String.raw`
        先把字典序和概率对齐（BST 的中序必须按字典序）：

        | 字典序 | do | for | repeat | while |
        |---|---|---|---|---|
        | 概率 | **0.35** | 0.15 | 0.15 | **0.35** |

        **(1) 顺序存储结构**

        顺序存储 $\Rightarrow$ 顺序表 $\Rightarrow$ 逻辑上是线性表。
        线性表上要让 $\mathrm{ASL}$ 最小，做法是==按查找概率**降序**排列 + 顺序查找==
        （概率大的放前面，先被比到）。

        排列：$\texttt{do}(0.35),\ \texttt{while}(0.35),\ \texttt{for}(0.15),\ \texttt{repeat}(0.15)$

        $$\mathrm{ASL}=0.35\times 1+0.35\times 2+0.15\times 3+0.15\times 4=0.35+0.70+0.45+0.60=\boxed{2.1}$$

        $2.1<2.2$，满足"更短" ✓

        **(2) 链式存储结构**

        ==链式存储不等于单链表==：用**二叉链表**存一棵**二叉排序树**同样是链式存储。

        为让 $\mathrm{ASL}$ 最小，把两个高概率元素 $\texttt{do}$、$\texttt{while}$ 尽量提到浅层，
        同时保证中序为字典序。最优形态为：

        - 根（第 1 层）：$\texttt{for}\ (0.15)$
        - 左孩子（第 2 层）：$\texttt{do}\ (0.35)$
        - 右孩子（第 2 层）：$\texttt{while}\ (0.35)$
        - $\texttt{while}$ 的左孩子（第 3 层）：$\texttt{repeat}\ (0.15)$

        中序检验：$\texttt{do},\texttt{for},\texttt{repeat},\texttt{while}$ ✓ 严格递增

        $$\mathrm{ASL}=0.15\times 1+0.35\times 2+0.35\times 2+0.15\times 3=0.15+0.70+0.70+0.45=\boxed{2.0}$$

        查找方法：==二叉排序树查找==。$2.0<2.1<2.2$，是全局最优 ✓

        （取 $\texttt{repeat}$ 作根可得到一棵对称的树，$\mathrm{ASL}$ 同为 $2.0$。）
      `,
      comment: String.raw`
        **追问一：第 (1) 问能不能用散列表？**

        算出来确实能更短（用"串长模 4"作散列函数可以做到 $\mathrm{ASL}=1.3$），
        但==这个答案不符合题意==：题目限定了"顺序存储结构"，
        而[散列存储是与顺序存储平级的另一类物理结构](#/ds/intro/adt?at=hash-not-seq)，
        ==底层用数组不等于它就是顺序存储==。

        **追问二：第 (1) 问能不能用"顺序存储的二叉排序树"？**

        理论上可以（[完全二叉树本来就能顺序存](#/ds/tree/properties?at=index-rule)），
        但==这条路在本题被两面堵死==：

        - **如果要求不浪费空间**（严格用满 4 个格子），树的物理形态==必须是完全二叉树==。
          4 个结点的完全二叉树骨架固定为"下标 1 根、2 左、3 右、4 是 2 的左孩子"，
          其中序序列是 $4\to 2\to 1\to 3$，配上字典序后==排布唯一==：
          $$\text{下标}1=\texttt{repeat},\quad 2=\texttt{for},\quad 3=\texttt{while},\quad 4=\texttt{do}$$
          $$\mathrm{ASL}=0.15\times 1+0.15\times 2+0.35\times 2+0.35\times 3=2.2$$
          ==恰好等于 2.2，没有"更短"==，不满足题目要求。

        - **如果坚持用 (2) 中那棵最优 BST**，把它按 $2i/2i+1$ 映射到数组：
          $\texttt{for}\to 1$、$\texttt{do}\to 2$、$\texttt{while}\to 3$、
          而 $\texttt{repeat}$ 是下标 3 的左孩子 $\Rightarrow$ ==下标 6==。
          数组至少要 6 个格子，==违背了"长度为 4 的顺序表"这一空间限定==。

        **所以第 (1) 问被逼到唯一正解：概率降序 + 顺序查找，$\mathrm{ASL}=2.1$。**

        **这道题真正的考点**：==打破"链式存储 = 单链表"的思维定势==。
        它先用一个"顺序表 + 折半查找"的题干把你锚定在线性结构上，
        再看你能不能在第 (2) 问跳出来。
      `,
    },

    { t: 'example', id: 'ex-judge',
      title: '★ 六个判断题（这一章的全部考法）',
      source: '选择/判断',
      level: 2,
      problem: String.raw`
        判断下列说法是否正确并说明理由：

        (1) 数据的逻辑结构是指数据的各数据项之间的逻辑关系；
        (2) 数据结构的基本运算的实现只与逻辑结构有关；
        (3) 顺序存储方式只能用于线性结构，不能用于非线性结构；
        (4) 采用顺序存储时，逻辑上相邻的两个元素在物理上也一定相邻；
        (5) 链式存储的单链表属于顺序表；
        (6) 用数组实现的散列表属于顺序存储结构。
      `,
      idea: String.raw`
        (1)(2)(3) 逐条对定义即可。

        (4)(5)(6) 是这一章真正的分水岭，==全都卡在"顺序表"与"顺序存储结构"的层级上==：
        - (4) 要区分==狭义（线性）与广义（含非线性）==；
        - (5) 要区分==逻辑结构与物理实现==；
        - (6) 要区分=="底层是数组"与"属于顺序存储"==。
      `,
      solution: String.raw`
        **(1) 错。** 逻辑结构描述的是==数据元素==之间的关系，==不是"数据项"之间的关系==。

        **(2) 错。** ==运算的**定义**针对逻辑结构，运算的**实现**针对存储结构==。
        同一个"插入"，在顺序表上要移动元素，在链表上只改指针。

        **(3) 错。** ==非线性结构同样可以顺序存储==：
        [完全二叉树用数组存](#/ds/tree/properties?at=index-rule)（堆）、
        [图用邻接矩阵存](#/ds/graph/storage?at=matrix-def)、
        [并查集用双亲数组存](#/ds/tree/union-find?at=forest-rep)。

        **(4) 要分情况 —— 单看这句话是错的。**
        - ==承载线性结构（顺序表）时成立==；
        - ==承载非线性结构（堆、并查集）时不成立==。
          并查集里 $\texttt{parent[5]=1}$，逻辑上 5 的父亲是 1，
          但下标 5 和 1 在物理上隔得很远。

        若题目问的是"顺序**表**"，答"对"；问的是"顺序**存储结构**"，答"不一定"。

        **(5) 错。** ==顺序表和单链表是平级的两种物理实现==，互斥。

        **(6) 错。** 虽然底层是连续数组、也可能不用指针，
        但==散列存储的地址由**关键字的值**决定，而非由**逻辑位置**决定==，
        它==抛弃了元素间的逻辑关系==，且概念上包含使用指针的拉链法。
        在四大存储结构的分类里，==散列与顺序平级、互斥==。
      `,
      comment: String.raw`
        **(4) 是这一组里最值得反复看的一题**。
        很多教材原文写的就是"逻辑相邻则物理相邻"，
        ==但那句话的主语是"顺序表"，不是"顺序存储结构"==。
        遇到这类题，==先看主语==。

        **一个通用过滤器**：凡是出现"只能""必须""一定"的说法，先找反例。
        这一章的反例库很固定 —— ==堆、并查集、邻接矩阵==（打非线性不能顺序存的脸），
        ==二叉链表==（打链式存储只有单链表的脸）。
      `,
    },

    /* ================================================================== */
    { t: 'h', id: 'pitfalls', c: '七、易错清单' },

    { t: 'warn', id: 'pitfall-list', title: '这一节的固定失分点', c: String.raw`
      1. **逻辑结构说成"数据项之间的关系"** —— 是==数据元素==之间的关系。
      2. **"数据元素是最小单位"** —— 数据元素是==基本单位==，==数据项才是最小单位==。
      3. **"运算的实现只与逻辑结构有关"** —— ==定义看逻辑，实现看存储==。
      4. **把"顺序表"和"顺序存储结构"当同义词** —— ==前者是后者在线性逻辑下的一个应用==。
      5. **把"链式存储"当成"单链表"** —— ==二叉链表、邻接表也是链式存储==，
         这是[例题 (2)](#/ds/intro/adt?at=ex-asl) 的全部考点。
      6. **"顺序存储只能存线性结构"** —— ==堆、并查集、邻接矩阵都是反例==。
      7. **把"逻辑相邻 ⟹ 物理相邻"当作顺序存储的通用定义** —— ==只在线性逻辑下成立==。
      8. **认为"底层是数组"就等于"顺序存储"** —— ==散列表底层也是数组，但它自成一类==。
      9. **索引存储 = 散列存储** —— ==索引要另建表并保持有序，散列直接算地址==。
      10. **认为存储结构变了逻辑结构就变了** —— ==逻辑结构独立于存储结构==。
      11. **看到"若采用……"仍然被题干的初始条件锁住** ——
          =="若"意味着另立新条件，前面的假设作废==。
      12. **把"纯散列能实现树 / 线性表"当成正交性的例证** ——
          ==纯散列只能承载"集合"==；加了 $\texttt{Next\_ID}$ 就已经是复合结构了。
      13. **反过来，因为上一条就在考试里答"逻辑结构与存储结构不独立"** ——
          ==考试口径仍然是"相互独立、可自由组合"==，
          [严谨边界](#/ds/intro/adt?at=orthogonal-limit)是拿来理解的，不是拿来答题的。
      14. **认为"顺序存储做不了 AVL"** —— ==做得了==，
          只是[纯位置映射的代价很大](#/ds/intro/adt?at=ex-avl-in-array)（$O(n)$ 旋转、$n^{1.44}$ 空间）。
    ` },

    /* ================================================================== */
    { t: 'h', id: 'notes', c: '八、我的思路记录' },

    { t: 'insight', id: 'note-my-trap', title: '我当时是怎么把自己框住的', c: String.raw`
      做那道 $\mathrm{ASL}$ 题时，我把限制写死成了两条：

      > ==第一问一定是顺序存储结构的；第二问一定是链式存储的**单链表**。==

      原因是我==把题干里的"顺序表"当成了贯穿全题的总要求==，
      然后在这个总要求下去读第一问和第二问。

      但题干那句"保存在一个长度为 4 的顺序表中，采用折半查找"其实只是==初始状态==，
      两问开头的"**若**"字==已经把它作废了==。

      所以第二问其实只被"链式存储"这四个字限制着 ——
      ==而链式存储里除了单链表，还有二叉链表==。一旦想到这一层，
      二叉排序树就自然浮出来了，$\mathrm{ASL}$ 也从 2.1 降到了 2.0。
    ` },

    { t: 'insight', id: 'note-my-question', title: '我追问的那三个问题（它们其实是同一个问题）', c: String.raw`
      想通上面那一点之后，我顺着往下问了三个问题：

      1. ==反过来说，顺序存储结构能对应哪些逻辑上的数据结构呢？只有顺序表吗？==
      2. ==对于完全二叉树来说，是它的层序遍历中相邻的元素必须存在相邻的存储单元里吗？
         还是说只要规定一种顺序，在这个顺序下相邻的元素是相邻的，就能叫顺序存储？==
      3. ==如果仅从这个角度理解，那么用数组实现的散列表，
         似乎也可以说是"用顺序存储来实现的散列存储"。==

      这三问其实是同一件事的三个侧面：
      ==**到底什么才算"顺序"？**==

      问完之后我发现，前面那句"逻辑相邻则物理相邻"==根本不是通用定义==，
      它只是顺序表的特征被拔高了。真正的定义只有两条：
      ==空间连续 + 靠下标映射维系关系（不用指针）==。

      而散列表之所以还是被单列出来，==不是因为它的底层不连续==，
      而是因为它==映射时看的是"值"而不是"位置"==，
      并且它==主动放弃了逻辑关系==——这跟顺序存储"保存结构"的目的正好相反。
    ` },

    { t: 'insight', id: 'note-orthogonal-doubt', title: '"正交"这个说法，我觉得有矛盾', c: String.raw`
      顺着上面那条线往下想，我对"逻辑结构与存储结构完全正交、
      任何逻辑结构都能用任何存储结构实现"这句话产生了怀疑：

      > ==既然你也提到工程实践上的非任意性，那为什么还要说可以由任意存储结构来实现呢？
      > 散列就是无法实现前驱后继的关系啊。
      > 如果还需要借助额外附加指针，那只是相当于在链式存储的基础上增加了散列而已。
      > 如果追求严格的定义，这里是否存在一定的矛盾性？==

      核对下来==这个质疑是成立的==：

      - ==纯散列确实表达不了前驱后继==，它只能无损地承载"集合"；
      - ==给它加一个 $\texttt{Next\_ID}$ 字段==，那个字段==就是虚拟指针==，
        此时已经是"散列 + 静态链式"的复合结构了，==拿它去论证"散列能实现树"是循环论证==。

      所以更严谨的说法应该是：==正交性只在"顺序 vs 链式"这两个**拓扑组织基元**之间成立==；
      ==索引和散列是**检索优化策略**，必须依附在前两者之上==。

      ==但考试仍然按教材的口径答==（"同一逻辑结构可用不同存储结构"），
      我这里记的是它的边界在哪，不是要去改教材的说法。
    ` },

    { t: 'insight', id: 'note-avl-array', title: '我追问的最后一个问题', c: String.raw`
      > ==那假如说我要实现平衡二叉搜索树，但是我非要使用顺序存储结构，能实现吗？==

      问这句的时候我其实是想反过来检验"正交"到底有多真 ——
      ==如果连 AVL 这种要频繁旋转的结构都能塞进数组，那顺序与链式的正交性就是实打实的。==

      结论是==能==，而且[有两条路](#/ds/intro/adt?at=ex-avl-in-array)。
      但两条路给我的启发不太一样：

      - **纯位置映射那条**证明了==正交性在理论上成立==（确实不用一根指针也能做出 AVL），
        同时也暴露了代价：==旋转退化成 $O(n)$、空间膨胀到 $n^{1.44}$==；
      - **静态链表那条**则说明：==工程上的"顺序存储"往往是借了链式的思想==
        （用下标当指针），它性能极好，但概念上已经站在两类中间了。

      ==所以"能不能"和"值不值"真的要分开答== ——
      这也是我以后看到"某某结构能不能用某某存储实现"这类问题时的默认拆法。
    ` },

  ],
});
