/* ==========================================================================
   数据结构 / 1 绪论与算法分析 / 时间 / 空间复杂度分析
   ========================================================================== */

KM.page({
  path: 'ds/intro/complexity',
  title: '时间 / 空间复杂度分析',
  subtitle: '只数「基本操作执行了多少次」，然后扔掉常数和低阶项 —— 以及考场上三分钟看出复杂度的方法',
  tags: ['高频', '必考', '概念辨析'],
  updated: '2026-08-21',

  blocks: [

    /* ================================================================== */
    { t: 'h', id: 'algo', c: '一、算法的五个特性与四个目标' },

    { t: 'key', id: 'five-features', title: '算法的五个特性（填空题原句）', c: String.raw`
      | 特性 | 含义 | 最容易被拿来出错的点 |
      |---|---|---|
      | **有穷性** | ==在有穷步之后结束，且每一步都在有穷时间内完成== | 死循环的东西==不是算法== |
      | **确定性** | 每条指令含义明确，==相同输入必得相同输出== | 有二义性的描述不算 |
      | **可行性** | 每一步都能由已实现的基本运算执行有限次完成 | —— |
      | **输入** | ==$0$ 个或多个==输入 | ==可以没有输入== |
      | **输出** | ==$1$ 个或多个==输出 | ==必须有输出==，一个都没有不行 |

      =="输入可以是 0 个，输出至少 1 个"这句必须记牢==，两边不对称，是判断题常客。

      **程序 vs 算法**：程序==不要求有穷性==（操作系统就是一个不该停的程序），
      所以==程序不一定是算法==，但算法可以用程序实现。
    ` },

    { t: 'key', id: 'four-goals', title: '"好算法"的四个目标', c: String.raw`
      1. **正确性** —— 前提，不正确谈别的没意义；
      2. **可读性** —— 给人看的；
      3. **健壮性** —— ==输入非法时能做出适当反应，而不是崩溃或输出莫名其妙的结果==；
      4. **效率与低存储量需求** —— 即时间复杂度和空间复杂度。

      ==注意"健壮性"考的是"对非法输入的处理"==，不是"程序不出 bug"。
    ` },

    /* ================================================================== */
    { t: 'h', id: 'time', c: '二、时间复杂度' },

    { t: 'key', id: 'basic-op', title: '定义：数"基本操作"的执行次数', c: String.raw`
      时间复杂度==不是运行时间==，而是

      $$T(n)=\text{基本操作的执行次数关于问题规模 }n\text{ 的函数}$$

      **基本操作**通常取==最深层循环内的那条语句==（它执行得最频繁）。
      然后用渐进记号简化：

      $$T(n)=O\big(f(n)\big)\iff \exists\,c>0,\ n_0,\ \text{使 }n\ge n_0\text{ 时 }T(n)\le c\,f(n)$$

      **四个渐进记号**（$O$ 之外的三个也考）：

      | 记号 | 含义 | 类比 |
      |---|---|---|
      | $O(f)$ | ==上界==（最多这么快地增长） | $\le$ |
      | $\Omega(f)$ | ==下界== | $\ge$ |
      | $\Theta(f)$ | ==同阶==（既是上界也是下界） | $=$ |
      | $o(f)$ | ==严格小于==（低阶） | $<$ |

      ==平时说的"时间复杂度是 $O(n^2)$"，严格讲应该是 $\Theta(n^2)$==，
      但 408 里按习惯写 $O$ 即可。
      不过==判断题问"$n^2+n=O(n^3)$ 对不对"时，答案是**对**== —— $O$ 只是上界。
    ` },

    { t: 'key', id: 'simplify-rules', title: '化简的三条规则', c: String.raw`
      1. ==只保留最高阶项==：$3n^2+5n+100 \to O(n^2)$；
      2. ==去掉最高阶项的系数==：$5n^2 \to O(n^2)$；
      3. **加法取大、乘法相乘**：
         $$O(f)+O(g)=O(\max(f,g)),\qquad O(f)\times O(g)=O(f\cdot g)$$

      **对数的底数无关紧要**：$\log_2 n$ 与 $\log_{10} n$ 只差一个常数倍，
      ==所以统一写 $O(\log n)$，不写底数==。
      但==$2^n$ 与 $3^n$ 不是同阶==（底数在指数上就不能省），这一点常被搞混。
    ` },

    { t: 'diagram', id: 'growth-curves', title: '常见复杂度的增长速度',
      note: '粗线 = 会"爆炸"的那几个',
      caption: String.raw`==横轴是问题规模 $n$，纵轴是操作次数==。
      关键不是曲线的具体形状，而是==它们冲上天花板的先后顺序==：
      $O(2^n)$ 在 $n$ 还很小的时候就已经算不动了，$O(n^2)$ 撑得久一些，
      而 $O(\log n)$ 几乎是一条平线。
      $$O(1)<O(\log n)<O(n)<O(n\log n)<O(n^2)<O(n^3)<O(2^n)<O(n!)$$`,
      svg: String.raw`
<svg class="dg" viewBox="0 0 700 250" role="img" aria-label="常见时间复杂度函数的增长速度对比曲线">
  <path class="ar" d="M60,225 H382"/>
  <path class="ar" d="M60,225 V26"/>
  <text class="cap" x="388" y="230">n</text>
  <text class="cap" x="20" y="30">T(n)</text>
  <path class="wv" d="M62,218 H368"/>
  <text class="cap" x="376" y="216">O(1)</text>
  <path class="wv" d="M62,218 C110,150 190,118 368,104"/>
  <text class="cap" x="376" y="104">O(log n)</text>
  <path class="wv" d="M62,218 L368,66"/>
  <text class="cap" x="376" y="64">O(n)</text>
  <path class="ar plain" d="M62,218 C170,192 280,140 352,42"/>
  <text class="cap" x="358" y="34">O(n log n)</text>
  <path class="ar plain" d="M62,218 C180,210 265,155 300,38"/>
  <text class="lb em" x="292" y="26" text-anchor="middle">O(n²)</text>
  <path class="ar plain" d="M62,218 C120,216 152,180 172,38"/>
  <text class="lb em" x="166" y="26" text-anchor="middle">O(2ⁿ)</text>
  <g class="n g"><rect x="450" y="46" width="238" height="66" rx="7"/>
    <text class="bt sm" x="569" y="66" text-anchor="middle" dominant-baseline="central">多项式时间：可用</text>
    <text class="bs" x="569" y="88" text-anchor="middle" dominant-baseline="central">O(1) O(log n) O(n) O(n log n) O(n²)</text></g>
  <g class="n r"><rect x="450" y="126" width="238" height="66" rx="7"/>
    <text class="bt sm" x="569" y="146" text-anchor="middle" dominant-baseline="central">指数 / 阶乘时间：不可用</text>
    <text class="bs" x="569" y="168" text-anchor="middle" dominant-baseline="central">O(2ⁿ)、O(n!) —— n = 50 就已算不完</text></g>
  <text class="cap" x="450" y="216">底数不影响对数：log₂n 与 log₁₀n 同阶</text>
  <text class="cap" x="450" y="238">但 2ⁿ 与 3ⁿ 不同阶</text>
</svg>
` },

    { t: 'compare', id: 'scale-table', title: '实际数量级：为什么 $O(n^2)$ 到了百万级就不能用',
      cols: ['$n$', '$\\log_2 n$', '$n$', '$n\\log_2 n$', '$n^2$', '$2^n$'],
      rows: [
        ['10', '≈ 3.3', '10', '≈ 33', '100', '1 024'],
        ['100', '≈ 6.6', '100', '≈ 664', '$10^4$', '≈ $10^{30}$'],
        ['1 000', '≈ 10', '1 000', '≈ $10^4$', '$10^6$', '天文数字'],
        ['$10^6$', '≈ 20', '$10^6$', '≈ $2\\times 10^7$', '==$10^{12}$==', '——'],
      ] },

    { t: 'method', id: 'how-to-count', title: '★ 三分钟看出复杂度的四条经验', c: String.raw`
      1. **顺序结构**：取==最大的那一段==；
      2. **单层循环**：看循环变量==怎么变==
         - $\texttt{i++}$ 走到 $n$ → $O(n)$；
         - ==$\texttt{i *= 2}$ 走到 $n$ → $O(\log n)$==（走了 $\log_2 n$ 步）；
         - $\texttt{i *= i}$ → $O(\log\log n)$（很少考）；
      3. **嵌套循环**：内层次数==是否依赖外层==
         - 不依赖 → 直接相乘；
         - 依赖（如 $\texttt{j < i}$）→ 求和 $\sum_{i=1}^{n} i=\frac{n(n+1)}{2}=O(n^2)$；
      4. **递归**：写出递归式再解（见[递归式求解](#/ds/intro/recurrence?at=three-ways)）。

      **一个最常见的陷阱**：
      ~~~c
      for (i = 1; i <= n; i++)
          for (j = 1; j <= n; j *= 2)
              x++;
      ~~~
      内层是==倍增==而不是自增，所以是 $O(n\log n)$ 而不是 $O(n^2)$。
      ==看到 $\texttt{*=}$ 就要立刻警觉==。
    ` },

    { t: 'key', id: 'three-cases', title: '最好、最坏、平均', c: String.raw`
      | | 含义 | 例子（顺序查找） |
      |---|---|---|
      | **最好** | 输入最有利时的次数 | 第一个就是要找的 → $O(1)$ |
      | **最坏** | ==输入最不利时的次数==，是一个**保证** | 最后一个或不存在 → $O(n)$ |
      | **平均** | ==按各种输入出现的概率加权==的期望次数 | $\frac{n+1}{2}=O(n)$ |

      **三个易错点**：

      1. ==平均复杂度需要假设输入的概率分布==（通常假设等概率），
         ==不是"最好和最坏的平均"==；
      2. 说"时间复杂度"而不加限定时，==默认指最坏情况==；
      3. ==最坏情况的复杂度是有意义的保证，平均情况不是==
         —— 这就是为什么[堆排序虽然平均慢于快排，但在实时系统里更受青睐](#/ds/sort/sort-compare?at=choose)。
    ` },

    /* ================================================================== */
    { t: 'h', id: 'space', c: '三、空间复杂度' },

    { t: 'key', id: 'space-def', title: '只算"额外"的那部分', c: String.raw`
      空间复杂度 $S(n)$ 指算法运行时==临时占用的**辅助**存储空间==对 $n$ 的函数。

      ==输入数据本身占的空间**不计入**==。
      所以"对一个长度为 $n$ 的数组排序，空间复杂度 $O(1)$"这种说法是成立的
      —— 数组是输入，不算。

      **原地工作（in-place）**：$S(n)=O(1)$，即==只用了常数个额外变量==。

      **递归算法的空间复杂度 = 递归栈的最大深度**：
      $$S(n)=O(\text{递归深度})\times O(\text{每层栈帧大小})$$
      每层栈帧通常是 $O(1)$，所以==直接看最大递归深度==。

      **两个例子**：
      - [二叉树的递归遍历](#/ds/tree/traversal?at=complexity)：深度 = 树高，最坏 $O(n)$；
      - [快速排序](#/ds/sort/insert-swap?at=quick-complexity)：平均 $O(\log n)$，最坏 $O(n)$。
    ` },

    { t: 'warn', id: 'space-traps', title: '空间复杂度的三个坑', c: String.raw`
      1. ==把输入数组也算进去==，导致所有排序的空间都成了 $O(n)$。**不算输入**。
      2. ==递归的空间只看深度，不看调用总次数==。
         例如求斐波那契的朴素递归调用了 $O(2^n)$ 次，但==同一时刻栈里最多只有 $n$ 层==，
         所以空间是 ==$O(n)$==，不是 $O(2^n)$。这是极高频的陷阱。
      3. =="尾递归"在 408 里仍按普通递归算空间==（除非题目明说编译器做了尾递归优化）。
    ` },

    /* ================================================================== */
    { t: 'h', id: 'examples', c: '四、例题' },

    { t: 'example', id: 'ex-loops',
      title: '★ 五段代码，五种复杂度',
      source: '选择题必考',
      level: 2,
      problem: String.raw`
        分别写出下列代码段的时间复杂度：

        **(1)**
        ~~~c
        int i = 1;
        while (i <= n) i = i * 2;
        ~~~

        **(2)**
        ~~~c
        for (i = 0; i < n; i++)
            for (j = i; j < n; j++)
                x++;
        ~~~

        **(3)**
        ~~~c
        i = 0;
        while (i * i * i <= n) i++;
        ~~~

        **(4)**
        ~~~c
        int count = 0;
        for (k = 1; k <= n; k *= 2)
            for (j = 1; j <= n; j++)
                count++;
        ~~~

        **(5)**
        ~~~c
        for (i = 1; i <= n; i++)
            for (j = 1; j <= i; j++)
                for (k = 1; k <= j; k++)
                    x++;
        ~~~
      `,
      idea: String.raw`
        统一的做法：==设基本操作执行了 $T$ 次，把 $T$ 写成关于 $n$ 的式子==。

        - 遇到 $\texttt{*=}$ 就想"翻了几倍" → 对数；
        - 遇到内层上界依赖外层就==求和==；
        - 遇到 $\texttt{i*i*i <= n}$ 就==反解 $i$==。

        ==千万别看见两层循环就写 $O(n^2)$==，(4) 就是专门用来坑这个的。
      `,
      solution: String.raw`
        **(1)** 设循环体执行 $t$ 次，则 $i$ 变成 $2^t$，退出条件是 $2^t>n$，
        故 $t=\lfloor\log_2 n\rfloor+1$：
        $$T(n)=\boxed{O(\log n)}$$

        **(2)** 内层执行 $n-i$ 次：
        $$T(n)=\sum_{i=0}^{n-1}(n-i)=n+(n-1)+\dots+1=\frac{n(n+1)}{2}=\boxed{O(n^2)}$$

        **(3)** 退出条件是 $i^3>n$，即 $i>\sqrt[3]{n}$：
        $$T(n)=\boxed{O(\sqrt[3]{n})}=O(n^{1/3})$$

        **(4)** 外层是==倍增==，执行 $\lfloor\log_2 n\rfloor+1$ 次；内层每次都执行 $n$ 次：
        $$T(n)=\boxed{O(n\log n)}$$

        **(5)** 三层嵌套，且层层依赖：
        $$T(n)=\sum_{i=1}^{n}\sum_{j=1}^{i}\sum_{k=1}^{j}1=\sum_{i=1}^{n}\frac{i(i+1)}{2}=\boxed{O(n^3)}$$
      `,
      comment: String.raw`
        **(4) 是这组题的核心**：两层循环，答案却是 $O(n\log n)$。
        ==判断嵌套循环的复杂度时，必须分别看每一层的"步数"，而不是数有几层==。

        **(5) 的快速判法**：不必真的算三重求和。
        ==三层嵌套、每层最多走 $n$ 步、且求和不会降阶== → 直接 $O(n^3)$。
        （严格说结果是 $\frac{n^3}{6}$ 量级，常数无所谓。）

        **一个通用检查**：把 $n$ 换成 $2n$，看 $T$ 大约变成几倍。
        $O(n^2)$ 会变 4 倍，$O(n^3)$ 变 8 倍，$O(\log n)$ 只加 1。
        ==算完拿这个验一下，很难出错==。
      `,
    },

    { t: 'example', id: 'ex-fib-space',
      title: '递归的时间与空间为什么不一样',
      source: '概念辨析',
      level: 3,
      problem: String.raw`
        ~~~c
        int Fib(int n) {
            if (n <= 1) return n;
            return Fib(n-1) + Fib(n-2);
        }
        ~~~

        求这段代码的时间复杂度与空间复杂度。
      `,
      idea: String.raw`
        ==时间看"一共调用了多少次"，空间看"同一时刻栈里最多有多少层"==。
        这两个数字在这里差得极其悬殊，正是这道题的用意。

        画递归树：$\text{Fib}(n)$ 的树是一棵==高度为 $n$、近似满的二叉树==，
        结点总数是指数级；但==从根到叶的任何一条路径长度最多是 $n$==。
      `,
      solution: String.raw`
        **时间**：设调用次数为 $T(n)$，则 $T(n)=T(n-1)+T(n-2)+1$，
        与斐波那契数列同阶：

        $$T(n)=O\!\left(\varphi^{\,n}\right)\approx O(1.618^n)=\boxed{O(2^n)}$$

        （408 里写 $O(2^n)$ 即可。）

        **空间**：递归栈在任一时刻只保存==当前这一条调用链==。
        最深的一条是 $\text{Fib}(n)\to \text{Fib}(n-1)\to\dots\to \text{Fib}(1)$，深度 $n$：

        $$S(n)=\boxed{O(n)}$$
      `,
      comment: String.raw`
        ==$O(2^n)$ 的时间配 $O(n)$ 的空间==，这个反差要记住：
        递归树上有指数多个结点，但==它们不是同时存在的==
        —— 左子树算完就出栈了，右子树才进栈。

        **对照**：如果改成用数组做动态规划，时间降到 $O(n)$、空间 $O(n)$；
        再用两个变量滚动，空间还能降到 $O(1)$。
        =="用空间换时间"和"用时间换空间"是这一章唯一真正的主题==。
      `,
    },

    /* ================================================================== */
    { t: 'h', id: 'pitfalls', c: '五、易错清单' },

    { t: 'warn', id: 'pitfall-list', title: '这一节的固定失分点', c: String.raw`
      1. **"算法必须有输入"** —— ==输入可以是 0 个，输出至少 1 个==。
      2. **把程序当算法** —— 程序==不要求有穷性==。
      3. **$O$ 当成"恰好"** —— $O$ 是==上界==，$n=O(n^2)$ 成立。
      4. **看见两层循环就写 $O(n^2)$** —— 要看每层各走几步。
      5. **$\texttt{i *= 2}$ 当成 $O(n)$** —— 是 ==$O(\log n)$==。
      6. **对数写了底数** —— 底数只差常数倍，==统一写 $O(\log n)$==；
         但 $2^n$ 与 $3^n$ ==不同阶==。
      7. **空间复杂度把输入算进去** —— ==只算辅助空间==。
      8. **递归的空间按调用次数算** —— ==只看最大深度==。
      9. **"平均"当成"最好与最坏的平均"** —— 是==按概率加权的期望==。
      10. **不说明就默认"平均"** —— 不加限定时==默认最坏==。
    ` },

    /* ================================================================== */
    { t: 'h', id: 'notes', c: '六、我的思路记录' },

    { t: 'insight', id: 'note-slot', title: '这里放你自己的话', c: String.raw`
      复杂度分析真正难的从来不是"会不会算求和公式"，
      而是==找对那个"基本操作"==。

      一个可以自查的问题：==如果我把 $n$ 翻一倍，我刚才数的那条语句会多执行多少次？==
      答得上来，复杂度就已经出来了；答不上来，说明基本操作找错了。
    ` },

  ],
});
