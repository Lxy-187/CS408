/* ==========================================================================
   数据结构 / 8 排序 / 八大排序对比与选择
   ========================================================================== */

KM.page({
  path: 'ds/sort/sort-compare',
  title: '八大排序对比与选择',
  subtitle: '一张总表 + 五条横切线索 —— 「一趟之后能确定什么」「谁不稳定」「谁能用于链表」',
  tags: ['高频', '必考', '概念辨析'],
  updated: '2026-08-21',

  blocks: [

    /* ================================================================== */
    { t: 'h', id: 'table', c: '一、总表（这一页的核心）' },

    { t: 'compare', id: 'master-table', title: '★ 九种内部排序算法全表',
      cols: ['算法', '最好', '平均', '最坏', '空间', '稳定性'],
      rows: [
        ['**直接插入**', '==$O(n)$==', '$O(n^2)$', '$O(n^2)$', '$O(1)$', '✅ 稳定'],
        ['**折半插入**', '$O(n^2)$', '$O(n^2)$', '$O(n^2)$', '$O(1)$', '✅ 稳定'],
        ['**希尔**', '——', '$\\approx O(n^{1.3})$', '$O(n^2)$', '$O(1)$', '❌ 不稳定'],
        ['**冒泡**', '==$O(n)$==', '$O(n^2)$', '$O(n^2)$', '$O(1)$', '✅ 稳定'],
        ['**快速**', '$O(n\\log n)$', '==$O(n\\log n)$==', '==$O(n^2)$==', '$O(\\log n)$ ~ $O(n)$', '❌ 不稳定'],
        ['**简单选择**', '$O(n^2)$', '$O(n^2)$', '$O(n^2)$', '$O(1)$', '❌ 不稳定'],
        ['**堆排序**', '$O(n\\log n)$', '$O(n\\log n)$', '$O(n\\log n)$', '==$O(1)$==', '❌ 不稳定'],
        ['**二路归并**', '$O(n\\log n)$', '$O(n\\log n)$', '$O(n\\log n)$', '==$O(n)$==', '✅ 稳定'],
        ['**基数**', '$O(d(n+r))$', '$O(d(n+r))$', '$O(d(n+r))$', '$O(r)$', '✅ 稳定'],
      ] },

    { t: 'key', id: 'table-highlights', title: '这张表里最该盯住的六个格子', c: String.raw`
      1. ==只有直接插入和冒泡有 $O(n)$ 的最好情况==（都需要"已基本有序"，冒泡还要带 flag）；
      2. ==只有快排的最坏是 $O(n^2)$，而它的平均是 $O(n\log n)$== —— 这个反差每年都考；
      3. ==只有堆排序做到了"$O(n\log n)$ 且 $O(1)$ 空间"==；
      4. ==只有归并排序做到了"$O(n\log n)$ 且稳定"==；
      5. ==只有归并排序的空间是 $O(n)$==（顺序表实现）；
      6. ==简单选择排序三种情况全是 $O(n^2)$==，它对初始序列完全"无感"。
    ` },

    { t: 'key', id: 'mnemonic', title: '不稳定的四个：「快些选堆」', c: String.raw`
      $$\boxed{\textbf{快}\ \text{速排序}\quad \textbf{希}\ \text{尔排序}\quad
      \textbf{选}\ \text{择排序}\quad \textbf{堆}\ \text{排序}}$$

      口诀"**快些选堆**"（快、希、选、堆）。其余五个（直接插入、折半插入、冒泡、归并、基数）都稳定。

      **不稳定的共同原因**：==它们都含"远距离交换 / 跨组比较"==。
      - 快排：划分时把元素从一端搬到另一端；
      - 希尔：相同关键字被分到不同的组里各自排序；
      - 简单选择：找到最小值后与当前位置直接交换；
      - 堆排序：建堆和调整时父子结点跨层交换。

      ==理解了这一条，就不用死记口诀==：凡是"元素能一次跳很远"的算法，都不稳定。
      而稳定的那几个，元素要么只与相邻位置交换（冒泡、插入），
      要么在合并/收集时有明确的"先来后到"规则（归并、基数）。
    ` },

    /* ================================================================== */
    { t: 'h', id: 'cross', c: '二、五条横切线索（选择题的出题点）' },

    { t: 'key', id: 'one-pass-final', title: '① 一趟之后，谁能保证有元素到达最终位置', c: String.raw`
      | 算法 | 一趟之后 | 说明 |
      |---|---|---|
      | **冒泡** | ==✅ 至少 1 个== | 冒到端点的那个 |
      | **快速** | ==✅ 至少 1 个== | 基准归位 |
      | **简单选择** | ==✅ 恰好 1 个== | 选出的最值 |
      | **堆排序** | ==✅ 恰好 1 个== | 换到末尾的堆顶 |
      | 直接插入 / 折半插入 | ❌ | 前面的元素还可能被后来者挤走 |
      | 希尔 | ❌ | 同上 |
      | 二路归并 | ❌ | 只是子表有序，还要继续合 |
      | 基数 | ❌ | 要做完 $d$ 趟才全部到位 |

      ==记法：交换类 + 选择类 = 能；插入类 + 归并 + 基数 = 不能==。

      **注意冒泡可能不止 1 个**：若序列末尾本来就是有序的，一趟能确定好几个。
      所以严格表述是"==至少一个=="。
    ` },

    { t: 'key', id: 'compare-count', title: '② 比较次数与初始序列无关的算法', c: String.raw`
      - ==**简单选择排序**==：恒为 $\dfrac{n(n-1)}{2}$ 次；
      - ==**折半插入排序**==：比较次数恒定（$O(n\log n)$），==但移动次数仍与初始序列有关==。

      其余算法的比较次数都会随初始序列变化。
      **基数排序**是个特例 —— ==它根本不做关键字之间的比较==，
      所以问"比较次数"时它应该单独说明。

      **一个易混点**：==归并排序的"时间复杂度"与初始序列无关（恒 $O(n\log n)$），
      但它的"比较次数"是会变的==。题目问哪一个要看清。
    ` },

    { t: 'key', id: 'linked-list', title: '③ 哪些排序能用于链表', c: String.raw`
      | 能用于链表 | 不能用于链表 | 原因 |
      |---|---|---|
      | 直接插入、冒泡、简单选择、==二路归并==、基数 | ==折半插入、希尔、快速、堆排序== | 后者都需要==按下标随机访问== |

      **具体理由**：
      - **折半插入**需要 $O(1)$ 取中间元素；
      - **希尔**需要按 $i,\ i+d,\ i+2d$ 跳跃分组；
      - **快排**需要两个指针从两端向中间靠拢；
      - **堆排序**需要用 $2i$、$\lfloor i/2\rfloor$ 定位父子。

      ==链表排序的最佳选择是[归并排序](#/ds/sort/merge-radix?at=merge-complexity)==：
      它在链表上==只需改指针、不需辅助数组，空间可降到 $O(1)$==，
      而且仍然是稳定的 $O(n\log n)$。
    ` },

    { t: 'key', id: 'lower-bound', title: '④ 基于比较的排序的下界', c: String.raw`
      任何==基于比较==的排序算法，在最坏情况下至少需要

      $$\lceil\log_2 (n!)\rceil \approx n\log_2 n-1.44n=\Omega(n\log n)$$

      次比较。

      **证明思路（判定树模型）**：$n$ 个元素有 $n!$ 种可能的排列，
      每次比较只有两种结果，所以判定树是二叉的，
      ==要区分 $n!$ 种情况，树高至少 $\log_2(n!)$==。

      **推论**：
      - 快排、堆排、归并==已经达到了这个下界的量级==，不可能有渐进更快的比较排序；
      - ==[基数排序](#/ds/sort/merge-radix?at=radix-complexity)不受此约束==，
        因为它一次比较都不做。这不是"打破了下界"，而是==不在下界的适用范围内==。
    ` },

    { t: 'key', id: 'choose', title: '⑤ 选型：题目给条件，你给算法', c: String.raw`
      | 题目条件 | 首选 | 理由 |
      |---|---|---|
      | $n$ 很小（几十以内） | ==直接插入 / 简单选择== | 常数小，$O(n^2)$ 无所谓 |
      | 序列==基本有序== | ==直接插入==（或带 flag 的冒泡） | 能达到 $O(n)$ |
      | $n$ 很大，只关心==平均==速度 | ==快速排序== | 平均性能最优 |
      | $n$ 很大，要求==最坏也快== | ==堆排序== | 最坏仍 $O(n\log n)$ |
      | $n$ 很大，要求==稳定== | ==归并排序== | 唯一的 $O(n\log n)$ 稳定算法 |
      | 内存吃紧 | ==堆排序== | $O(1)$ 空间且 $O(n\log n)$ |
      | 链式存储 | ==归并排序== | 见上一条线索 |
      | 关键字位数少、$n$ 大 | ==基数排序== | $O(d(n+r))$ |
      | 只要==最大/最小的 $k$ 个== | ==堆== | $O(n+k\log n)$，不必全排 |
      | 数据在外存放不下 | ==[外部排序](#/ds/sort/external?at=why)== | 归并 + 败者树 |
    ` },

    /* ================================================================== */
    { t: 'h', id: 'examples', c: '三、例题' },

    { t: 'example', id: 'ex-which-algo',
      title: '★ 看中间状态，判断这是哪种排序',
      source: '选择题最爱',
      level: 3,
      problem: String.raw`
        对序列 $\{49,\ 38,\ 65,\ 97,\ 76,\ 13,\ 27\}$ 分别用不同算法排序，
        下面各是**一趟**（或建堆完成）之后的结果，判断分别是哪种算法：

        (1) $\{38,\ 49,\ 65,\ 97,\ 76,\ 13,\ 27\}$
        (2) $\{27,\ 38,\ 13,\ 49,\ 76,\ 97,\ 65\}$
        (3) $\{13,\ 49,\ 38,\ 65,\ 97,\ 76,\ 27\}$
        (4) $\{13,\ 38,\ 65,\ 97,\ 76,\ 49,\ 27\}$
        (5) $\{97,\ 76,\ 65,\ 38,\ 49,\ 13,\ 27\}$
      `,
      idea: String.raw`
        **判断的三步套路**：

        1. ==先看有没有元素明显"到了最终位置"==（最大/最小值跑到端点、或某个元素两侧分别都小都大）
           → 有的话是交换类或选择类；
        2. ==再看序列的前几个是不是已经有序==、且和原序列的前几个是同一批
           → 是的话是插入类；
        3. ==最后看整体形态==：像完全二叉树的层序（父都比子大）就是建堆。

        ==光看"变了几个位置"是不够的，要看"变成了什么结构"==。
      `,
      solution: String.raw`
        **(1)** 只有前两个 $49,38$ 换成了 $38,49$，其余完全没动 ——
        ==前 2 个有序、后面原封不动==，这是**直接插入排序**的第 1 趟。

        **(2)** 观察 $49$：它左边是 $27,38,13$（全 $<49$），右边是 $76,97,65$（全 $>49$）。
        ==这是典型的"基准归位"==，所以是**快速排序**的一趟划分（基准取 $49$）。

        **(3)** 最小值 $13$ 到了最前面，而中间的 $49,38,65,97,76$ 相对顺序发生了变化
        （原来是 $38,65,97,76$ 在 $49$ 之后，现在 $49$ 跑到了第 2 位）——
        ==这是相邻元素逐对交换的痕迹==，所以是**冒泡排序**（从后往前）的第 1 趟。

        **(4)** 最小值 $13$ 到了最前面，而 ==$13$ 原来的位置（第 6 位）现在放着 $49$== ——
        典型的"一次交换"，其余元素纹丝不动，所以是**简单选择排序**的第 1 趟。

        **(5)** 检查是否满足大根堆：$97\ge 76,65$；$76\ge 38,49$；$65\ge 13,27$ ✓
        ==这是一棵大根堆==，所以是**堆排序的建堆阶段**完成后的结果。
      `,
      comment: String.raw`
        **(3) 与 (4) 的区分是这类题的核心难点**：两者都把 $13$ 挪到了最前面。

        | | 冒泡 | 简单选择 |
        |---|---|---|
        | 中间元素 | ==全部右移一位==（被 13 一路"顶"过来） | ==纹丝不动== |
        | 原来放 13 的位置 | 放着原来它左边的那个数（$76$） | 放着原来第 1 位的数（$49$） |

        ==看"原来最小值那个坑里现在是谁"最快==：
        冒泡是"邻居填坑"，简单选择是"队首填坑"。

        **(5) 的验证方法**：把序列按完全二叉树摆开，
        逐个检查 $A[i]\ge A[2i]$ 且 $A[i]\ge A[2i+1]$。
        ==只要有一处不满足就不是堆==，不能只看第一个是最大值就下结论。
      `,
    },

    { t: 'example', id: 'ex-choose',
      title: '选型与判断',
      source: '选择/判断综合',
      level: 2,
      problem: String.raw`
        判断下列说法是否正确：

        (1) 若只需要求出 $n$ 个元素中最大的 $10$ 个，用堆比用快排整体排序更划算；
        (2) 对基本有序的序列，快速排序的效率最高；
        (3) 排序算法的稳定性越好，效率越高；
        (4) 归并排序在任何情况下都需要 $O(n)$ 的辅助空间；
        (5) 用堆排序对 $n$ 个元素排序，最坏情况下的时间复杂度是 $O(n\log n)$。
      `,
      idea: String.raw`
        (2) 是这一组里最容易上当的 —— ==快排的最坏情况恰恰出现在有序序列上==。
        (4) 要想到==链表实现==这个例外。
      `,
      solution: String.raw`
        **(1) 对。** 建堆 $O(n)$，再摘 $10$ 次堆顶 $O(10\log n)$，
        总代价 $O(n+10\log n)$，==远小于完整排序的 $O(n\log n)$==。

        **(2) 错。** 基本有序时，若基准取第一个元素，
        ==划分会极度不平衡，快排退化到 $O(n^2)$==。
        基本有序应该用==直接插入排序（$O(n)$）==。

        **(3) 错。** ==稳定性和效率是两个互不相干的属性==。
        反例：简单选择排序不稳定但也慢（$O(n^2)$）；
        归并排序既稳定又快（$O(n\log n)$）；
        冒泡排序稳定但慢。

        **(4) 错。** ==在链式存储上归并排序只需 $O(1)$ 辅助空间==
        （合并时只改指针，不需要拷贝数组）。
        顺序存储时才需要 $O(n)$。

        **(5) 对。** ==堆排序三种情况都是 $O(n\log n)$==：
        建堆 $O(n)$ + $n-1$ 次调整，每次 $O(\log n)$。
      `,
      comment: String.raw`
        **(2) 值得单独记一句**：
        =="基本有序"对插入类和冒泡是**福音**，对快排是**灾难**==。
        这个反直觉的点几乎每年都以某种形式出现。

        **(3) 的一般化**：排序算法有四个互相独立的维度 ——
        ==时间、空间、稳定性、对存储结构的要求==。
        题目问哪一个就答哪一个，==不要用某一维去推另一维==。
      `,
    },

    /* ================================================================== */
    { t: 'h', id: 'pitfalls', c: '四、易错清单' },

    { t: 'warn', id: 'pitfall-list', title: '这一节的固定失分点', c: String.raw`
      1. **快排最坏答成 $O(n\log n)$** —— ==最坏是 $O(n^2)$==。
      2. **认为"基本有序"对快排有利** —— ==恰恰是它的最坏情况==。
      3. **堆排序空间答成 $O(\log n)$** —— ==是 $O(1)$==。
      4. **归并空间一律答 $O(n)$** —— ==链表实现是 $O(1)$==。
      5. **稳定性口诀记成"快些选队"** —— 是"快**堆**"的堆（堆排序）。
      6. **认为插入类一趟后也有元素到最终位置** —— ==不能保证==。
      7. **把"简单选择排序的比较次数固定"说成"时间与初始序列有关"** —— 无关。
      8. **认为基数排序打破了 $\Omega(n\log n)$** —— ==它不基于比较，不受该下界约束==。
      9. **认为不稳定的算法可以通过"改一改"变稳定** ——
         部分可以（如加下标做次关键字），但==算法本身的稳定性是固有属性==，
         考试问的是原始算法。
      10. **折半插入排序答成 $O(n\log n)$** —— ==总时间仍是 $O(n^2)$==。
    ` },

    /* ================================================================== */
    { t: 'h', id: 'notes', c: '五、我的思路记录' },

    { t: 'insight', id: 'note-slot', title: '这里放你自己的话', c: String.raw`
      把八个算法按"每一步做了多少有效功"排一遍，整张表就活了：

      - ==插入 / 冒泡==：每步只处理相邻一对，$O(n^2)$ 是必然的，
        但也因此==能感知"已经有序"==，最好情况 $O(n)$；
      - ==希尔 / 快排 / 堆==：每步都在做"远距离"的调整，效率上去了，
        代价是==打乱了相同元素的相对次序（不稳定）==；
      - ==归并 / 基数==：靠额外空间换取"既快又稳"，
        代价是==必须付出 $O(n)$ 或 $O(r)$ 的空间==。

      ==时间、稳定性、空间，这三者之间的取舍就是整张表的全部内容==。
    ` },

  ],
});
