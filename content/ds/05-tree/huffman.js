/* ==========================================================================
   数据结构 / 5 树与二叉树 / 哈夫曼树与哈夫曼编码
   ========================================================================== */

KM.page({
  path: 'ds/tree/huffman',
  title: '哈夫曼树与哈夫曼编码',
  subtitle: '每次挑两个最小的合并 —— 一个贪心算法，和它带来的最优前缀码',
  tags: ['高频', '必考', '手算'],
  updated: '2026-08-21',

  blocks: [

    /* ================================================================== */
    { t: 'h', id: 'wpl', c: '一、WPL：要最小化的那个量' },

    { t: 'key', id: 'wpl-def', title: '带权路径长度', c: String.raw`
      给二叉树的每个**叶子**结点 $i$ 一个权值 $w_i$，设它的**路径长度**（从根到它的边数）
      为 $l_i$，则整棵树的**带权路径长度（WPL）**为

      $$\mathrm{WPL}=\sum_{i=1}^{n} w_i\,l_i$$

      ==只有叶子参与求和==，分支结点的权值不计入。

      **哈夫曼树（最优二叉树）**：在所有以 $w_1,\dots,w_n$ 为叶子权值的二叉树中，
      ==WPL 最小的那一棵==。

      直观理解：$l_i$ 是"这个叶子的深度"，$w_i$ 是"它被访问的频率"。
      要让总代价最小，就得让==权值大的靠近根、权值小的沉到底==。
    ` },

    { t: 'key', id: 'wpl-shortcut', title: '★ 算 WPL 的秒杀法：内部结点权值求和', c: String.raw`
      构造过程中每次合并都会产生一个新的**内部结点**，它的权值 = 两个孩子之和。
      有一个漂亮的恒等式：

      $$\boxed{\mathrm{WPL}=\sum_{\text{所有内部结点}} (\text{该结点的权值})}$$

      **为什么**：某个叶子 $i$ 每被"往下压一层"，就是被卷进了一次合并；
      它一共被卷进 $l_i$ 次合并，==于是 $w_i$ 在内部结点权值之和里恰好被算了 $l_i$ 遍==。

      **用法**：手算时不用画完树再逐个数深度，==把每一步合并出来的和加起来就是 WPL==，
      而且这个方法==天然可以和逐个 $w_il_i$ 相加互相验算==。
    ` },

    /* ================================================================== */
    { t: 'h', id: 'build', c: '二、构造算法' },

    { t: 'steps', id: 'build-steps', title: '哈夫曼算法（贪心）', items: [
      { title: '初始化森林', c: String.raw`把 $n$ 个权值各看成一棵==只有根结点==的树，构成森林 $F$。` },
      { title: '取两个最小的', c: String.raw`从 $F$ 中选出==根结点权值最小的两棵树==（记作 $T_1,T_2$）。` },
      { title: '合并', c: String.raw`新建一个结点作为根，$T_1$、$T_2$ 分别作它的左右子树，
        ==新根的权值 = 两个孩子权值之和==。把这棵新树放回 $F$。` },
      { title: '重复直到只剩一棵', c: String.raw`每合并一次，森林里的树少一棵。
        $n$ 棵树需要==恰好 $n-1$ 次合并==。` },
    ] },

    { t: 'key', id: 'huffman-props', title: '哈夫曼树的四条固定性质', c: String.raw`
      设有 $n$ 个权值（即 $n$ 个叶子）：

      1. ==总结点数 $=2n-1$==（$n$ 个叶子 $+\ n-1$ 个合并出来的内部结点）；
      2. ==不存在度为 1 的结点==（每次合并都同时接两棵树），
         所以它是一棵**正则二叉树**，满足 $n_0=n_2+1$，即 $n_2=n-1$ ✓；
      3. ==权值越大的叶子越靠近根==（严格地说：$w_i>w_j\Rightarrow l_i\le l_j$）；
      4. ==哈夫曼树不唯一，但 WPL 唯一==。
         不唯一的来源有两处：合并时**左右子树谁在左**没规定，
         以及**出现权值相同**时选谁也没规定。

      **第 4 条极其常考**：题目问"哈夫曼树是否唯一"答==否==，
      问"WPL 是否唯一"答==是==，问"哈夫曼编码是否唯一"答==否==（但==码长分布唯一==）。
    ` },

    { t: 'diagram', id: 'huff-tree', title: '以 $\\{7,5,2,4\\}$ 为例的哈夫曼树',
      note: '左分支标 0、右分支标 1（这只是约定）',
      caption: String.raw`构造顺序：$2+4=6\ \to\ 5+6=11\ \to\ 7+11=18$。
      ==三次合并、三个内部结点，权值和 $6+11+18=35$ 正好等于 WPL==。
      如果把 $2$ 和 $4$ 的左右位置对调，得到的是==另一棵哈夫曼树==，但 WPL 仍是 $35$。`,
      svg: String.raw`
<svg class="dg" viewBox="0 0 700 244" role="img" aria-label="权值为 7、5、2、4 的哈夫曼树及其编码">
  <path class="ar plain" d="M160,32 L95,90"/><path class="ar plain" d="M160,32 L230,90"/>
  <path class="ar plain" d="M230,90 L175,148"/><path class="ar plain" d="M230,90 L290,148"/>
  <path class="ar plain" d="M290,148 L245,206"/><path class="ar plain" d="M290,148 L330,206"/>
  <g class="n p"><rect x="142" y="14" width="36" height="36" rx="18"/><text class="bt sm" x="160" y="32" text-anchor="middle" dominant-baseline="central">18</text></g>
  <g class="n g"><rect x="77" y="72" width="36" height="36" rx="18"/><text class="bt sm" x="95" y="90" text-anchor="middle" dominant-baseline="central">7</text></g>
  <g class="n p"><rect x="212" y="72" width="36" height="36" rx="18"/><text class="bt sm" x="230" y="90" text-anchor="middle" dominant-baseline="central">11</text></g>
  <g class="n g"><rect x="157" y="130" width="36" height="36" rx="18"/><text class="bt sm" x="175" y="148" text-anchor="middle" dominant-baseline="central">5</text></g>
  <g class="n p"><rect x="272" y="130" width="36" height="36" rx="18"/><text class="bt sm" x="290" y="148" text-anchor="middle" dominant-baseline="central">6</text></g>
  <g class="n g"><rect x="227" y="188" width="36" height="36" rx="18"/><text class="bt sm" x="245" y="206" text-anchor="middle" dominant-baseline="central">2</text></g>
  <g class="n g"><rect x="312" y="188" width="36" height="36" rx="18"/><text class="bt sm" x="330" y="206" text-anchor="middle" dominant-baseline="central">4</text></g>
  <text class="lb em" x="116" y="60">0</text>
  <text class="lb em" x="202" y="60">1</text>
  <text class="lb em" x="192" y="118">0</text>
  <text class="lb em" x="268" y="118">1</text>
  <text class="lb em" x="258" y="176">0</text>
  <text class="lb em" x="318" y="176">1</text>
  <text class="cap" x="14" y="238">绿 = 叶子（原始权值）　紫 = 合并产生的内部结点</text>
  <g class="n a"><rect x="400" y="14" width="284" height="130" rx="7"/>
    <text class="bt sm" x="542" y="36" text-anchor="middle" dominant-baseline="central">权 7 → 编码 0 　　码长 1</text>
    <text class="bt sm" x="542" y="60" text-anchor="middle" dominant-baseline="central">权 5 → 编码 10 　码长 2</text>
    <text class="bt sm" x="542" y="84" text-anchor="middle" dominant-baseline="central">权 2 → 编码 110　码长 3</text>
    <text class="bt sm" x="542" y="108" text-anchor="middle" dominant-baseline="central">权 4 → 编码 111　码长 3</text>
    <text class="bs" x="542" y="130" text-anchor="middle" dominant-baseline="central">没有任何一个编码是另一个的前缀</text></g>
  <text class="cap" x="400" y="168">WPL = 7×1 + 5×2 + 2×3 + 4×3 = 35</text>
  <text class="cap" x="400" y="192">　　 = 6 + 11 + 18 = 35（内部结点求和）</text>
  <text class="cap" x="400" y="216">两种算法互为验算</text>
</svg>
` },

    { t: 'method', id: 'by-hand', title: '手算时的三个提速习惯', c: String.raw`
      1. ==把权值从小到大排好写成一行==，每次从左端取两个；
         合并出的新值==按大小插回这一行==（而不是追加到末尾），下一轮还是从左端取。
      2. ==边合并边把和累加起来==，最后直接得到 WPL（见上面的秒杀法）。
      3. **画树时从下往上画**：先把最先合并的那对画在最底下。
         很多人从上往下画，画到一半发现层数不够又要重来。

      **权值相同的处理**：题目通常不规定，==随便选一个即可==，
      但要在答案里写一句"当权值相同时取先出现的"，避免被认为漏考虑。
    ` },

    /* ================================================================== */
    { t: 'h', id: 'coding', c: '三、哈夫曼编码' },

    { t: 'key', id: 'prefix-code', title: '前缀码：为什么必须让字符只出现在叶子上', c: String.raw`
      **定长编码**：$n$ 个字符每个用 $\lceil\log_2 n\rceil$ 位，简单但浪费。

      **变长编码**：让常用字符用短码。但会出现歧义 ——
      若 $\texttt{A}=\texttt{0}$、$\texttt{B}=\texttt{01}$，
      那么收到 $\texttt{01}$ 时不知道是 $\texttt{B}$ 还是 $\texttt{A}$ 后面跟别的。

      **前缀码（prefix-free code）**：==没有任何一个编码是另一个编码的前缀==。
      前缀码可以==从左到右一遍扫描、无歧义地唯一译码==。

      **树的语言**：把编码看成从根到某结点的路径（左 0 右 1），
      则"$X$ 的编码是 $Y$ 的编码的前缀" $\iff$ "$X$ 是 $Y$ 的祖先"。
      所以
      $$\boxed{\text{前缀码}\iff\text{所有字符都只出现在叶子上}}$$

      哈夫曼树的字符全在叶子，==所以哈夫曼编码天然是前缀码==。
    ` },

    { t: 'key', id: 'optimal', title: '"最优"到底优在哪', c: String.raw`
      设字符 $c_i$ 出现频次 $w_i$、编码长度 $l_i$，则报文总长度为

      $$\text{总位数}=\sum w_i l_i = \mathrm{WPL}$$

      ==WPL 最小 $\iff$ 编码后的报文最短==，这就是哈夫曼编码被称为"最优前缀码"的含义。

      **压缩率**：与定长编码相比
      $$\text{压缩率}=1-\frac{\mathrm{WPL}}{n_{\text{总字符数}}\times\lceil\log_2 n\rceil}$$

      **注意哈夫曼编码不是"最优压缩"**：它只是==在"每个字符独立编成整数位"这个前提下最优==。
      考试若问"哈夫曼编码是否一定是最短的编码方案"，==严格说答案是"在前缀码范围内是"==。
    ` },

    { t: 'example', id: 'ex-huffman',
      title: '★ 构造哈夫曼编码并算压缩率',
      source: '经典大题',
      level: 3,
      problem: String.raw`
        某段报文只由 6 个字符组成，各字符出现的次数如下：

        | 字符 | A | B | C | D | E | F |
        |---|---|---|---|---|---|---|
        | 次数 | 45 | 13 | 12 | 16 | 9 | 5 |

        (1) 构造哈夫曼树，写出各字符的哈夫曼编码；
        (2) 求 WPL（即编码后报文的总位数）；
        (3) 与等长编码相比，压缩了多少？
      `,
      idea: String.raw`
        ==先把权值排序：$5,\,9,\,12,\,13,\,16,\,45$==，然后机械地取两个最小的。

        每合并一次就把新值==按序插回队列==，这样下一次还是取最左边两个，
        ==不用每轮重新扫一遍找最小值==。

        编码那一问，==不要从叶子往上倒着读再翻转==（容易反），
        而是画好树之后==从根往下顺着读==。
      `,
      solution: String.raw`
        **(1) 构造过程**（每步取两个最小，新值插回）

        | 步 | 当前队列（升序） | 取出 | 新结点 |
        |---|---|---|---|
        | 1 | 5, 9, 12, 13, 16, 45 | 5, 9 | **14** |
        | 2 | 12, 13, 14, 16, 45 | 12, 13 | **25** |
        | 3 | 14, 16, 25, 45 | 14, 16 | **30** |
        | 4 | 25, 30, 45 | 25, 30 | **55** |
        | 5 | 45, 55 | 45, 55 | **100** |

        得到的哈夫曼树（左 0 右 1，把小的放左边）：

        - 根 $100$：左 = $\texttt{A}(45)$，右 = $55$
        - $55$：左 = $25$，右 = $30$
        - $25$：左 = $\texttt{C}(12)$，右 = $\texttt{B}(13)$
        - $30$：左 = $14$，右 = $\texttt{D}(16)$
        - $14$：左 = $\texttt{F}(5)$，右 = $\texttt{E}(9)$

        **编码**

        | 字符 | A | B | C | D | E | F |
        |---|---|---|---|---|---|---|
        | 权 | 45 | 13 | 12 | 16 | 9 | 5 |
        | 编码 | $\texttt{0}$ | $\texttt{101}$ | $\texttt{100}$ | $\texttt{111}$ | $\texttt{1101}$ | $\texttt{1100}$ |
        | 码长 | 1 | 3 | 3 | 3 | 4 | 4 |

        **(2) WPL**

        $$\mathrm{WPL}=45\cdot 1+13\cdot 3+12\cdot 3+16\cdot 3+9\cdot 4+5\cdot 4=224$$

        用内部结点求和验算：$14+25+30+55+100=224$ ✓

        **(3) 压缩率**

        总字符数 $45+13+12+16+9+5=100$。
        $6$ 个字符的等长编码需要 $\lceil\log_2 6\rceil=3$ 位，共 $100\times 3=300$ 位。

        $$\text{压缩率}=1-\frac{224}{300}\approx 25.3\%$$
      `,
      comment: String.raw`
        **译码练习**：$\texttt{1101100}$ 是什么？
        从根出发：$\texttt{1101}\to \texttt{E}$，剩下 $\texttt{100}\to \texttt{C}$，
        所以是 $\texttt{EC}$。==前缀码的译码不需要任何分隔符==，走到叶子就切一刀。

        **常见错误**：
        1. ==把 $\texttt{B}$ 和 $\texttt{C}$ 的编码写反==。$12<13$，所以 $\texttt{C}$ 在左（$\texttt{100}$）。
           题目若不规定左右，两种答案都对，==但同一张表里必须自洽==。
        2. ==等长编码位数算成 $\log_2 6\approx 2.58$==，必须向上取整到 $3$。
        3. ==第 3 步取的是 14 和 16，不是 14 和 25==。每一轮都要重新看谁最小，
           新合并出来的 $14$ 已经参与排序了。
      `,
    },

    /* ================================================================== */
    { t: 'h', id: 'm-ary', c: '四、$m$ 叉哈夫曼树（补虚结点）' },

    { t: 'key', id: 'm-ary-rule', title: '什么时候要补 0', c: String.raw`
      $m$ 叉哈夫曼树每次合并==$m$ 棵树成 1 棵==，森林里的树每次减少 $m-1$ 棵。
      要最终恰好剩 1 棵，必须满足

      $$(n-1)\ \bmod\ (m-1)=0$$

      若不满足，就==补若干个权值为 $0$ 的"虚结点"==，
      直到 $n'$ 使得 $(n'-1)\bmod(m-1)=0$ 为止。

      **为什么补 0 而不是别的**：权值 $0$ 的叶子不贡献 WPL，
      而==把它们放在最深处==（第一轮就被合并掉），才能保证真实叶子尽量浅。

      **补几个**：设 $r=(n-1)\bmod (m-1)$，若 $r\ne 0$ 则需补 $(m-1)-r$ 个。

      **例**：$n=8$ 个权值构造 $3$ 叉哈夫曼树。
      $(8-1)\bmod 2=1\ne 0$，需补 $2-1=1$ 个虚结点，按 $n'=9$ 来做。
      ==第一轮合并时必须把这个 $0$ 用掉==。
    ` },

    /* ================================================================== */
    { t: 'h', id: 'pitfalls', c: '五、易错清单' },

    { t: 'warn', id: 'pitfall-list', title: '这一节的固定失分点', c: String.raw`
      1. **WPL 把内部结点也算进去了** —— ==只有叶子的 $w_il_i$ 参与求和==
         （不过"所有内部结点权值之和"这个**等价**算法是对的，别把两者混着用）。
      2. **说哈夫曼树唯一** —— ==树不唯一、编码不唯一，只有 WPL 和码长分布唯一==。
      3. **合并后忘了把新结点放回森林** —— 新结点必须参与后续比较。
      4. **每轮只在"原始权值"里找最小** —— 合并产生的新值==也要参与竞争==。
      5. **编码从叶子往根读却忘了翻转** —— 从根往下读最稳。
      6. **认为频率最高的字符编码一定最短** —— 是的，这条成立；
         但反过来 =="码长相同的字符频率一定相同"是错的==。
      7. **$m$ 叉哈夫曼树忘了补虚结点** —— 会导致最后一轮凑不齐 $m$ 棵。
      8. **把哈夫曼树当成 BST** —— ==两者毫无关系==，哈夫曼树的中序序列没有任何有序性。
      9. **求"最少需要几位"时用哈夫曼码长的最大值** —— 那是==单个字符的最长码长==，
         而报文总长要用 WPL。
    ` },

    /* ================================================================== */
    { t: 'h', id: 'notes', c: '六、我的思路记录' },

    { t: 'insight', id: 'note-slot', title: '这里放你自己的话', c: String.raw`
      一个值得自己想一遍的问题：==为什么"每次取两个最小的"就一定全局最优==？

      提示：最优解里，==最深的那一层一定成对出现==（否则把落单的那个提上去 WPL 会更小），
      而==两个权值最小的字符一定可以放在最深那一层==（交换不会让 WPL 变大）。
      于是"先把两个最小的配成一对"是安全的，剩下的问题规模减一 —— 归纳即得。
      想通这一步，贪心算法的正确性就不是"背下来的结论"了。
    ` },

  ],
});
