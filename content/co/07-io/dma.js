/* ==========================================================================
   计算机组成原理 / 7 输入输出系统 / DMA 传送过程与周期挪用
   —— DMA 的一句话定义：让数据【绕开 CPU】直接在设备与主存之间流动。
      它在五种 I/O 方式中的位置见 co/io/io-mode。
   ========================================================================== */

KM.page({
  path: 'co/io/dma',
  title: 'DMA 传送过程与周期挪用',
  subtitle: 'DMA 把 CPU 从"搬运工"降级成了"包工头"：**只管发包和验收，不碰货**',
  tags: ['高频', '必考', '手算', '概念辨析'],
  updated: '2026-08-16',

  blocks: [

    /* ================================================================== */
    { t: 'h', id: 'why', c: '一、DMA 要解决的到底是什么' },

    { t: 'key', id: 'the-problem', title: '中断方式的死穴：每个字节都要 CPU 亲手搬', c: String.raw`
      中断方式已经让 CPU 不必"等"了，但它==没有让 CPU 不必"搬"==。
      看一次中断服务程序里真正干的事：

      $$\text{设备接口} \to \texttt{MDR} \to \text{CPU 寄存器} \to \texttt{MDR} \to \text{主存}$$

      ==数据在 CPU 里转了一圈==，而 CPU 除了当中转站什么也没做。
      更糟的是外面还包着一整套开销：
      [保存断点、保护现场、恢复现场、中断返回](#/co/io/interrupt?at=single-vs-multi)。

      $$\underbrace{500\ \text{个时钟周期的开销}}_{\text{一次中断}}\ \text{只搬了}\ \underbrace{4\ \text{个字节}}_{\text{一次传送}}$$

      [那道三方对比题](#/co/io/io-mode?at=ex-three-cost)算出来的 $100\%$ 就是这么来的。

      **DMA 的思路很直接**：==既然 CPU 只是中转站，那就把它拆掉==。
      让设备和主存之间==有一条不经过 CPU 的路==，
      再配一个小硬件（DMA 控制器）来指挥这条路上的搬运。
    ` },

    { t: 'code', id: 'data-path-diff', title: '两条数据通路的差别', lang: '',
      c: String.raw`
        中断方式：数据必须【穿过】CPU
        ─────────────────────────────────────────────
            设备 ──▶ 接口 ──▶ 数据总线 ──▶ ┌─────┐
                                          │ CPU │  ← 每个字节都要
            主存 ◀── 数据总线 ◀────────────└─────┘     经过它的寄存器

        DMA 方式：数据【绕开】CPU
        ─────────────────────────────────────────────
                       ┌──────────┐
            设备 ──────│ DMA 控制器│──────▶ 主存
                       └────┬─────┘
                            │ 只借用【总线】，不经过 CPU 的寄存器
                            ▼
                    ┌───────────────┐
                    │      CPU       │  ← 只在【开始】和【结束】时出面
                    └───────────────┘

        ★ 注意 DMA 控制器【本身是一个主设备】，它要和 CPU 抢总线使用权。
          （见 co/bus/bus-timing 的主从设备）
      ` },

    /* ================================================================== */
    { t: 'h', id: 'controller', c: '二、DMA 控制器的组成' },

    { t: 'code', id: 'dmac-struct', title: '★ 六个部件，每个都对应一个考点', lang: '',
      c: String.raw`
        ┌──────────────────── DMA 控制器 ────────────────────┐
        │                                                    │
        │  ┌──────────────┐   主存地址寄存器 AR                │
        │  │  AR (地址)    │──▶ 当前要访问的主存地址            │
        │  └──────┬───────┘   每传一个字【自动 +1】            │
        │         │                                          │
        │  ┌──────▼───────┐   传送长度计数器 WC                │
        │  │  WC (计数)    │──▶ 还要传多少个字                 │
        │  └──────┬───────┘   ★ 常用【负数补码】装入，          │
        │         │              每传一个字 +1，【溢出=传完】    │
        │         │              溢出信号 ──▶ 中断机构          │
        │  ┌──────▼───────┐                                   │
        │  │  BR (数据)    │──▶ 暂存正在搬的那个字              │
        │  └──────────────┘                                   │
        │  ┌──────────────┐                                   │
        │  │ DMA 请求触发器 │──▶ 设备准备好一个字时置 1          │
        │  └──────────────┘                                   │
        │  ┌──────────────┐                                   │
        │  │ 控制/状态逻辑  │──▶ 定传送方向、改参数、与 CPU 同步 │
        │  └──────────────┘                                   │
        │  ┌──────────────┐                                   │
        │  │   中断机构     │──▶ ★ 只在【整块传完】时才发中断    │
        │  └──────────────┘                                   │
        └────────────────────────────────────────────────────┘
      ` },

    { t: 'key', id: 'wc-trick', title: '★ 字计数器为什么用负数装入（一个很讨巧的硬件设计）', c: String.raw`
      要传 $n$ 个字，直观做法是"装入 $n$，每传一个减 1，==减到 0 就停=="。
      但"判断是否等于 0"需要一个==比较器==（或者对所有位做或非）。

      硬件的做法是：==装入 $-n$ 的补码，每传一个字加 1==。

      $$-n \xrightarrow{\ +1\ } -n+1 \xrightarrow{\ +1\ } \cdots
      \xrightarrow{\ +1\ } -1 \xrightarrow{\ +1\ } 0\ (\text{产生进位溢出})$$

      =="传完了"这个事件，直接表现为计数器的进位输出==——
      一根线就够了，==不需要任何比较电路==。

      这正是补码=="把减法变成加法"==那套思想在计数器上的应用：
      ==让"归零"这个判断变成硬件本来就有的进位信号==。

      ==所以题目说"字计数器溢出表示一批数据传送完毕"，说的就是这件事。==
    ` },

    /* ================================================================== */
    { t: 'h', id: 'three-phases', c: '三、DMA 传送的三个阶段' },

    { t: 'steps', id: 'phases', title: '★★ 预处理 → 数据传送 → 后处理', items: [
      { title: '预处理（由 **CPU** 用程序完成）',
        c: String.raw`
          CPU 执行几条 I/O 指令，把参数写进 DMA 控制器的寄存器：

          1. ==指明数据传送方向==（输入还是输出）；
          2. ==向设备地址寄存器送入设备号==，并启动设备；
          3. ==向主存地址寄存器 $\texttt{AR}$ 送入数据在主存的起始地址==；
          4. ==向字计数器 $\texttt{WC}$ 送入要传送的字数==。

          做完这四件事，==CPU 就转去执行别的程序了==。` },
      { title: '数据传送（由 **DMA 控制器** 用硬件完成）',
        c: String.raw`
          ==以数据块为单位==，全程不需要 CPU 干预：

          ~~~
          循环 {
              设备准备好一个字 → DMA 请求触发器置 1
              向总线仲裁器【申请总线】
              获得总线后：设备 → BR → 主存(AR 所指单元)
              AR + 1,  WC + 1
              【归还总线】
          } 直到 WC 溢出
          ~~~

          ==每传一个字就申请一次、归还一次总线==，
          这就是下一节[周期挪用](#/co/io/dma?at=cycle-steal)要讲的东西。` },
      { title: '后处理（由 **CPU** 执行中断服务程序完成）',
        c: String.raw`
          $\texttt{WC}$ 溢出 $\Rightarrow$ 中断机构==发出一次中断请求==，CPU 转去做：

          1. ==校验==送入主存的数据是否正确；
          2. ==决定是否继续==用 DMA 传送下一个数据块；
          3. ==测试传送过程中是否出错==，出错则处理。

          ==注意这是整个过程中唯一的一次中断==。` },
    ] },

    { t: 'md', c: String.raw`
      三个阶段里==只有中间那段是 DMA 控制器自己在跑==：
      预处理是 CPU 按部就班执行普通指令，后处理是中断服务程序。
      这也正是"DMA 与 CPU 并行"这句话的准确含义——
      [并行、串行依赖、资源互斥三者的区别](#/co/cpu/multi-ctrl?at=three-kinds)。
    ` },

    { t: 'warn', id: 'dma-needs-int', title: '★ "DMA 方式不需要中断"是错的', c: String.raw`
      这是本节最高频的判断题。正确的说法是：

      $$\text{DMA 把"每个字一次中断"降成了}\ \textbf{"每个数据块一次中断"}$$

      ==中断次数下降了几个数量级，但没有降到零==。
      整块传完时那一次中断==是必需的==，因为：

      - CPU 需要知道"可以用这块数据了"；
      - 需要校验、需要处理错误、需要决定要不要接着传。

      ==没有这次中断，CPU 就无从得知传送何时结束==（除非退回去轮询状态位，
      那就白费劲了）。

      **顺带辨析两次"中断"的性质完全不同**：

      | | 中断方式里的中断 | DMA 方式里的中断 |
      |---|---|---|
      | 目的 | ==请求 CPU 来搬数据== | ==通知 CPU 传送已结束== |
      | 频率 | ==每个字/字节一次== | ==每个数据块一次== |
      | CPU 要做什么 | ==搬运==（真正的数据传送） | ==后处理==（校验、善后） |
    ` },

    /* ================================================================== */
    { t: 'h', id: 'access-modes', c: '四、DMA 与主存交换数据的三种方式' },

    { t: 'md', c: String.raw`
      DMA 控制器和 CPU ==都要访问主存==，而主存一次只能服务一个。
      怎么分配这个冲突，有三种做法。==这是本节的核心考点。==
    ` },

    { t: 'code', id: 'three-access', title: '★★ 三种方式的时间轴对比', lang: '',
      c: String.raw`
        ① 停止 CPU 访问主存
        ────────────────────────────────────────────────
        CPU  ████████░░░░░░░░░░░░░░░░░░░░░░░░████████
                     └──── CPU 完全停下来 ────┘
        DMA          ████████████████████████
                     └── 一次传完【整块】 ──┘
        ★ 控制最简单；但 CPU 长时间停摆，主存利用率低

        ② 周期挪用（周期窃取）★ 最常用
        ────────────────────────────────────────────────
        CPU  ███░███░░███░█░███░░░███░████░███
        DMA     █    ██   █    ███   █
                ↑    ↑    ↑    ↑     ↑
             每次只【偷走一个存取周期】，偷完就还
        ★ CPU 和 DMA 交错前进，效率最好

        ③ DMA 与 CPU 交替访存
        ────────────────────────────────────────────────
        一个 CPU 周期 ──▶ ┌──C1──┬──C2──┐
                          │ DMA  │ CPU  │
                          └──────┴──────┘
        CPU  ░░██░░██░░██░░██░░██░░██
        DMA  ██░░██░░██░░██░░██░░██░░
        ★ 【不需要申请、建立、归还总线】——时间片是预先分好的
          前提：CPU 的工作周期 > 主存的存取周期
      ` },

    { t: 'compare', id: 'access-compare', title: '三种方式对比',
      cols: ['', '停止 CPU 访存', '周期挪用', 'DMA 与 CPU 交替访存'],
      rows: [
        ['DMA 一次占用多久', '==整个数据块==', '==一个存取周期==', '==预分的半个 CPU 周期=='],
        ['要不要申请/归还总线', '要（一次）', '==要（每次都要）==', '==不要==，时间片预先分好'],
        ['CPU 停摆时间', '==长==', '==很短==', '==几乎为零=='],
        ['主存利用率', '==低==', '==高==', '==最高=='],
        ['控制复杂度', '==最简单==', '中', '==最复杂=='],
        ['适用场合', '数据传输率很高、成组传送', '==I/O 设备读写周期 > 主存存取周期==（最常见）', '==CPU 工作周期 > 主存存取周期=='],
      ] },

    { t: 'key', id: 'cycle-steal', title: '★★ 周期挪用的三种情况（必考）', c: String.raw`
      DMA 提出访存请求时，==恰好撞上 CPU 什么状态==，决定了会发生什么：

      | 情况 | CPU 此刻在干什么 | 结果 |
      |---|---|---|
      | **①** | ==CPU 不需要访存==（比如正在做乘法运算） | ==毫无冲突==，DMA 直接用，==CPU 一点不受影响== |
      | **②** | ==CPU 正在访存== | DMA ==必须等这个存取周期结束==，CPU 让出总线后再用 |
      | **③** | ==CPU 与 DMA 同时请求访存== | ==DMA 优先==，CPU 暂缓一个存取周期 |

      **情况 ③ 里 DMA 为什么优先** —— 这是个必答的"为什么"：

      ==因为 I/O 传送有时间窗口限制==。
      外设的数据缓冲寄存器只有一个（或很浅的队列），
      ==下一个数据到来时会把上一个覆盖掉==。
      DMA 若不及时把数据搬走，==数据就永久丢失了==。

      而 CPU 晚一个存取周期访存，==只是慢一点，什么也不会丢==。

      $$\text{DMA 的紧迫性来自}\ \textbf{"不做就丢数据"},\ \text{不是"更重要"}$$

      ==这也是 [DMA 请求优先级高于中断请求](#/co/cpu/exception?at=dma-vs-int-timing)的同一个理由。==

      **"挪用"这个名字的含义**：==DMA 是在 CPU 的访存周期里"偷"走一个==，
      CPU 察觉不到自己被偷了，只感觉到访存偶尔慢了一拍
      —— 所以它==不影响 CPU 的现行程序，不需要保存现场==。
    ` },

    /* ================================================================== */
    { t: 'h', id: 'vs-interrupt', c: '五、DMA 与中断的全面对比' },

    { t: 'compare', id: 'dma-int-table', title: '★★ 这张表几乎每年都以某种形式出现',
      cols: ['', '程序中断方式', 'DMA 方式'],
      rows: [
        ['**数据传送靠什么**', '==程序（软件）==', '==硬件（DMA 控制器）=='],
        ['**数据经过 CPU 吗**', '==经过==', '==不经过=='],
        ['**响应时机**', '==一条指令执行结束后==', '==一个存取周期（总线周期）结束后=='],
        ['**抢走什么**', '==CPU 的执行权==', '==总线使用权=='],
        ['**中断请求的作用**', '==请求 CPU 传送数据==', '==通知 CPU 传送已结束=='],
        ['**优先级**', '==低==', '==高=='],
        ['**是否影响 CPU 现行程序**', '==影响==，要保存现场、恢复现场', '==不影响==，只挪用总线周期'],
        ['**响应粒度**', '每个字/字节', '每个数据块'],
        ['**适用设备**', '==中低速==', '==高速、成批=='],
      ] },

    { t: 'key', id: 'granularity-insight', title: '★ 抓住"抢什么"这一行，其余全能推出来', c: String.raw`
      整张表可以压缩成一句话：

      $$\text{中断抢的是}\ \textbf{CPU 的执行权},\qquad \text{DMA 抢的是}\ \textbf{总线使用权}$$

      **从这一句往下推**：

      - 抢执行权 $\Rightarrow$ 要让 CPU==转去跑另一段程序== $\Rightarrow$
        ==必须保存现场==，必须等指令边界（[为什么](#/co/cpu/exception?at=why-instruction-end)）；
      - 抢总线 $\Rightarrow$ CPU ==照常执行不访存的部分== $\Rightarrow$
        ==不用保存任何东西==，只要等一个总线周期结束即可插入。

      $\Rightarrow$ ==于是"响应时机"和"是否影响现行程序"两行自动就出来了。==

      **再往下推一层**：
      正因为 DMA 不打断程序流，它==才可能做到"高速"==；
      而中断每次都要付一整套现场开销，==注定只能服务中低速设备==。

      ==别把这张表当九条互不相干的事实背，它是一条因果链。==
    ` },

    /* ================================================================== */
    { t: 'h', id: 'calc', c: '六、DMA 的性能计算' },

    { t: 'example',
      id: 'ex-dma-bandwidth',
      title: 'DMA 偷走了多少主存带宽',
      source: '408 常见计算',
      level: 3,
      problem: String.raw`
        某磁盘的数据传输率为 $20\ \text{MB/s}$，采用==周期挪用==方式的 DMA
        与主存交换数据。主存的存取周期为 $50\ \text{ns}$，
        ==每次 DMA 传送 $4$ 个字节，占用一个存取周期==。
        （取 $1\ \text{MB}=10^{6}\ \text{B}$）

        求：**(1)** 每秒 DMA 要挪用多少个存取周期
        　**(2)** DMA 占用了主存带宽的百分之几
        　**(3)** 传送一个 $4\ \text{KB}$ 的数据块需要多长时间（不计预处理与后处理，$1\ \text{KB}=1024\ \text{B}$）
      `,
      idea: String.raw`
        (1)(2) 的关键是先算清==每秒挪用次数==：

        $$\text{挪用次数} = \frac{\text{数据传输率}}{\textbf{每次挪用传送的字节数}}$$

        (2) 的分母是==主存每秒能提供的存取周期总数==，
        即 $1\div50\ \text{ns}=2\times10^{7}$ 个/秒。
        ==别把分母写成 CPU 主频==——这一问问的是==主存带宽==，
        和[中断开销那类题问 CPU 时间](#/co/io/io-mode?at=ex-three-cost)==分母不一样==。
      `,
      solution: String.raw`
        **(1) 每秒挪用的存取周期数**

        $$\frac{20\times10^{6}\ \text{B/s}}{4\ \text{B}} = 5\times10^{6}\ \text{次/秒}$$

        每次占用一个存取周期，故每秒挪用 $\boxed{5\times10^{6}}$ 个存取周期。

        **(2) 占用主存带宽的比例**

        主存每秒可提供的存取周期总数：

        $$\frac{1\ \text{s}}{50\ \text{ns}} = \frac{1}{50\times10^{-9}} = 2\times10^{7}\ \text{个/秒}$$

        $$\frac{5\times10^{6}}{2\times10^{7}} = \boxed{25\%}$$

        （等价算法：每秒被偷走的时间 $=5\times10^{6}\times50\ \text{ns}=0.25\ \text{s}$，
        同样是 $25\%$。）

        **(3) 传送 $4\ \text{KB}$ 所需时间**

        传送次数：$\dfrac{4\times1024}{4} = 1024$ 次

        $$1024\times50\ \text{ns} = 51200\ \text{ns} = \boxed{51.2\ \mu\text{s}}$$
      `,
      comment: String.raw`
        **$25\%$ 这个数字值得记住**：==DMA 不是"免费"的==。

        它确实把 ==CPU 的时间开销==降到了几乎为零
        （[那道题算出 $0.2\%$](#/co/io/io-mode?at=ex-three-cost)），
        但它==实实在在地吃掉了主存带宽==。
        CPU 在这段时间里访存会变慢 —— ==只是它不知道自己变慢了==。

        $$\underbrace{\text{CPU 时间开销}\approx0}_{\text{DMA 的卖点}}
        \qquad\ne\qquad
        \underbrace{\text{主存带宽开销}=25\%}_{\text{DMA 的隐性成本}}$$

        ==这两个百分比问的是完全不同的东西，别混用==：
        问"CPU 用于 I/O 的时间占比" $\Rightarrow$ 分母是 ==CPU 主频==；
        问"占用主存带宽的比例" $\Rightarrow$ 分母是 ==主存每秒的存取周期数==。

        **一个自然的追问**：如果再挂三个这样的磁盘会怎样？
        $4\times25\%=100\%$ ——==主存带宽被 DMA 吃光，CPU 一次都访不了存==。
        ==这就是"DMA 也有上限"的具体含义==，
        也是多设备系统里必须核算总带宽的原因。
      `,
    },

    /* ================================================================== */
    { t: 'h', id: 'traps', c: '七、略高于考纲：DMA 在真实系统里的两个陷阱' },

    { t: 'key', id: 'coherency', title: '★ 陷阱一：DMA 与 Cache 的一致性', c: String.raw`
      ==DMA 直接读写主存，它完全绕过了 CPU 的 Cache。==
      于是两个方向各有一个问题：

      **① 设备 $\to$ 主存（DMA 写）：CPU 会读到过时数据**

      ~~~
      Cache:  [地址 A: 旧值 100]        ← CPU 之前读过，缓存着
      主存:   [地址 A: 旧值 100]
                    ↓ DMA 把新数据写进主存
      主存:   [地址 A: 新值 999]  ✓
      Cache:  [地址 A: 旧值 100]  ✗    ← 【没人告诉它】
                    ↓
      CPU 读 A → Cache 命中 → 读到 100  ★ 错！
      ~~~
      **对策**：DMA 完成后，==把这些地址对应的 Cache 行作废（invalidate）==。

      **② 主存 $\to$ 设备（DMA 读）：设备会读到过时数据**

      ~~~
      CPU 写 A = 999  → 写回法(write-back)下只改了 Cache，【主存还是旧的】
      Cache:  [地址 A: 999，脏]
      主存:   [地址 A: 100]
                    ↓ DMA 从主存读
      设备收到 100  ★ 错！
      ~~~
      **对策**：DMA 开始前，==把脏的 Cache 行写回主存（flush / writeback）==。

      **三种工程解法**：

      | 方案 | 做法 | 代价 |
      |---|---|---|
      | **软件维护** | 驱动在 DMA 前后显式 flush / invalidate | ==容易漏，漏了就是极难复现的 bug== |
      | **总线侦听** | Cache 控制器==监听总线上的 DMA 地址==，自动作废/供数 | 硬件复杂，但==对软件透明== |
      | **不可缓存区** | 把 DMA 缓冲区标记为 ==uncacheable== | 简单，但==CPU 访问这块内存会很慢== |

      ==这是 Cache 章和 I/O 章真正交汇的地方==：
      Cache 的正确性建立在"主存只被 CPU 改"这个假设上，
      ==而 DMA 恰恰打破了这个假设==。
      [MMIO 必须标记为不可缓存](#/co/io/io-mode?at=mmio-wins)是同一类问题的另一个方向。
    ` },

    { t: 'key', id: 'iommu', title: '★ 陷阱二：DMA 用的是物理地址', c: String.raw`
      DMA 控制器==没有 MMU，它发出的是物理地址==。而两个麻烦随之而来：

      **① 虚拟连续 $\ne$ 物理连续。**
      用户程序申请的 $64\ \text{KB}$ 缓冲区在虚拟地址上是连续的，
      ==在物理上很可能散落在十几个不连续的页帧里==。
      而 DMA 控制器只会"从起始地址开始一路加 1"。

      **对策：分散/聚集 DMA（scatter-gather）**——
      给 DMA 控制器一张==描述符链表==，每个描述符描述一段物理连续的区域，
      ==DMA 自己顺着链表走==。

      **② 页面可能被换出。**
      DMA 期间那些物理页==绝不能被 OS 换出或移动==，
      否则数据会写进别的进程的内存。
      **对策**：驱动必须先把缓冲区==锁定（pin）==在物理内存中。

      **③ 更彻底的方案：IOMMU。**
      ==给设备也配一个 MMU==，设备发出的"IO 虚拟地址"由 IOMMU 翻译成物理地址。
      于是设备眼中的缓冲区==又变回连续的了==，分散/聚集的复杂性被硬件吸收。

      ==IOMMU 之于设备，正如 MMU 之于进程==——
      同一个"地址翻译 + 隔离"的思想，用在了另一类主体上。
    ` },

    { t: 'key', id: 'dma-attack', title: 'IOMMU 的另一半价值：防住 DMA 攻击', c: String.raw`
      ==没有 IOMMU 时，任何能发起 DMA 的设备都能读写全部物理内存。==
      注意这句话有多可怕：==它绕过了操作系统的一切保护机制==，
      因为 OS 的保护建立在 MMU 上，而 DMA 根本不经过 MMU。

      **DMA 攻击**：通过 Thunderbolt / PCIe 接口插入一个恶意设备，
      直接把整个物理内存 dump 出来 ——
      ==可以读出磁盘加密密钥，甚至改写内存里的锁屏校验代码==。
      锁屏的笔记本电脑曾经就这样被攻破过。

      **IOMMU 的防御**：每个设备只能访问==被明确授权的那几个物理页==，
      越界的 DMA 请求==直接被 IOMMU 拒绝==。

      ==这也是为什么现代笔记本默认开启 IOMMU（Intel VT-d / AMD-Vi），
      而且在解锁前不给外接设备授予 DMA 权限。==

      **回到考试**：408 不会考这些，
      但它解释了一件教材没说清的事 ——
      ==DMA 的本质是"给设备开了一条绕过 CPU 的后门"==，
      性能上这是巨大的胜利，==安全上这是必须被重新管住的漏洞==。
    ` },

    /* ================================================================== */
    { t: 'h', id: 'pitfalls', c: '八、易错清单' },

    { t: 'warn', id: 'pitfall-list', title: '这一节的固定失分点', c: String.raw`
      1. **"DMA 方式不需要中断"** —— ==错==，整块传完仍要中断一次做后处理，
         见[这里](#/co/io/dma?at=dma-needs-int)。
      2. **"DMA 传送期间 CPU 完全不能工作"** ——
         ==只有"停止 CPU 访存"那一种方式才是这样==；
         周期挪用下 CPU 照常执行不访存的部分。
      3. **"DMA 要保存现场"** —— ==不需要==，它抢的是总线不是执行权，
         见[这条因果链](#/co/io/dma?at=granularity-insight)。
      4. **把三种访存方式的适用条件搞混** ——
         ==周期挪用适用于"I/O 读写周期 > 主存存取周期"==，
         ==交替访存适用于"CPU 工作周期 > 主存存取周期"==。
      5. **说 DMA 优先是因为"DMA 更重要"** ——
         是因为==外设有时间窗口，不及时搬走就丢数据==。
      6. **算带宽占用时分母用了 CPU 主频** ——
         问主存带宽就用==主存每秒的存取周期数==，
         见[这道题的辨析](#/co/io/dma?at=ex-dma-bandwidth)。
      7. **认为 DMA 是"免费"的** ——
         ==CPU 时间几乎不花，但主存带宽实实在在被吃掉==。
      8. **预处理和后处理搞反谁来做** ——
         ==两头都是 CPU 做的，中间的数据传送才是 DMA 硬件做的==。
      9. **认为字计数器减到 0 才算传完** ——
         硬件常用==负数装入、加到溢出==，见[原因](#/co/io/dma?at=wc-trick)。
      10. **认为 DMA 控制器是从设备** ——
          ==它是主设备==，要参与[总线仲裁](#/co/bus/bus-timing?at=roles)。
      11. **认为一次 DMA 传送只申请一次总线** ——
          ==周期挪用下每传一个字都要申请和归还一次==。
    ` },

    { t: 'md', c: String.raw`
      ---

      回到全景：[五种 I/O 方式的那条轴](#/co/io/io-mode?at=the-axis)；
      前一格：[中断响应与处理流程](#/co/io/interrupt?at=timeline)。
    ` },

  ],
});
