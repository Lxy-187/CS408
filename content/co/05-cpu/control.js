/* ==========================================================================
   计算机组成原理 / 5 中央处理器 / 控制器（硬布线 / 微程序）
   —— 这一节的全部内容，都在回答同一个问题：
      「在第几个时刻，让哪几根控制线变成 1」这张表，该怎么实现。
      硬布线用门电路把它算出来，微程序把它存起来查出来。
   ========================================================================== */

KM.page({
  path: 'co/cpu/control',
  title: '控制器（硬布线 / 微程序）',
  subtitle: '控制器要做的事只有一件：**在正确的时刻发出正确的控制信号**。两种实现的分歧只在于，这张信号表是**算出来的**还是**存起来查的**',
  tags: ['高频', '必考', '综合应用', '概念辨析'],
  updated: '2026-08-16',

  blocks: [

    /* ================================================================== */
    { t: 'h', id: 'position', c: '一、先看清控制器站在哪' },

    { t: 'md', c: String.raw`
      CPU 可以粗暴地切成两半：**运算器**负责"做"，**控制器**负责"指挥"。
      运算器就是[数据通路](#/co/cpu/datapath?at=def)——
      一堆寄存器、ALU 和连接它们的通路，它==本身没有任何主动性==——
      $\texttt{PC}$ 里的值不会自己跑到 $\texttt{MAR}$ 去，ALU 不会自己知道该做加法还是减法。

      ==所有这些"动一下"的动作，都需要有一根控制线在那一拍变成 1。==
      控制器的全部职责，就是产生这些线上的电平。
    ` },

    { t: 'diagram', id: 'cu-io', title: '控制器的输入与输出（先把它当一个黑盒看清接口）',
      note: '四类输入进去，一大把控制信号出来',
      caption: String.raw`一句话概括这张图：==控制信号 $=f(\,$指令,\ 第几步,\ 状态标志$\,)$==。
        硬布线用**门电路**实现这个 $f$，微程序用**查表**实现这个 $f$ ——
        整节剩下的内容都只是在讲这两种实现。`,
      svg: String.raw`
<svg class="dg" viewBox="0 0 700 244" role="img" aria-label="控制器的四类输入与控制信号输出">
  <g class="n k"><rect x="16" y="16" width="210" height="40" rx="7"/>
    <text class="bt sm" x="121" y="31" text-anchor="middle" dominant-baseline="central">① 操作码 OP(IR)</text>
    <text class="bs" x="121" y="47" text-anchor="middle" dominant-baseline="central">现在要执行哪条指令</text></g>
  <g class="n k"><rect x="16" y="60" width="210" height="40" rx="7"/>
    <text class="bt sm" x="121" y="75" text-anchor="middle" dominant-baseline="central">② 时序信号 T0 T1 …</text>
    <text class="bs" x="121" y="91" text-anchor="middle" dominant-baseline="central">现在是这条指令的第几步</text></g>
  <g class="n k"><rect x="16" y="104" width="210" height="40" rx="7"/>
    <text class="bt sm" x="121" y="119" text-anchor="middle" dominant-baseline="central">③ 状态标志 ZF CF OF SF</text>
    <text class="bs" x="121" y="135" text-anchor="middle" dominant-baseline="central">上一步算出来的结果如何</text></g>
  <g class="n k"><rect x="16" y="148" width="210" height="40" rx="7"/>
    <text class="bt sm" x="121" y="163" text-anchor="middle" dominant-baseline="central">④ 外来信号 中断 / DMA</text>
    <text class="bs" x="121" y="179" text-anchor="middle" dominant-baseline="central">外面出了什么事</text></g>

  <path class="ar plain" d="M226,36 H248"/>
  <path class="ar plain" d="M226,80 H248"/>
  <path class="ar plain" d="M226,124 H248"/>
  <path class="ar plain" d="M226,168 H248"/>
  <path class="ar plain" d="M248,36 V168"/>
  <path class="ar" d="M248,102 H274"/>

  <g class="n p"><rect x="278" y="72" width="132" height="60" rx="8"/>
    <text class="bt" x="344" y="93" text-anchor="middle" dominant-baseline="central">控制器 CU</text>
    <text class="bs" x="344" y="113" text-anchor="middle" dominant-baseline="central">门电路 或 查表</text></g>
  <path class="ar" d="M410,102 H444"/>

  <g class="n a"><rect x="448" y="46" width="236" height="112" rx="8"/>
    <text class="bt sm" x="566" y="66" text-anchor="middle" dominant-baseline="central">一大把控制信号（微命令）</text>
    <text class="bs" x="566" y="92" text-anchor="middle" dominant-baseline="central">PC→MAR　1→R　MDR→IR</text>
    <text class="bs" x="566" y="112" text-anchor="middle" dominant-baseline="central">ALU 功能选择 · MUX 选择端</text>
    <text class="bs" x="566" y="132" text-anchor="middle" dominant-baseline="central">各个寄存器的写使能 …</text></g>

  <text class="cap" x="0" y="216">数据通路本身没有任何主动性：每一个「动一下」，都要有一根控制线在那一拍变成 1</text>
  <text class="lb" x="0" y="236">控制器的全部职责，就是产生这些线上的电平</text>
</svg>` },

    { t: 'key', id: 'the-whole-point', title: '★ 一句话看穿这一整节', c: String.raw`
      把一条指令的执行过程画成一张表：==行是时间步，列是每一根控制线，格子里填 0 或 1==。
      整个指令系统的这张大表，就是控制器要实现的全部内容。

      | 时间步 | $\texttt{PC}\to\texttt{MAR}$ | 读命令 | $\texttt{MDR}\to\texttt{IR}$ | $\texttt{PC}+1$ | ALU 加 | … |
      |---|---|---|---|---|---|---|
      | 取指 $T_0$ | **1** | **1** | 0 | 0 | 0 | |
      | 取指 $T_1$ | 0 | 0 | 0 | **1** | 0 | |
      | 取指 $T_2$ | 0 | 0 | **1** | 0 | 0 | |
      | ADD 执行 $T_1$ | 0 | 0 | 0 | 0 | **1** | |

      - **硬布线**：把每一==列==看成一个布尔函数，用与门或门把它算出来；
      - **微程序**：把每一==行==原封不动存进一块 ROM，用地址把它读出来。

      ==同一张表，一个按列切，一个按行切。==
      这一节后面所有的概念、格式、编码、地址形成，都是这两句话的展开。
    ` },

    /* ================================================================== */
    { t: 'h', id: 'timing', c: '二、时序：把指令切成节拍' },

    { t: 'key', id: 'four-levels', title: '四级时间层次（术语必须分清）', c: String.raw`
      $$\underbrace{\text{指令周期}}_{\text{执行完一条指令}}
      \supset \underbrace{\text{机器周期}}_{\text{取指/间址/执行/中断}}
      \supset \underbrace{\text{节拍（时钟周期）}}_{\text{最小时间单位}}
      \supset \underbrace{\text{微操作}}_{\text{一次数据传送/一次运算}}$$

      | 层次 | 别名 | 一个里面有几个下一级 |
      |---|---|---|
      | **指令周期** | —— | 若干个机器周期（不同指令不一样多） |
      | **机器周期** | CPU 周期、工作周期 | 若干个节拍 |
      | **节拍** | 时钟周期、$T$ 周期、状态周期 | ==若干个可以并行的微操作== |
      | **微操作** | —— | 不可再分 |

      ==节拍是"最小的时间单位"，微操作是"最小的动作单位"==，两者不是一回事：
      **一个节拍内可以同时发生好几个微操作**，只要它们互不冲突。

      四种机器周期用四个触发器标识：$\texttt{FE}$（取指）、$\texttt{IND}$（间址）、
      $\texttt{EX}$（执行）、$\texttt{INT}$（中断），==任何时刻只有一个为 1==。
      中断周期那一个见[中断机制](#/co/cpu/exception?at=cpu-cycle)。
    ` },

    { t: 'code', id: 'fetch-micro', title: '取指周期的微操作序列（所有指令都一样，务必背熟）', lang: '',
      c: String.raw`
        T0:   (PC) → MAR ,  1 → R
              └ 把要取的指令地址送地址寄存器，同时发读命令

        T1:   M(MAR) → MDR ,  (PC) + 1 → PC
              └ 主存把指令送到 MDR      └ PC 加 1（这两件事互不干扰，可并行）

        T2:   (MDR) → IR ,  OP(IR) → 控制单元
              └ 指令进指令寄存器        └ 操作码送去译码，决定后面怎么走

        ★ 注意 (PC)+1 为什么放在 T1 而不是 T0：
          T0 时 PC 的值刚被送去 MAR，此刻若改 PC，MAR 未必已经锁存稳定。
          放在 T1 既安全，又和"等主存"这段慢操作重叠起来，不额外花时间。
      ` },

    { t: 'key', id: 'timing-rules', title: '★ 微操作往节拍里塞的三条原则（设计题的评分点）', c: String.raw`
      1. **有先后依赖的微操作，不能放在同一节拍**。
         $\texttt{(PC)}\to\texttt{MAR}$ 和 $\texttt{M(MAR)}\to\texttt{MDR}$ 之间隔着一次访存，
         ==前者的结果是后者的输入==，必须分开。
      2. **被控对象不同、互不冲突的微操作，尽量塞进同一节拍**。
         这是提高性能的唯一手段 —— ==节拍数就是指令的执行时间==。
      3. **占用时间较短的微操作，可以放进同一节拍并允许有先后**。
         一个节拍内部，信号传播是有先后的；
         只要在这一拍结束前全部稳定，就允许"先做寄存器传送，再做一次简单的与运算"。

      **判断两个微操作能不能同拍，就问两句**：
      ==有没有数据依赖（一个的输出是另一个的输入）？会不会抢同一个部件（同一条总线、同一个 ALU）？==
      两个都没有，就可以并行。

      在单总线结构上，第二问有个更硬的版本：
      ==同一拍内只能有一个部件把值送上总线==，
      见[单总线的三条规矩](#/co/cpu/datapath?at=signal-rules)。
    ` },

    { t: 'compare', id: 'control-mode', title: '控制方式：节拍怎么定长短',
      cols: ['方式', '怎么做', '优点', '缺点'],
      rows: [
        ['**同步控制**', '统一时钟，==节拍长度固定==，以最慢的微操作为准', '控制简单、易实现', '==浪费==：快的微操作也得等满一拍'],
        ['同步·不定长机器周期', '大多数操作用短周期，慢的（如访存）==延长几个节拍==', '折中，主流做法', '控制略复杂'],
        ['**异步控制**', '==没有统一时钟==，靠"请求—应答"握手，做完就报告', '==时间利用率最高==', '控制线路复杂'],
        ['**联合控制**', '==同步为主==，个别慢部件（访存、I/O）用异步', '兼顾两者，==实际机器普遍采用==', '——'],
      ] },

    /* ================================================================== */
    { t: 'h', id: 'hardwired', c: '三、硬布线控制器：把信号算出来' },

    { t: 'md', c: String.raw`
      硬布线控制器又叫**组合逻辑控制器**。它的想法极其朴素：
      既然控制信号是 $f(\text{指令},\text{节拍},\text{状态})$ 的函数，
      ==那就把这个函数写成布尔表达式，用与门或门搭出来==。
    ` },

    { t: 'diagram', id: 'hardwired-struct', title: '硬布线控制器的结构',
      note: '左边三路输入，中间一大块组合逻辑，右边一把控制信号',
      caption: String.raw`中间那块==组合逻辑网络就是全部的设计工作量==，也是"硬布线"这个名字的由来——
        它是用导线硬接出来的。==要改一条指令，就得重新设计、重新布线==，
        这也正是[为什么 RISC 用硬布线、CISC 早年用微程序](#/co/cpu/control?at=why-risc-hardwired)。`,
      svg: String.raw`
<svg class="dg" viewBox="0 0 700 232" role="img" aria-label="硬布线控制器：指令译码器、节拍发生器、状态标志送入组合逻辑网络产生控制信号">
  <text class="lb" x="84" y="42" text-anchor="end">OP(IR)</text>
  <g class="n k"><rect x="90" y="20" width="160" height="44" rx="7"/>
    <text class="bt sm" x="170" y="37" text-anchor="middle" dominant-baseline="central">指令译码器</text>
    <text class="bs" x="170" y="54" text-anchor="middle" dominant-baseline="central">n → 2ⁿ</text></g>

  <text class="lb" x="84" y="110" text-anchor="end">时钟</text>
  <g class="n k"><rect x="90" y="88" width="160" height="44" rx="7"/>
    <text class="bt sm" x="170" y="105" text-anchor="middle" dominant-baseline="central">节拍发生器</text>
    <text class="bs" x="170" y="122" text-anchor="middle" dominant-baseline="central">环形计数器</text></g>

  <text class="lb" x="84" y="178" text-anchor="end">外来信号</text>
  <g class="n k"><rect x="90" y="156" width="160" height="44" rx="7"/>
    <text class="bt sm" x="170" y="173" text-anchor="middle" dominant-baseline="central">标志与请求</text>
    <text class="bs" x="170" y="190" text-anchor="middle" dominant-baseline="central">ZF CF OF · INT</text></g>

  <path class="ar" d="M250,42 H330 V70 H392"/>
  <text class="lb mono" x="256" y="36">I_ADD I_SUB I_LDA …</text>
  <path class="ar" d="M250,110 H392"/>
  <text class="lb mono" x="256" y="104">T0 T1 T2 …</text>
  <path class="ar" d="M250,178 H330 V150 H392"/>
  <text class="lb mono" x="256" y="172">FE IND EX INT</text>

  <g class="n g"><rect x="396" y="48" width="176" height="124" rx="8"/>
    <text class="bt" x="484" y="98" text-anchor="middle" dominant-baseline="central">组合逻辑网络</text>
    <text class="bs" x="484" y="122" text-anchor="middle" dominant-baseline="central">一堆与门 / 或门</text></g>

  <path class="ar em" d="M572,72 H636"/>
  <path class="ar em" d="M572,98 H636"/>
  <path class="ar em" d="M572,124 H636"/>
  <path class="ar em" d="M572,150 H636"/>
  <text class="lb mono em" x="642" y="76">C1</text>
  <text class="lb mono em" x="642" y="102">C2</text>
  <text class="lb mono em" x="642" y="128">C3</text>
  <text class="lb mono em" x="642" y="154">Cm</text>

  <text class="cap" x="0" y="222">每一根 Ci 都是一个布尔表达式：把「哪条指令 · 第几拍 · 什么标志」与起来，再把需要它的场合或起来</text>
</svg>` },

    { t: 'key', id: 'boolean-expr', title: '★ 一个控制信号的表达式长什么样（会写这个就懂硬布线了）', c: String.raw`
      以"发出读命令 $1\to\texttt{R}$"这根线为例，问：==它在哪些时刻该是 1？==

      逐个机器周期数过去：

      - **取指周期**：一定要读主存取指令 $\Rightarrow$ $\texttt{FE}\cdot T_0$
      - **间址周期**：要读出有效地址 $\Rightarrow$ $\texttt{IND}\cdot T_0$
      - **执行周期**：==只有需要访存取操作数的指令才读== $\Rightarrow$
        $\texttt{EX}\cdot T_0\cdot(\texttt{ADD}+\texttt{SUB}+\texttt{LDA}+\cdots)$
      - **中断周期**：中断是往主存==写==断点，不读 $\Rightarrow$ 无贡献

      合起来：

      $$1\to\texttt{R}\ =\ \texttt{FE}\cdot T_0\ +\ \texttt{IND}\cdot T_0\
      +\ \texttt{EX}\cdot T_0\cdot(\texttt{ADD}+\texttt{SUB}+\texttt{LDA}+\cdots)$$

      提取公因子化简：

      $$=\ T_0\cdot\big[\texttt{FE}+\texttt{IND}+\texttt{EX}\cdot(\texttt{ADD}+\texttt{SUB}+\texttt{LDA}+\cdots)\big]$$

      ==每一根控制线都要这样推导一遍==，几十上百根线，
      这就是为什么硬布线控制器"设计繁琐、不易修改"。
    ` },

    { t: 'steps', id: 'hardwired-steps', title: '硬布线控制器的设计步骤（简答题按这四步答）', items: [
      { title: '列出每条指令的微操作序列',
        c: String.raw`
          把取指、间址、执行、中断四个周期的微操作全部写出来，
          ==注意提取公共部分==（取指周期所有指令都一样，只需设计一次）。` },
      { title: '选择控制方式，安排微操作时序',
        c: String.raw`
          决定一个机器周期分几个节拍，按[三条原则](#/co/cpu/control?at=timing-rules)
          把微操作分配到具体节拍上。==这一步决定了机器的性能==。` },
      { title: '列微操作命令的操作时间表，写逻辑表达式',
        c: String.raw`
          表的行是"周期 + 节拍"，列是每一条指令，格子里填该发哪些微命令。
          然后==按列（按信号）汇总==，写出每根控制线的布尔表达式并化简。` },
      { title: '用门电路实现',
        c: String.raw`
          化简后的表达式画成逻辑图。现代做法是==写成 HDL 交给综合工具==，
          或者用 PLA（可编程逻辑阵列）实现，兼顾规整性与速度。` },
    ] },

    { t: 'key', id: 'fsm-view', title: '略高一点：状态机视角（现代硬布线的真实写法）', c: String.raw`
      教材里的"节拍发生器 + 组合逻辑"是一种==把时间显式编码==的写法：
      $T_0,T_1,T_2$ 就是三个时间槽。

      现代数字设计更常用的是==有限状态机（FSM）==：
      用一个**状态寄存器**保存"当前走到哪一步了"，
      再用组合逻辑同时算出**输出信号**和**下一个状态**。

      ~~~
      现态 ──┬──▶ 组合逻辑 ──▶ 控制信号（Moore 机只看现态，Mealy 机还看输入）
             │        │
             │        └──▶ 次态 ──▶ 状态寄存器 ──┐
             └──────────────────────────────────┘
      ~~~

      **两者是等价的**，但 FSM 更灵活：
      ==状态数不必等于节拍数==，可以为不同指令安排不同长度的状态序列，
      "多周期 CPU"就是这么实现的。

      ==这个视角还揭示了一件事==：微程序控制器其实也是一个状态机，
      只不过它的"状态"就是**微地址**，"次态逻辑"就是**微地址形成部件**。
      两种控制器的差别，==仅在于次态与输出这两个函数是用逻辑门实现还是用 ROM 实现==。

      这条线还能往下拉两层，单开了一页：
      [每一拍到底谁在变、控制信号活在哪一段时间](#/co/cpu/fsm?at=timing-chart)，
      以及[为什么"有反馈环"不足以把 CU 和 $	exttt{PC}$ 区分开](#/co/cpu/fsm?at=broadcast-is-the-line)。
    ` },

    /* ================================================================== */
    { t: 'h', id: 'micro-why', c: '四、微程序控制器：把信号存起来' },

    { t: 'key', id: 'wilkes', title: 'Wilkes 的那个洞察（理解微程序的起点）', c: String.raw`
      1951 年 Maurice Wilkes 注意到一件事：
      ==控制信号的时序表，本质上就是一个二维的 0/1 阵列==
      （行 = 时间步，列 = 控制线）。

      而计算机里==本来就有一种极其规整、极其便宜的二维 0/1 阵列==，那就是**存储器**。

      > 那为什么要用一堆杂乱无章的随机逻辑去"算"这张表，
      > 而不直接把它==存进一块只读存储器==，用地址读出来？

      这就是微程序设计的全部思想：==用规整的存储阵列，取代不规整的随机逻辑==。

      **代价与收益一目了然**：
      读一次 ROM 比过几级门电路==慢==，这是代价；
      但改一张表比重新布线==容易太多==，这是收益。
      后面所有的对比，都是这一句的展开。
    ` },

    { t: 'key', id: 'five-terms', title: '★★ 五个概念，考试年年考辨析', c: String.raw`
      | 概念 | 定义 | 关键点 |
      |---|---|---|
      | **微命令** | 控制部件通过控制线发往执行部件的==控制信号== | ==命令==，是"发出去的东西" |
      | **微操作** | 执行部件收到微命令后所进行的==操作== | ==动作==，是"做出来的事" |
      | **微指令** | ==若干微命令的集合==，存在控制存储器的一个单元里 | 一条微指令 = ==一个节拍==内并行发出的全部微命令 |
      | **微周期** | ==读出并执行一条微指令==所需的时间 | 通常 = 一个节拍 |
      | **微程序** | 一系列微指令的有序集合 | ==一条机器指令 ↔ 一段微程序== |

      **微命令与微操作是==一一对应==的**：
      微命令是"因"，微操作是"果"，谈的是同一件事的两端。
      考试问"两者关系"，答==一一对应==就对了。

      **一条微指令 ≠ 一条机器指令**，差着一个数量级：

      $$\text{一条机器指令} = \text{一段微程序} = \text{若干条微指令}$$
      $$\text{一条微指令} = \text{若干个微命令（同一拍并行发出）}$$
    ` },

    { t: 'code', id: 'micro-struct', title: '微程序控制器的结构与工作循环', lang: '',
      c: String.raw`
                     ┌─────────────────────────────────┐
                     │      控制存储器 CM (ROM)          │
                     │  ┌───────────────────────────┐  │
        μAR ─── 地址 ─▶│  │ 0  取指微程序第 1 条        │  │
       (CMAR)         │  │ 1  取指微程序第 2 条        │  │
          ▲           │  │ 2  ADD 微程序第 1 条        │  │
          │           │  │ 3  ADD 微程序第 2 条        │  │
          │           │  │ …                          │  │
          │           │  └───────────────────────────┘  │
          │           └──────────────┬──────────────────┘
          │                          ▼
          │              ┌────────────────────────┐
          │              │   微指令寄存器 μIR (CMDR) │
          │              ├──────────┬──────┬──────┤
          │              │ 操作控制字段│判别字段│下地址 │
          │              └────┬─────┴──┬───┴──┬───┘
          │                   │        │      │
          │                   ▼        ▼      │
          │             微命令送数据通路  ┌────────────┐
          └─────────────────────────────│ 微地址形成部件 │
                                        └────────────┘
                                          ▲        ▲
                                 OP(IR) ──┘        └── 状态标志

        工作循环（永远在转）：
          ① μAR 指向一条微指令 → 从 CM 读出送 μIR
          ② μIR 的操作控制字段 → 变成控制信号发给数据通路
          ③ μIR 的下地址字段 + 判别字段 + OP(IR) + 标志 → 算出下一个微地址 → μAR
          ④ 回到 ①
      ` },

    { t: 'steps', id: 'micro-flow', title: '执行一条机器指令，微程序控制器都做了什么', items: [
      { title: '执行「取指微程序」',
        c: String.raw`
          开机 / 上一条指令结束后，$\mu\texttt{AR}$ ==被硬件置成取指微程序的入口==
          （通常是 CM 的 0 号单元）。这段微程序==所有指令公用，只有一份==，
          它把机器指令从主存取进 $\texttt{IR}$。` },
      { title: '由操作码转入该指令的执行微程序',
        c: String.raw`
          $\texttt{IR}$ 里有操作码了，==把 $\texttt{OP(IR)}$ 送进微地址形成部件==，
          翻译成这条指令对应微程序的==入口微地址==，送 $\mu\texttt{AR}$。
          ==这是"一条机器指令找到自己那段微程序"的唯一时刻。==` },
      { title: '逐条执行该微程序',
        c: String.raw`
          每读出一条微指令，就发出一拍的控制信号；
          下一条的地址由下地址字段（可能还要看标志位）决定。` },
      { title: '返回取指微程序入口',
        c: String.raw`
          该微程序的最后一条微指令，==把下地址置回取指微程序的入口==，
          于是自然开始取下一条机器指令。==这个循环永不停止。==` },
    ] },

    { t: 'key', id: 'cm-facts', title: '控制存储器 CM：三个必考的属性', c: String.raw`
      | 问题 | 答案 | 为什么 |
      |---|---|---|
      | 在哪 | ==在 CPU 内部== | 它要在每个微周期被访问，放外面太慢 |
      | 什么器件 | ==ROM==（只读） | 微程序出厂固定，==运行时不需要改写== |
      | 存什么 | ==微程序（微指令）== | 对比：主存存的是==程序（机器指令）== |

      **配套的两个寄存器，和主存那一对是完全对应的**：

      $$\begin{array}{ccc}
      \text{主存} & \longleftrightarrow & \text{控制存储器 CM}\\
      \texttt{MAR}\ (\text{地址}) & \longleftrightarrow & \mu\texttt{AR}\ /\ \texttt{CMAR}\\
      \texttt{MDR}\ (\text{数据}) & \longleftrightarrow & \mu\texttt{IR}\ /\ \texttt{CMDR}\\
      \texttt{PC}\ (\text{下条地址}) & \longleftrightarrow & \text{微地址形成部件}
      \end{array}$$

      ==把 CM 想成"CPU 内部的一台小计算机的主存"，一切就顺了==：
      它有自己的"程序"（微程序）、自己的"PC"（微地址形成部件）、
      自己的"指令"（微指令）。
    ` },

    { t: 'warn', id: 'micro-pc-analogy', title: '★ 微地址寄存器就是微程序控制器的"节拍发生器"', c: String.raw`
      这一句能把两种控制器彻底串起来：

      - **硬布线**里，"现在是第几步"由==节拍发生器显式给出==（$T_0,T_1,T_2\ldots$），
        它是一个环形计数器，硬件上看得见摸得着；
      - **微程序**里，"现在是第几步"==隐含在 $\mu\texttt{AR}$ 的值里==——
        微地址走到哪，就说明执行到第几拍了。

      所以 ==$\mu\texttt{AR}$ 扮演的角色，正是节拍发生器 + 状态寄存器==。
      这也解释了为什么微程序控制器==天然支持不同指令有不同的执行步数==：
      微程序长一点，微地址就多走几步，不需要额外设计。
      而硬布线要做到这一点，得让节拍发生器==能提前结束==（发一个"周期结束"信号）。
    ` },

    /* ================================================================== */
    { t: 'h', id: 'encoding', c: '五、微指令的编码方式（三种，考计算）' },

    { t: 'md', c: String.raw`
      微指令的操作控制字段要表示"这一拍发哪些微命令"。
      ==怎么把几十上百个微命令塞进尽可能短的字段里==，就是编码方式要解决的问题。
      这是一个==字长与速度的权衡==。
    ` },

    { t: 'compare', id: 'encoding-table', title: '★ 三种编码方式',
      cols: ['方式', '怎么做', '字长', '速度', '并行性'],
      rows: [
        ['**直接编码**（不译法）', '==一位对应一个微命令==，该位为 1 就发出该命令', '==最长==（微命令有多少个就多少位）', '==最快==（不需要译码）', '==最好==（任意组合）'],
        ['**字段直接编码**', '把==互斥==的微命令分到同一字段，字段经==译码器==产生微命令', '==显著缩短==', '慢一点（多一级译码延迟）', '字段之间可并行'],
        ['**字段间接编码**', '一个字段的含义==由另一个字段解释==', '==最短==', '最慢', '==削弱并行性==，只作辅助'],
      ] },

    { t: 'key', id: 'mutex-rule', title: '★★ 字段直接编码的两条铁律', c: String.raw`
      **① 互斥的放同一字段，相容的放不同字段。**

      - **互斥性微命令**：==不可能在同一拍同时出现==的命令。
        比如"ALU 做加法"和"ALU 做减法"—— 一个 ALU 一拍只能做一件事。
        它们==可以共用一个字段==，用不同的编码值区分。
      - **相容性微命令**：==可以同时出现==的命令。
        比如"$\texttt{PC}\to\texttt{MAR}$"和"发读命令"——
        它们必须==放在不同字段==，否则就没法同时发出。

      ==判断互斥还是相容，就看它们抢不抢同一个部件==，
      这和[微操作能否同拍](#/co/cpu/control?at=timing-rules)是同一个判据。

      **② 每个字段必须留出一个"什么都不发"的编码。**

      因为这一拍很可能==根本不需要动这个部件==。
      所以 $n$ 个互斥的微命令，需要的位数是

      $$\left\lceil \log_2 (n+1) \right\rceil \quad\text{而不是}\quad \left\lceil \log_2 n \right\rceil$$

      ==这个 $+1$ 是计算题最常丢分的地方==，几乎每次都有人漏。
    ` },

    { t: 'example',
      id: 'ex-field-encode',
      title: '字段直接编码：操作控制字段至少几位',
      source: '2011 真题',
      level: 2,
      problem: String.raw`
        某计算机的控制器采用微程序控制方式，微指令中的操作控制字段采用字段直接编码法，
        共有 $33$ 个微命令，构成 $5$ 个互斥类，分别包含 $7$、$3$、$12$、$5$ 和 $6$ 个微命令，
        则操作控制字段至少有（　）位。

        **A.** $5$　　**B.** $6$　　**C.** $15$　　**D.** $33$
      `,
      idea: String.raw`
        题目给了 $33$ 这个总数，==但它是个诱饵==（对应选项 D，那是直接编码的答案）。
        字段直接编码的位数==只和"分成几类、每类几个"有关==，和总数没有直接关系。

        真正要做的只有两件事：

        1. 每个互斥类算一个字段，位数 $=\lceil\log_2(n_i+1)\rceil$，
           ==别忘了那个 $+1$==（[为什么](#/co/cpu/control?at=mutex-rule)）；
        2. 五个字段的位数==相加==。

        ==注意是"相加"不是"取最大"== —— 五个字段是并排放在微指令里的，
        不是五选一。选项 A、B 就是给"取最大"和"$\log_2 33$"准备的。
      `,
      solution: String.raw`
        每一类留一个"不发命令"的编码，故第 $i$ 类需要 $\lceil\log_2(n_i+1)\rceil$ 位：

        | 互斥类 | 微命令数 $n_i$ | 需表示的状态数 $n_i+1$ | 位数 |
        |---|---|---|---|
        | 第 1 类 | $7$ | $8$ | $\log_2 8=\mathbf{3}$ |
        | 第 2 类 | $3$ | $4$ | $\log_2 4=\mathbf{2}$ |
        | 第 3 类 | $12$ | $13$ | $\lceil\log_2 13\rceil=\mathbf{4}$ |
        | 第 4 类 | $5$ | $6$ | $\lceil\log_2 6\rceil=\mathbf{3}$ |
        | 第 5 类 | $6$ | $7$ | $\lceil\log_2 7\rceil=\mathbf{3}$ |

        $$3+2+4+3+3=\boxed{15\ \text{位，选 C}}$$
      `,
      comment: String.raw`
        **四个选项的来历**：

        | 选项 | 怎么来的 | 错在哪 |
        |---|---|---|
        | A. $5$ | $\lceil\log_2 33\rceil=6$？或"5 个字段" | ==把字段数当成位数== |
        | B. $6$ | $\lceil\log_2 33\rceil$ | ==把 33 个微命令当成互斥的一组==（那就只能一次发一个了） |
        | **C. $15$** ✅ | 逐类算再相加 | —— |
        | D. $33$ | ==直接编码==的位数 | 答错了编码方式 |

        **顺手把三种编码在这道题上的结果都算出来，对比记忆**：

        | 编码方式 | 操作控制字段位数 |
        |---|---|
        | 直接编码（不译法） | $\mathbf{33}$ 位 |
        | 字段直接编码 | $\mathbf{15}$ 位 |
        | 若 33 个全互斥、编成一个字段 | $\lceil\log_2 34\rceil=\mathbf{6}$ 位（==但一拍只能发一个命令，并行性全丢==） |

        ==这张表把"字长 ↔ 并行性"的权衡说得很清楚==：
        位数从 33 压到 6，代价是从"任意组合"退化成"一次一个"。
        字段直接编码的 15 位是个折中 —— ==它保住了"5 个部件可以同时动"这个并行度==。

        **一句话记住**：==按互斥类分字段，每类位数 $\lceil\log_2(n+1)\rceil$，最后相加。==
      `,
    },

    /* ================================================================== */
    { t: 'h', id: 'format', c: '六、微指令的格式：水平型与垂直型' },

    { t: 'compare', id: 'hv-table', title: '★ 水平型 vs 垂直型微指令',
      cols: ['', '水平型微指令', '垂直型微指令'],
      rows: [
        ['基本特征', '==一次能定义并执行多个并行的微命令==', '==类似机器指令==，设置微操作码，一次只能定义一两个微命令'],
        ['微指令字长', '==长==', '==短=='],
        ['微程序长度（条数）', '==短==', '==长=='],
        ['执行速度', '==快==', '==慢=='],
        ['并行性', '==好==', '差'],
        ['与硬件的关系', '==直接对应数据通路，关系密切==', '==与数据通路关系不密切==，抽象一层'],
        ['编写难度', '==难==，要求编写者熟悉数据通路细节', '==容易==，像写汇编'],
        ['编码方式', '直接编码 / 字段直接编码', '字段编码 + 微操作码'],
      ] },

    { t: 'key', id: 'hv-tradeoff', title: '这组对比只有一条主线', c: String.raw`
      ==水平型是"宽而浅"，垂直型是"窄而深"==。

      同一条机器指令，用水平型可能 4 条微指令搞定，用垂直型要 12 条。
      于是：

      $$\text{CM 总容量} = \underbrace{\text{微指令条数}}_{\text{垂直型多}}\times\underbrace{\text{微指令字长}}_{\text{水平型长}}$$

      ==两者的乘积（总容量）往往差不了太多==，
      真正的差别在**执行时间**：垂直型要多访问几次 CM，
      而==每访问一次 CM 就是一个微周期==，所以垂直型明显慢。

      **还有一种混合型**：在水平型基础上做部分字段编码，缩短字长又不太伤并行性，
      ==实际机器多用这种==。

      **一个好记的类比**：水平型微指令像 VLIW（超长指令字），
      一条里塞满并行动作；垂直型像普通的 RISC 指令，一条干一件事。
    ` },

    /* ================================================================== */
    { t: 'h', id: 'next-addr', c: '七、下一条微指令的地址从哪来' },

    { t: 'key', id: 'next-addr-ways', title: '后继微地址的五种来源', c: String.raw`
      | 方式 | 怎么来 | 用在哪 |
      |---|---|---|
      | **断定方式**（下地址字段） | ==微指令自己带一个下地址字段，直接指出下一条== | ==最主流==，微程序可以在 CM 里任意存放 |
      | **计数器方式** | $(\mu\texttt{PC})+1$，像 PC 一样自增 | 微程序==必须连续存放==，字长省但不灵活 |
      | **由操作码形成** | $\texttt{OP(IR)}$ 经微地址形成部件译码 | ==取指微程序结束、转入执行微程序的那一刻== |
      | **由标志位分支** | 判别测试字段选中某个标志，==用它去修改下地址的某几位== | 条件转移类微指令 |
      | **由硬件产生** | 固定值 | 开机复位、取指微程序入口、中断微程序入口 |

      **断定方式与计数器方式的取舍**，和主存里"顺序执行 vs 无条件转移"是一回事：

      - 计数器方式==省下地址字段==（微指令短），但==微程序必须连续存放==，
        碎片多、不好安排；
      - 断定方式==每条微指令都背着一个下地址==（字长增加 $\lceil\log_2(\text{CM 单元数})\rceil$ 位），
        换来==任意存放的自由==。

      ==现代微程序控制器普遍用断定方式==，因为 CM 便宜，而灵活性值钱。
    ` },

    { t: 'code', id: 'branch-trick', title: '条件分支怎么做：不是"跳"，是"改地址的某一位"', lang: '',
      note: '这个技巧很巧妙，理解了就明白判别测试字段为什么那么短',
      c: String.raw`
        微指令的下地址字段：  1 0 1 1 0 ?      ← 最低位待定
        判别测试字段：        选中 "ZF"（结果为零标志）

                              ZF = 0  →  下地址 = 101100  → 走"不为零"那条路
                              ZF = 1  →  下地址 = 101101  → 走"为零"那条路

        ★ 两条分支的微指令被【故意】安排在相邻的两个单元里，
          于是"条件转移"退化成"用标志位填上地址的最后一位"——
          不需要比较器，不需要加法器,一根线接过去就行。

        ★ 要做 4 路分支？留 2 位待定，用两个标志去填。
      ` },

    /* ================================================================== */
    { t: 'h', id: 'capacity', c: '八、容量与字长计算（这一节的计算题全在这）' },

    { t: 'key', id: 'formulas', title: '★ 四个公式', c: String.raw`
      $$\textbf{① 微程序个数} = \text{机器指令条数} + \underbrace{1}_{\text{公共取指微程序}}\ (+\ \text{间址}\ +\ \text{中断})$$

      $$\textbf{② 微指令总数} = \sum \text{每段微程序的微指令条数}$$

      $$\textbf{③ 微指令字长} = \underbrace{\text{操作控制字段}}_{\text{发什么命令}}
      + \underbrace{\text{判别测试字段}}_{\text{看哪个标志}}
      + \underbrace{\text{下地址字段}}_{\lceil\log_2(\text{CM 单元数})\rceil}$$

      $$\textbf{④ CM 容量} = \text{微指令总数}\times\text{微指令字长}$$

      **三个最常丢分的细节**：

      1. ==取指微程序只有一段，不是每条指令一段==（它是公共的）。
         算微程序个数时是 $+1$，不是 $\times$。
      2. ==下地址字段的位数由"CM 有多少个单元"决定==，
         而 CM 单元数 = 微指令总数（向上取到 2 的幂）。==这里有个循环依赖，先算微指令总数==。
      3. 题目问"容量至少为多少"，==用实际微指令总数算==；
         问"CM 的寻址空间"，==用 $2^{\text{下地址位数}}$ 算==。两者不一样，看清问的是哪个。
    ` },

    { t: 'example',
      id: 'ex-cm-capacity',
      title: '控制存储器的容量',
      source: '408 常见计算',
      level: 3,
      problem: String.raw`
        某计算机采用微程序控制器，指令系统中共有 $64$ 条机器指令。
        公共的取指微程序由 $3$ 条微指令组成，
        每条机器指令对应的执行微程序平均由 $5$ 条微指令组成。
        微指令采用水平型格式、字段直接编码，操作控制字段占 $18$ 位，
        判别测试字段占 $4$ 位，后继微地址采用断定方式。

        求：**(1)** 微指令总数　**(2)** 下地址字段至少多少位　**(3)** 微指令字长
        　**(4)** 控制存储器的容量
      `,
      idea: String.raw`
        四问是一条链，==必须按顺序做，因为后面一问依赖前面的结果==：

        $$\text{微指令总数}\ \to\ \text{CM 要多少个单元}\ \to\ \text{下地址字段位数}\ \to\ \text{字长}\ \to\ \text{容量}$$

        ==第 (1) 问的坑在"取指微程序是公共的"==：
        它只有一段 $3$ 条，==不是 $64\times 3$==。
        写成算式就是 $3+64\times5$，而不是 $64\times(3+5)$。
      `,
      solution: String.raw`
        **(1) 微指令总数**

        取指微程序==只有一段==（所有指令公用）：$3$ 条
        执行微程序共 $64$ 段，每段平均 $5$ 条：$64\times5=320$ 条

        $$N = 3 + 320 = \boxed{323\ \text{条}}$$

        **(2) 下地址字段位数**

        CM 至少要能寻址 $323$ 个单元：

        $$2^8=256 < 323 \le 512=2^9 \quad\Rightarrow\quad \lceil\log_2 323\rceil=\boxed{9\ \text{位}}$$

        **(3) 微指令字长**

        $$18\ (\text{操作控制}) + 4\ (\text{判别测试}) + 9\ (\text{下地址}) = \boxed{31\ \text{位}}$$

        **(4) 控制存储器容量**

        $$323 \times 31 = \boxed{10013\ \text{位}}$$

        若按 CM 的完整寻址空间（$2^9=512$ 个单元）配置：

        $$512\times31 = 15872\ \text{位} = 1984\ \text{字节} \approx 1.94\ \text{KB}$$
      `,
      comment: String.raw`
        **这道题的三个陷阱，每个都对应一类错误答案**：

        | 错误答案 | 怎么算出来的 | 错在哪 |
        |---|---|---|
        | 微指令总数 $=512$ | $64\times(3+5)$ | ==把公共取指微程序按每条指令算了一遍== |
        | 下地址 $=8$ 位 | $\lceil\log_2 256\rceil$ 或直接 $\log_2 323\approx8.3$ 取整 | ==向下取整了==，$8$ 位只能寻址 $256$ 个单元，装不下 $323$ |
        | 字长忘了判别字段 | $18+9=27$ | ==漏掉判别测试字段==，公式③ 是三段不是两段 |

        **量级自查**：微指令字长通常在 $20\sim100$ 位之间，
        CM 容量通常在几 KB 到几十 KB。==算出来字长几百位或者容量几 MB，一定错了。==

        **换个问法要会转**：

        - "该机采用直接编码，操作控制字段要多少位？"
          $\Rightarrow$ ==等于微命令的总个数==，题目会另给。
        - "改用垂直型微指令，容量怎么变？"
          $\Rightarrow$ ==微指令条数上升、字长下降==，
          总容量变化不大，但==执行速度明显下降==（见[权衡](#/co/cpu/control?at=hv-tradeoff)）。
      `,
    },

    /* ================================================================== */
    { t: 'h', id: 'compare', c: '九、两种控制器的全面对比' },

    { t: 'compare', id: 'final-compare', title: '★★ 硬布线 vs 微程序（背这一张就够）',
      cols: ['', '硬布线控制器', '微程序控制器'],
      rows: [
        ['**别名**', '组合逻辑控制器', '存储逻辑控制器'],
        ['**控制信号怎么来**', '==组合逻辑电路算出来==', '==从控制存储器读出来=='],
        ['**核心部件**', '节拍发生器 + 门电路网络', '控制存储器 CM + $\\mu\\texttt{AR}$ + $\\mu\\texttt{IR}$'],
        ['**速度**', '==快==（只有门延迟）', '==慢==（每个微周期要访问一次 CM）'],
        ['**规整性**', '==不规整==，杂乱的"随机逻辑"', '==规整==，就是一张表'],
        ['**设计难度**', '==难==，每根线都要推表达式并化简', '==容易==，像写程序'],
        ['**修改扩充**', '==极难==，要重新设计并重新布线', '==容易==，改 CM 里的内容即可'],
        ['**增加一条指令**', '几乎要重来一遍', '==加一段微程序=='],
        ['**成本**', '指令系统复杂时成本高', '指令系统复杂时==反而更省=='],
        ['**典型应用**', '==RISC==', '==CISC=='],
      ] },

    { t: 'key', id: 'why-risc-hardwired', title: '★ 为什么 RISC 一定用硬布线（这个因果要讲得出来）', c: String.raw`
      不是"RISC 恰好选了硬布线"，而是==RISC 的三个特征让硬布线变得既可行又必要==：

      **① 指令少、格式规整 $\Rightarrow$ 硬布线不再难写。**
      微程序的最大卖点是"指令系统复杂时也好设计"。
      RISC 指令定长、寻址方式就那么几种，==组合逻辑本来就没几项==，
      微程序的优势直接消失了。

      **② 流水线要求每一段的控制信号==在一拍内产生==。**
      访问一次 CM 就要一个微周期，==这个延迟塞不进流水线的一级==。
      硬布线的门延迟才够快。

      **③ RISC 追求 CPI 接近 1 $\Rightarrow$ 一条指令不能拆成好几个微周期。**
      而微程序天然是"一条机器指令 = 一段微程序 = 多个微周期"，
      ==从定义上就和 CPI=1 矛盾==。

      **反过来，CISC 用微程序的理由同样成立**：
      指令又多又不规整（$\texttt{x86}$ 有的指令要做几十步），
      硬布线的表达式会爆炸，==而微程序只是多存几行表==。
    ` },

    { t: 'warn', id: 'speed-reason', title: '★ "微程序慢"慢在哪，别答错原因', c: String.raw`
      常见的错误回答：==「因为微程序比较长」==。不对。

      正确的原因是：==每执行一条微指令，都要访问一次控制存储器==，
      而一次存储器访问的延迟==远大于==组合逻辑的门延迟。

      $$\text{微程序的一拍} = \text{读 CM 的时间} + \text{发信号} \quad\gg\quad \text{硬布线的一拍} = \text{几级门延迟}$$

      **验证一下这个理解**：如果把 CM 做得和寄存器一样快，微程序还慢吗？
      ==就不慢了== —— 这正是现代 CPU 里微码 ROM 被做得极快、
      并且加了 $\mu$op cache 的原因（见[下一节](#/co/cpu/control?at=modern)）。
    ` },

    /* ================================================================== */
    { t: 'h', id: 'modern', c: '十、略高于考纲：微程序的兴衰与它现在的样子' },

    { t: 'md', c: String.raw`
      下面这些==不会直接考==，但它们能把上面那些干巴巴的对比==变成有因果的故事==，
      而且解释了很多"为什么教材要这么讲"。
    ` },

    { t: 'key', id: 'system360', title: '① System/360：微程序催生了"体系结构"这个概念', c: String.raw`
      1964 年 IBM 要卖一整个系列的机器，从便宜的到昂贵的，
      ==但希望它们能跑同一套软件==。这在当时是不可想象的：
      不同价位的机器硬件差异极大，指令集只能各做各的。

      微程序解决了这个问题：

      - **低端机**：数据通路窄（8 位）、硬件简单，==用很长的微程序去"模拟"复杂指令==；
      - **高端机**：数据通路宽（64 位）、硬件强，==微程序很短，甚至部分改用硬布线==；
      - ==两者对外呈现的指令集完全一样==。

      于是第一次出现了这样的区分：

      $$\underbrace{\text{指令集（程序员看到的）}}_{\textbf{体系结构 architecture}}
      \quad\ne\quad
      \underbrace{\text{怎么实现的（硬件长什么样）}}_{\textbf{实现 implementation}}$$

      ==「计算机体系结构」这个词，就是在 System/360 的设计文档里被定义出来的。==
      微程序是这次分离的技术基础 —— ==它让"同一个接口，多种实现"第一次成为可能==。
    ` },

    { t: 'key', id: 'emulation', title: '② 仿真：改一段微程序，让机器变成另一台机器', c: String.raw`
      既然指令集是微程序"翻译"出来的，==那把微程序换掉，机器不就变成另一台了？==

      IBM 确实这么干了：System/360 通过加载一套不同的微程序，
      ==可以直接执行老式 IBM 1401 的指令==，让客户的旧程序原样跑起来。
      这叫**仿真（emulation）**，是硬件层面的兼容手段。

      这也是**可写控制存储器 WCS** 的由来 ——
      把 CM 做成可写的（RAM 而不是 ROM），
      ==用户就能自己定义指令集==。
      70 年代有过一波"用户可微程序设计"的热潮，后来因为太难用而消退。

      ==注意：408 考试里 CM 一律按 ROM 答==，WCS 只是个课外知识。
    ` },

    { t: 'key', id: 'x86-microcode', title: '③ 微程序没有死：x86 的微码', c: String.raw`
      现代 x86 处理器是==硬布线与微程序的混合体==：

      | 指令类型 | 走哪条路 | 占比 |
      |---|---|---|
      | 简单指令（$\texttt{ADD}$、$\texttt{MOV}$…） | ==硬布线快速译码器==，直接译成 1 个 $\mu$op | ==绝大多数== |
      | 中等复杂指令 | 复杂译码器，译成少数几个 $\mu$op | 少数 |
      | 极复杂 / 罕见指令 | ==微码 ROM（MSROM）==，展开成一长串 $\mu$op | 极少 |

      也就是说，==微程序退化成了"处理复杂与罕见情况的兜底路径"==：
      常见指令走快车道，罕见指令才去查表。
      这是一个非常典型的"==常见情况优先=="的体系结构设计手法。

      **最有意思的一点**：x86 的微码==可以在开机时由 BIOS / 操作系统更新==
      （microcode update）。
      2018 年 Spectre / Meltdown 漏洞的部分缓解措施，
      ==就是通过下发新的微码实现的== ——
      在不换 CPU 的情况下，==改掉了硬件的行为==。

      ==这正是 Wilkes 当年那条"微程序易于修改"的优点，在 60 年后的回响。==
    ` },

    { t: 'key', id: 'pipeline-control', title: '④ 流水线里，"控制器"被拆散了', c: String.raw`
      前面画的控制器都是==集中式==的：一个状态机，管着一条指令从头到尾。
      ==流水线 CPU 里这个东西不存在了。==

      流水线上同时有 5 条指令，各自处在不同阶段，==它们需要的控制信号完全不同==。
      解决办法是：

      > 在 **ID（译码）段**==一次性产生这条指令全程需要的所有控制信号==，
      > 然后让这些信号==跟着指令一起，存进流水寄存器往下流==。

      ~~~
      ID 段产生全部控制信号
            │
            ├─ EX 段要用的 ──▶ 存进 ID/EX 寄存器 ──▶ EX 段取用
            │                        │
            ├─ MEM 段要用的 ─────────┴──▶ 存进 EX/MEM ──▶ MEM 段取用
            │                                  │
            └─ WB 段要用的 ────────────────────┴──▶ 存进 MEM/WB ──▶ WB 段取用
      ~~~

      所以在流水线数据通路图上，==你会看到流水寄存器里除了数据还有一小块控制信号==。
      =="控制器"没有消失，它被拆开分布到了每一段。==

      这也再次说明了[为什么流水线不能用微程序](#/co/cpu/control?at=why-risc-hardwired)：
      控制信号必须在 ID 段==一拍之内==全部产生，没有时间去读 CM。

      流水线本身的时空图、三种冒险与性能计算，见
      [流水线那一页](#/co/cpu/pipeline?at=pipeline-reg)。
    ` },

    /* ================================================================== */
    { t: 'h', id: 'pitfalls', c: '十一、易错清单' },

    { t: 'warn', id: 'pitfall-list', title: '这一节的固定失分点', c: String.raw`
      1. **微命令和微操作分不清** —— ==微命令是信号（因），微操作是动作（果），一一对应==。
      2. **以为一条微指令对应一条机器指令** ——
         ==一条机器指令对应一段微程序（多条微指令）==。
      3. **算微程序个数时把取指微程序乘了指令条数** ——
         ==取指微程序是公共的，只有一段，$+1$ 而不是 $\times$==，
         见[这道题](#/co/cpu/control?at=ex-cm-capacity)。
      4. **字段编码忘了 $+1$** —— 每个字段要留一个"不发命令"的编码，
         $n$ 个互斥命令要 $\lceil\log_2(n+1)\rceil$ 位，见[铁律](#/co/cpu/control?at=mutex-rule)。
      5. **字段位数取最大而不是求和** —— ==多个字段是并排的，要相加==，
         见[2011 真题](#/co/cpu/control?at=ex-field-encode)。
      6. **把相容的微命令编进同一字段** —— ==同一字段里的命令必须互斥==，
         否则它们就没法同时发出了。
      7. **认为 CM 是主存的一部分** —— ==CM 在 CPU 内部，且是 ROM==。
      8. **微指令字长漏掉判别测试字段** —— ==字长 = 操作控制 + 判别测试 + 下地址==。
      9. **答"微程序慢是因为微程序长"** ——
         ==是因为每条微指令都要访问一次 CM==，见[这里](#/co/cpu/control?at=speed-reason)。
      10. **把"节拍"和"微操作"当同一层** ——
          ==一个节拍内可以并行好几个微操作==。
      11. **硬布线"速度快但难修改"记反** ——
          记住因果：==信号是算出来的所以快，电路是焊死的所以难改==。
      12. **忘了 $(\texttt{PC})+1$ 在取指周期的哪一拍** ——
          在 $T_1$，和等主存那段重叠，见[取指序列](#/co/cpu/control?at=fetch-micro)。
    ` },

    { t: 'md', c: String.raw`
      ---

      同一章的邻居：[异常与中断机制](#/co/cpu/exception?at=hidden-def) ——
      那里的**中断隐指令**正是"由硬件（中断周期的微操作序列）自动完成"的一组微操作，
      放在这一页的框架下看会更清楚：==它不过是控制器里的又一段固定信号序列而已==。
    ` },

  ],
});
