/* ==========================================================================
   计算机组成原理 / 5 中央处理器 / 数据通路与单指令执行过程
   —— 这一页是本章的地基：控制器发出的每一个信号，作用的对象都在这里。
      「信号怎么产生」在 co/cpu/control，「被中断打断时怎么办」在 co/cpu/exception。
   ========================================================================== */

KM.page({
  path: 'co/cpu/datapath',
  title: '数据通路与单指令执行过程',
  subtitle: '数据通路是**路**，控制器是**红绿灯**。这一页把路铺清楚，再让一条指令从头到尾走一遍',
  tags: ['高频', '必考', '综合应用', '手算'],
  updated: '2026-08-14',

  blocks: [

    /* ================================================================== */
    { t: 'h', id: 'what', c: '一、数据通路是什么' },

    { t: 'key', id: 'def', title: '定义与分工', c: String.raw`
      **数据通路（Data Path）** = ==数据在各功能部件之间传送的路径==，
      以及路径上那些==对数据进行加工处理的部件==。

      具体包括：ALU、各种寄存器、连接它们的总线或专用连线、多路选择器、三态门。

      **它和控制器的分工，是整章的骨架：**

      | | 数据通路 | 控制器 |
      |---|---|---|
      | 是什么 | ==路==（数据实际流过的地方） | ==红绿灯==（决定哪条路此刻通） |
      | 干什么 | ==执行==：传送、运算 | ==指挥==：在第几拍发哪些信号 |
      | 有没有主动性 | ==完全没有==，不给信号就一动不动 | 有，它是时序的源头 |

      ==数据通路本身决定了"能做什么、最快多快"，控制器决定"什么时候做"。==
      一台机器的性能上限由数据通路划定，控制器只能在这个上限内安排。
    ` },

    { t: 'warn', id: 'bus-vs-bus', title: '内部总线 ≠ 系统总线', c: String.raw`
      这一节说的"单总线结构"里的总线，是==CPU 内部总线==：
      连接 CPU 内部各寄存器与 ALU，==外面看不见==。

      而"总线"那一章讲的是==系统总线==（片外总线）：
      连接 CPU、主存、I/O 接口，就是 ==地址线 / 数据线 / 控制线== 那一套。

      ==两者完全不是一个东西==，唯一的接触点是 $\texttt{MAR}$ 和 $\texttt{MDR}$：

      $$\texttt{MAR}\ \xrightarrow{\ \text{接}\ }\ \text{地址总线},\qquad
      \texttt{MDR}\ \xrightarrow{\ \text{接}\ }\ \text{数据总线}$$

      ==$\texttt{MAR}$ 和 $\texttt{MDR}$ 是 CPU 与外界之间唯一的两个门==，
      所有访存都必须经过它俩。记住这一点，很多题就不会绕晕。
    ` },

    /* ================================================================== */
    { t: 'h', id: 'registers', c: '二、先把 CPU 里的寄存器认全' },

    { t: 'compare', id: 'reg-table', title: '★★ CPU 内部寄存器一览（位数那一列年年考）',
      cols: ['寄存器', '全称', '作用', '位数由什么决定', '用户可见'],
      rows: [
        ['$\\texttt{PC}$', '程序计数器', '存放==下一条==指令的地址', '==存储单元个数==（同 MAR）', '==可见=='],
        ['$\\texttt{IR}$', '指令寄存器', '存放==当前正在执行==的指令', '==指令字长==', '不可见'],
        ['$\\texttt{MAR}$', '存储器地址寄存器', '存放要访问的主存地址', '==存储单元个数== $2^n\\Rightarrow n$ 位', '不可见'],
        ['$\\texttt{MDR}$', '存储器数据寄存器', '存放读出或要写入的数据', '==存储字长==', '不可见'],
        ['$\\texttt{ACC}$ / 通用寄存器', '累加器 / GPR', '存放操作数与运算结果', '机器字长', '==可见=='],
        ['$\\texttt{PSW}$', '程序状态字寄存器', '存放标志位 ZF/CF/OF/SF 等', '——', '==可见=='],
        ['$\\texttt{Y}$、$\\texttt{Z}$', '暂存寄存器', '==单总线结构下 ALU 的输入/输出缓冲==', '机器字长', '不可见'],
        ['$\\texttt{IDR}$ / 暂存器', '数据暂存寄存器', '暂存从主存读来的数据', '存储字长', '不可见'],
      ] },

    { t: 'key', id: 'mar-mdr-bits', title: '★ MAR 与 MDR 的位数：一句话判据', c: String.raw`
      $$\texttt{MAR 位数} = \log_2(\text{存储单元的个数}) = \textbf{地址线的条数}$$
      $$\texttt{MDR 位数} = \textbf{存储字长} = \textbf{数据线的条数}$$

      ==$\texttt{MAR}$ 管"有多少个格子"，$\texttt{MDR}$ 管"一个格子多大"。==

      **算 MAR 的两步**（顺序不能反）：

      1. 先看==按什么编址==（按字节 / 按字），确定==一个存储单元是多大==；
      2. 用总容量除以单元大小，得到==单元个数==，取 $\log_2$。

      **例**：主存容量 $64\ \text{MB}$，==按字节编址==，存储字长 $32$ 位。

      - 单元个数 $=64\ \text{MB}\div 1\ \text{B}=2^{26}$ $\Rightarrow$ ==$\texttt{MAR}=26$ 位==
      - 存储字长 $32$ 位 $\Rightarrow$ ==$\texttt{MDR}=32$ 位==

      **同一台机器改成按字编址**（字长 $32$ 位 $=4$ 字节）：

      - 单元个数 $=64\ \text{MB}\div 4\ \text{B}=2^{24}$ $\Rightarrow$ ==$\texttt{MAR}=24$ 位==，少了 2 位
      - $\texttt{MDR}$ ==不变==，还是 32 位

      ==编址单位一变，MAR 就变，MDR 不变== —— 这是这类题唯一的机关。
    ` },

    { t: 'warn', id: 'visible-regs', title: '用户可见 vs 用户不可见（辨析题）', c: String.raw`
      "用户可见"的准确含义是：==汇编程序员能用指令直接读写它==。

      | 可见 | 不可见 |
      |---|---|
      | 通用寄存器（$\texttt{R0}\sim\texttt{Rn}$、$\texttt{ACC}$） | $\texttt{MAR}$、$\texttt{MDR}$ |
      | $\texttt{PSW}$（标志位可被条件转移指令读、被专门指令写） | $\texttt{IR}$ |
      | $\texttt{PC}$（==转移指令就是在改它==） | 暂存器 $\texttt{Y}$、$\texttt{Z}$ |
      | 基址/变址寄存器 | 微指令寄存器 $\mu\texttt{IR}$ |

      **$\texttt{PC}$ 有争议**：它不能像通用寄存器那样随便读写，
      但转移指令、调用指令确实在修改它。
      ==408 的口径是"用户可见"==（王道明确列在可见一侧），照这个答。

      **一条好用的判据**：==这个寄存器出现在指令的汇编写法里吗？==
      $\texttt{MOV R1, R2}$ 里有 $\texttt{R1}$，所以 $\texttt{R1}$ 可见；
      没有任何一条指令写得出 $\texttt{MAR}$，所以它不可见 ——
      ==它是硬件在执行指令的过程中自动用的中转站==。
    ` },

    /* ================================================================== */
    { t: 'h', id: 'structures', c: '三、数据通路的三种结构' },

    { t: 'compare', id: 'struct-table', title: '三种结构的取舍',
      cols: ['结构', '怎么连', '一拍能传几个数', '性能', '硬件量'],
      rows: [
        ['**CPU 内部单总线**', '所有部件都挂在==一条==公共总线上', '==只能一个==', '==最低==', '==最少=='],
        ['**CPU 内部多总线**', '两条或三条内部总线并行', '多个', '较高', '较多'],
        ['**专用数据通路**', '==部件之间直接连线==，不共享总线', '受连线限制，==并行度最高==', '==最高==', '==最多=='],
      ] },

    { t: 'code', id: 'single-bus-fig', title: '★ 单总线数据通路（后面所有微操作序列都基于这张图）', lang: '',
      c: String.raw`
              ┌─────────────────── CPU 内部总线 ───────────────────┐
              │        │        │        │        │       │       │
           ┌──┴──┐  ┌──┴──┐  ┌──┴──┐  ┌──┴──┐  ┌──┴──┐ ┌──┴──┐    │
           │ PC  │  │ IR  │  │ R0  │  │ R1  │  │ MAR │ │ MDR │    │
           └──┬──┘  └─────┘  └─────┘  └─────┘  └──┬──┘ └──┬──┘    │
              │                                   │       │       │
              │  每个寄存器有两个控制信号：           ▼       ▼       │
              │    Xout：把 X 的值【送上】总线      地址总线  数据总线  │
              │    Xin ：把总线的值【打入】X                          │
              │                                                     │
              │        ┌──────┐                                     │
              └───────▶│  Y   │（暂存器，接 ALU 的 A 端）              │
                       └──┬───┘                                     │
                          ▼                                         │
                     ┌─────────┐                                    │
                     │   ALU   │◀──── B 端直接接总线 ─────────────────┘
                     └────┬────┘
                          ▼
                       ┌──────┐
                       │  Z   │（暂存器，存运算结果）──▶ 回到总线
                       └──────┘

        ★ 一拍之内，总线上【只能有一个】Xout 有效，
          但可以有【多个】Xin 同时有效（一个值同时打入几个寄存器）。
      ` },

    { t: 'key', id: 'why-yz', title: '★★ 为什么单总线里非要有 Y 和 Z（理解这个，单总线就通了）', c: String.raw`
      ALU 是==组合逻辑==，它的两个输入端==必须同时有效==才能算出结果。
      而单总线==一拍只能传一个数==。矛盾就在这里。

      **暂存器 $\texttt{Y}$ 解决"两个输入"的问题：**

      先把第一个操作数送上总线、打入 $\texttt{Y}$（占一拍）；
      下一拍再把第二个操作数送上总线 —— ==此刻 ALU 的 A 端有 $\texttt{Y}$、B 端有总线==，
      两个输入同时有效，可以算了。

      **暂存器 $\texttt{Z}$ 解决"结果往哪放"的问题：**

      ALU 算出结果的这一拍，==总线上还占着第二个操作数==。
      如果让 ALU 的输出直接回到总线，就会==和总线上已有的值打架==
      （而且 ALU 的输入还接着总线，会形成==组合逻辑环路==，电路会震荡）。

      所以结果先落进 $\texttt{Z}$ 暂存，==下一拍==再由 $\texttt{Z}$ 送上总线、写回目标寄存器。

      ~~~
      第 1 拍   R0out, Yin        总线: (R0)  →  Y          ← 存下第一个操作数
      第 2 拍   R1out, ADD, Zin   总线: (R1)  →  ALU 的 B 端
                                  Y ────────▶ ALU 的 A 端
                                  ALU 结果 → Z              ← 总线此刻被 R1 占着，只能落 Z
      第 3 拍   Zout, R0in        总线: (Z)   →  R0          ← 现在总线空了，写回
      ~~~

      ==一次加法要三拍==，这就是单总线"结构简单但效率低"的具体代价。
      ==多总线结构里 $\texttt{Y}$、$\texttt{Z}$ 就可以省掉==，因为两个操作数能同时送到 ALU。
    ` },

    /* ================================================================== */
    { t: 'h', id: 'basic-ops', c: '四、单总线上的三类基本操作' },

    { t: 'steps', id: 'three-ops', title: '所有微操作序列都是这三类的组合', items: [
      { title: String.raw`寄存器之间传送：$(\texttt{R1})\to\texttt{R2}$`,
        c: String.raw`
          ~~~
          R1out, R2in            ← 一拍搞定
          ~~~
          $\texttt{PC}\to\texttt{MAR}$ 也是这一类：$\texttt{PCout, MARin}$。` },
      { title: '主存与 CPU 之间传送：读 / 写',
        c: String.raw`
          **读主存**（把 $\texttt{(MAR)}$ 所指单元的内容取到 $\texttt{MDR}$）：
          ~~~
          第 1 拍   PCout, MARin, Read      ← 地址送 MAR，同时发读命令
          第 2 拍   MDRin                   ← 等主存把数据送上数据总线，打入 MDR
          ~~~
          **写主存**：
          ~~~
          第 1 拍   R1out, MARin
          第 2 拍   R2out, MDRin, Write
          ~~~
          ==访存至少两拍==，而且主存慢时第 2 拍要==等待多个时钟==
          （这就是[联合控制方式](#/co/cpu/control?at=control-mode)里"访存用异步"的由来）。` },
      { title: String.raw`算术逻辑运算：$(\texttt{R1})+(\texttt{R2})\to\texttt{R3}$`,
        c: String.raw`
          ~~~
          第 1 拍   R1out, Yin
          第 2 拍   R2out, ADD, Zin
          第 3 拍   Zout, R3in
          ~~~
          ==固定三拍==，原因见[上面](#/co/cpu/datapath?at=why-yz)。` },
    ] },

    { t: 'warn', id: 'signal-rules', title: '写微操作序列时必须守的三条规矩', c: String.raw`
      1. ==同一拍内只能有一个 $\texttt{Xout}$==（总线上只能有一个源），
         否则叫**总线冲突**，是这类题最常见的扣分点；
      2. ==$\texttt{Xin}$ 可以有多个==（一个值同时打入几个寄存器是允许的，
         比如 $\texttt{PCout, MARin, Yin}$ 合法）；
      3. ==有数据依赖的两步不能合并==：
         $\texttt{MARin}$ 和 $\texttt{MDRin}$ 之间隔着一次真实的主存访问，
         必须分拍，这和[微操作时序三原则](#/co/cpu/control?at=timing-rules)是同一条道理。
    ` },

    /* ================================================================== */
    { t: 'h', id: 'cycles', c: '五、四个机器周期的数据流' },

    { t: 'md', c: String.raw`
      "数据流"是 408 的固定问法：==问的是数据从哪个部件流到哪个部件==，
      不要求写控制信号。四个周期里==前两个和最后一个是固定的，只有执行周期因指令而异==。
    ` },

    { t: 'code', id: 'fetch-flow', title: '① 取指周期的数据流（所有指令都一样）', lang: '',
      c: String.raw`
        PC ──▶ MAR ──▶ 地址总线 ──▶ 主存
                                     │
        控制器发出【读】命令 ──────────┤
                                     ▼
        IR ◀── MDR ◀── 数据总线 ◀── 主存读出的指令

        同时：(PC) + 1 → PC
        最后：OP(IR) → 控制器（译码，决定接下来怎么走）

        微操作序列：
          T0:  PCout, MARin, Read
          T1:  MDRin, PC+1
          T2:  MDRout, IRin
      ` },

    { t: 'code', id: 'ind-flow', title: '② 间址周期的数据流（只有间接寻址才有）', lang: '',
      c: String.raw`
        Ad(IR) ──▶ MAR ──▶ 地址总线 ──▶ 主存
                                        │
        控制器发出【读】命令 ─────────────┤
                                        ▼
        Ad(IR) ◀── MDR ◀── 数据总线 ◀── 读出的【有效地址】

        ★ 间址周期读出来的不是操作数，是【操作数的地址】。
          它被送回 IR 的地址字段（或直接送 MAR），供执行周期使用。

        微操作序列：
          T0:  IRout(Ad), MARin, Read
          T1:  MDRin
          T2:  MDRout, IRin(Ad)      ← 用有效地址覆盖原来的形式地址
      ` },

    { t: 'code', id: 'int-flow', title: '④ 中断周期的数据流（写，不是读）', lang: '',
      note: '这一段就是中断隐指令，详见 co/cpu/exception',
      c: String.raw`
        SP-1 → SP → MAR ──▶ 地址总线 ──▶ 主存
                                          ▲
        控制器发出【写】命令 ───────────────┤
                                          │
        PC ──▶ MDR ──▶ 数据总线 ──────────┘   （把断点写进栈）

        然后：向量地址 → PC        （引出中断服务程序）
              0 → EINT            （关中断）

        ★ 四个周期里【只有中断周期是写主存】，其余都是读。
          这一条能直接判掉一批选择题。
      ` },

    { t: 'key', id: 'exec-flow', title: '③ 执行周期的数据流：唯一没有统一答案的一个', c: String.raw`
      ==执行周期的数据流完全取决于指令==，这正是"指令系统"和"数据通路"的接缝。

      | 指令类型 | 执行周期要不要访存 | 数据流 |
      |---|---|---|
      | **非访存指令**（$\texttt{CLA}$、$\texttt{COM}$、$\texttt{SHR}$） | ==不访存== | 只在 CPU 内部的寄存器与 ALU 之间流动 |
      | **访存指令**（$\texttt{ADD X}$、$\texttt{LDA X}$） | ==要读== | $\texttt{Ad(IR)}\to\texttt{MAR}\to$ 主存 $\to\texttt{MDR}\to$ ALU |
      | **存数指令**（$\texttt{STA X}$） | ==要写== | $\texttt{ACC}\to\texttt{MDR}\to$ 主存 |
      | **转移指令**（$\texttt{JMP X}$） | ==不访存== | $\texttt{Ad(IR)}\to\texttt{PC}$ |
      | **条件转移**（$\texttt{BAN X}$） | 不访存 | ==先看标志位==，满足才 $\texttt{Ad(IR)}\to\texttt{PC}$ |
      | **调用指令**（$\texttt{CALL}$） | ==要写==（压返回地址） | $\texttt{PC}\to\texttt{MDR}\to$ 栈，然后 $\texttt{Ad(IR)}\to\texttt{PC}$ |

      ==注意转移指令的本质：它就是"往 PC 里写一个数"==，
      别把它想复杂。$\texttt{CALL}$ 不过是"写 PC 之前先把老 PC 存起来"，
      这和[中断隐指令保存断点](#/co/cpu/exception?at=hidden-three)是同一套动作 ——
      区别只在于新 PC 从哪来（指令给的 vs 向量表给的）。
    ` },

    /* ================================================================== */
    { t: 'h', id: 'walkthrough', c: '六、完整走一遍：一条指令的一生' },

    { t: 'example',
      id: 'ex-full-trace',
      title: String.raw`写出 $\texttt{ADD R0, X}$ 的完整微操作序列与控制信号`,
      source: '408 大题标准模板',
      level: 4,
      problem: String.raw`
        某单总线 CPU 的数据通路如[前图](#/co/cpu/datapath?at=single-bus-fig)所示。
        指令 $\texttt{ADD R0, X}$ 的功能是 $(\texttt{X})+(\texttt{R0})\to\texttt{R0}$，
        其中 $\texttt{X}$ 为直接地址（存放在指令的地址字段 $\texttt{Ad(IR)}$ 中）。

        写出该指令从==取指到执行结束==的完整微操作序列，并标出每一拍的控制信号。
      `,
      idea: String.raw`
        这类题不要"想"，==要按固定套路拆==：

        $$\underbrace{\text{取指 3 拍}}_{\text{所有指令都一样，背下来}}
        \ +\ \underbrace{\text{（间址）}}_{\text{直接寻址没有}}
        \ +\ \underbrace{\text{执行}}_{\text{取操作数 + 运算 + 写回}}$$

        执行周期再拆成三小段，==每一段的拍数是固定的==：

        - **取操作数**：一次访存 $\Rightarrow$ ==2 拍==（送地址+发命令 / 收数据）；
        - **运算**：单总线的加法 $\Rightarrow$ ==固定 3 拍==（$\to\texttt{Y}$ / 算$\to\texttt{Z}$ / $\texttt{Z}\to$目标），
          原因见[为什么要有 Y 和 Z](#/co/cpu/datapath?at=why-yz)。

        所以答案必然是 $3+2+3=8$ 拍。==先把拍数算出来，再往里填内容，就不会漏步。==
      `,
      solution: String.raw`
        **取指周期**（$\texttt{FE}$）

        | 节拍 | 微操作 | 控制信号 |
        |---|---|---|
        | $T_0$ | $(\texttt{PC})\to\texttt{MAR}$，发读命令 | $\texttt{PCout, MARin, Read}$ |
        | $T_1$ | $\texttt{M(MAR)}\to\texttt{MDR}$，$(\texttt{PC})+1\to\texttt{PC}$ | $\texttt{MDRin, PC+1}$ |
        | $T_2$ | $(\texttt{MDR})\to\texttt{IR}$，$\texttt{OP(IR)}\to$ 控制器 | $\texttt{MDRout, IRin}$ |

        **执行周期**（$\texttt{EX}$）

        | 节拍 | 微操作 | 控制信号 | 在干什么 |
        |---|---|---|---|
        | $T_3$ | $\texttt{Ad(IR)}\to\texttt{MAR}$，发读命令 | $\texttt{IRout(Ad), MARin, Read}$ | 去取 $\texttt{X}$ 单元的操作数 |
        | $T_4$ | $\texttt{M(MAR)}\to\texttt{MDR}$ | $\texttt{MDRin}$ | 操作数到手 |
        | $T_5$ | $(\texttt{R0})\to\texttt{Y}$ | $\texttt{R0out, Yin}$ | ==把第一个操作数存进 Y== |
        | $T_6$ | $(\texttt{Y})+(\texttt{MDR})\to\texttt{Z}$ | $\texttt{MDRout, ADD, Zin}$ | ==两个输入同时有效，做加法== |
        | $T_7$ | $(\texttt{Z})\to\texttt{R0}$ | $\texttt{Zout, R0in}$ | 写回结果 |

        $$\boxed{\text{共 } 8 \text{ 个节拍}}$$
      `,
      comment: String.raw`
        **阅卷最常扣分的四个地方**：

        | 错误 | 为什么错 |
        |---|---|
        | 把 $T_3$ 和 $T_4$ 合并 | ==中间隔着一次真实的主存访问==，数据不可能同拍到达 |
        | 把 $T_5$ 和 $T_6$ 合并 | ==同一拍出现了 $\texttt{R0out}$ 和 $\texttt{MDRout}$ 两个源，总线冲突== |
        | 把 $T_6$ 和 $T_7$ 合并 | $T_6$ 时总线上占着 $\texttt{MDR}$ 的值，==$\texttt{Z}$ 挤不上去== |
        | 忘了 $(\texttt{PC})+1$ | 少了它==下一条指令就取不到了== |

        **换个题干要会改**：

        - **改成间接寻址** $\texttt{ADD R0, @X}$：在 $T_2$ 与 $T_3$ 之间==插入间址周期的 3 拍==
          （见[间址数据流](#/co/cpu/datapath?at=ind-flow)），总共 11 拍。
        - **改成寄存器寻址** $\texttt{ADD R0, R1}$：==不需要访存==，
          执行周期只剩运算的 3 拍，总共 $3+3=6$ 拍。
        - **改成多总线结构**：$T_5\sim T_7$ 可以压缩成 ==1 拍==
          （两个操作数同时送 ALU，结果直接写回），总共 6 拍。

        ==这三个变化正好说明了：指令的执行拍数 = 寻址方式的代价 + 数据通路结构的代价。==
        题目改一个字，拍数就变，所以==必须理解而不能背答案==。
      `,
    },

    /* ================================================================== */
    { t: 'h', id: 'cpu-types', c: '七、单周期 / 多周期 CPU' },

    { t: 'md', c: String.raw`
      前面写的 8 拍序列是**多周期 CPU** 的做法：==一条指令占用多个时钟周期==。
      另一条路是**单周期 CPU**：==硬把一条指令塞进一个时钟周期==。
      这两者的区别，是理解流水线之前必须过的一关。
    ` },

    { t: 'compare', id: 'cpu-type-table', title: '★★ 单周期 vs 多周期',
      cols: ['', '单周期 CPU', '多周期 CPU'],
      rows: [
        ['一条指令用几个时钟', '==恒为 1==', '多个，==不同指令不一样多=='],
        ['时钟周期定多长', '==最长那条指令==的总延迟', '==最长那一步==的延迟'],
        ['时钟周期长度', '==很长==', '==短=='],
        ['CPI', '$=1$', '$>1$'],
        ['功能部件能否复用', '==不能==：一拍内每个部件只能用一次', '==能==：不同拍复用同一个 ALU'],
        ['需要几个存储器', '==指令存储器与数据存储器必须分开==', '可以合并成一个'],
        ['算 PC+4 用什么', '==必须另配一个专用加法器==', '==可以复用 ALU=='],
        ['能不能用单总线', '==不能==（一拍只能传一个数）', '==可以=='],
        ['控制器形态', '纯组合逻辑（只看操作码）', '==状态机==（还要看"第几步"）'],
        ['硬件量', '多', '少'],
      ] },

    { t: 'key', id: 'why-harvard', title: '★ 单周期 CPU 为什么必须把指令和数据分开存（很好的"为什么"题）', c: String.raw`
      单周期的定义是==一条指令在一个时钟周期内全部完成==。
      考虑 $\texttt{LOAD}$ 指令，它在这一个周期里要做两件事：

      1. ==取指令==：读一次存储器；
      2. ==取操作数==：再读一次存储器。

      ==同一个存储器一拍之内不可能读两次==（它只有一套地址线和一套读出电路）。

      所以单周期 CPU ==必须配两个独立的存储器==：
      **指令存储器**和**数据存储器**（这就是**哈佛结构**）。

      **同样的道理还解释了另一件事**：单周期 CPU 里 $\texttt{PC}+4$ ==不能用 ALU 算==，
      因为 ALU 在同一拍里正忙着算这条指令的运算结果或访存地址。
      ==必须另配一个专用加法器==。

      ==这就是"单周期硬件量大"的真正来源==：不是设计者浪费，
      而是"一拍内每个部件只能用一次"这条物理约束==逼着你把部件复制一份==。

      **反过来看多周期**：取指在第 1 拍、取数在第 4 拍，==错开了==，
      所以一个存储器、一个 ALU 就够 —— ==用时间换硬件==。
    ` },

    { t: 'example',
      id: 'ex-clock-calc',
      title: '单周期与多周期的时钟周期和执行时间',
      source: '408 常见计算',
      level: 3,
      problem: String.raw`
        某 CPU 的各功能部件延迟如下：

        | 部件 | 取指（读指存） | 读寄存器 | ALU 运算 | 访问数据存储器 | 写寄存器 |
        |---|---|---|---|---|---|
        | 延迟 | $200\ \text{ps}$ | $100\ \text{ps}$ | $200\ \text{ps}$ | $200\ \text{ps}$ | $100\ \text{ps}$ |

        各类指令需要经过的部件为：

        - $\texttt{LOAD}$：取指 $\to$ 读寄存器 $\to$ ALU $\to$ 访存 $\to$ 写寄存器
        - $\texttt{R}$ 型：取指 $\to$ 读寄存器 $\to$ ALU $\to$ 写寄存器
        - $\texttt{BEQ}$：取指 $\to$ 读寄存器 $\to$ ALU

        **(1)** 单周期实现时，时钟周期至少多长？
        **(2)** 多周期实现时（每个部件占一拍），时钟周期至少多长？
        **(3)** 若指令构成为 $\texttt{LOAD}\ 25\%$、$\texttt{R}$ 型 $60\%$、$\texttt{BEQ}\ 15\%$，
        两种实现的平均指令执行时间各是多少？
      `,
      idea: String.raw`
        (1)(2) 两问的差别，就是[对比表](#/co/cpu/datapath?at=cpu-type-table)第二行那句话：

        - 单周期：==时钟周期 = 最长指令的总延迟==（因为它要在一拍内跑完）；
        - 多周期：==时钟周期 = 最长的单个部件延迟==（因为一拍只做一步）。

        (3) 单周期的平均时间==就是时钟周期本身==（CPI 恒为 1，和指令构成无关）——
        ==这是本题的关键观察==，很多人在这里还去加权平均一遍。
        多周期才需要按 CPI 加权。
      `,
      solution: String.raw`
        **(1) 单周期**

        先算各类指令的总延迟：

        $$\texttt{LOAD}: 200+100+200+200+100 = \mathbf{800}\ \text{ps}$$
        $$\texttt{R 型}: 200+100+200+100 = 600\ \text{ps}$$
        $$\texttt{BEQ}: 200+100+200 = 500\ \text{ps}$$

        时钟周期必须容纳==最长==的那条：

        $$\boxed{T_{\text{单}} = 800\ \text{ps}}$$

        **(2) 多周期**

        一拍只做一步，时钟周期取==单个部件的最大延迟==：

        $$\max\{200,100,200,200,100\} = \boxed{T_{\text{多}} = 200\ \text{ps}}$$

        各类指令的 CPI = 经过的部件数：
        $\texttt{LOAD}$ 为 $5$，$\texttt{R}$ 型为 $4$，$\texttt{BEQ}$ 为 $3$。

        **(3) 平均执行时间**

        **单周期**：$\text{CPI}=1$，==与指令构成无关==

        $$T_{\text{avg,单}} = 1\times 800 = \boxed{800\ \text{ps}}$$

        **多周期**：先算平均 CPI

        $$\overline{\text{CPI}} = 0.25\times5 + 0.60\times4 + 0.15\times3 = 1.25+2.40+0.45 = 4.1$$

        $$T_{\text{avg,多}} = 4.1 \times 200 = \boxed{820\ \text{ps}}$$

        **结论**：这组数据下 ==多周期反而略慢==（$820 > 800$）。
      `,
      comment: String.raw`
        **这道题最值钱的是那个反直觉的结论**：多周期==不一定==比单周期快。

        原因很清楚：多周期的时钟周期是 $200\ \text{ps}$，
        而 $\texttt{R}$ 型指令真正需要的只有 $100\ \text{ps}$ 的"读寄存器"和"写寄存器"两步，
        ==每一步都被凑成 $200$，浪费了一半==。
        单周期浪费在"短指令陪长指令等"，多周期浪费在"短步骤陪长步骤等"，
        ==两种浪费谁大，取决于具体数据==。

        **那多周期的真正好处是什么？**

        | 好处 | 说明 |
        |---|---|
        | ==硬件省== | 部件可复用，一个 ALU、一个存储器就够 |
        | ==为流水线铺路== | 指令已经被切成了阶段，==只差让不同指令的阶段重叠== |

        ==第二条才是历史意义所在==：多周期本身未必更快，
        但它把指令切成了 $5$ 段，于是流水线可以让 $5$ 条指令同时在跑，
        吞吐率变成 $5$ 倍。==多周期是通往流水线的必经之路==，
        接下去就是[流水线的时空图与三个性能指标](#/co/cpu/pipeline?at=overlap)。

        **量级自查**：单周期时钟周期必然==等于最长指令的延迟==，
        多周期必然==等于最长部件的延迟==，前者一定大于后者。
        算出来反过来了，一定是把两个"最长"搞混了。
      `,
    },

    /* ================================================================== */
    { t: 'h', id: 'advanced', c: '八、略高于考纲：数据通路设计里真正的约束' },

    { t: 'key', id: 'critical-path', title: '① 时钟周期是被"关键路径"卡住的', c: String.raw`
      上面那道题里"取最长"的动作，正式名字叫==找关键路径（critical path）==：

      > 时钟周期 $\ge$ ==从一个寄存器的输出，经过组合逻辑，到下一个寄存器输入==
      > 这条路上==最长==的那一条的延迟。

      更完整的约束还要加上寄存器本身的开销：

      $$T_{\text{clk}} \ge t_{\text{clk-to-q}} + t_{\text{组合逻辑}} + t_{\text{setup}}$$

      ==这解释了流水线为什么不能无限分下去==：
      每多切一级，就多付一次 $t_{\text{clk-to-q}}+t_{\text{setup}}$ 的固定开销。
      当流水段短到和寄存器开销同一量级时，==再切就没有收益了==。

      奔腾 4 曾把流水线做到 31 级去追高主频，==结果就撞在这堵墙上==，
      后来的架构反而把级数降了回来。
    ` },

    { t: 'key', id: 'regfile-ports', title: '② 寄存器堆为什么是"两读一写"', c: String.raw`
      现代 CPU 的通用寄存器不是一堆独立的寄存器，而是一块==寄存器堆（register file）==，
      本质是一小块多端口 SRAM。

      标准配置是==2 个读端口 + 1 个写端口==，因为典型的三地址指令

      $$\texttt{ADD R3, R1, R2}\qquad (\texttt{R1})+(\texttt{R2})\to\texttt{R3}$$

      ==恰好要同时读两个、写一个==。

      **这和单总线的 $\texttt{Y}$、$\texttt{Z}$ 是同一个问题的两种答案**：

      | 方案 | 怎么解决"ALU 要两个输入" | 代价 |
      |---|---|---|
      | 单总线 + $\texttt{Y}$、$\texttt{Z}$ | ==用时间==：分三拍轮流送 | 慢 |
      | 多端口寄存器堆 | ==用硬件==：一拍同时读出两个 | 端口多、面积大、功耗高 |

      ==端口是很贵的东西==：每加一个读端口，寄存器堆的面积大致按端口数平方增长。
      这就是为什么超标量 CPU（一拍要发射多条指令）的寄存器堆==是芯片上最难设计的部件之一==。
    ` },

    { t: 'key', id: 'forwarding-preview', title: '③ 转发：在数据通路上"抄近路"', c: String.raw`
      流水线里有个经典问题：

      ~~~
      ADD R1, R2, R3     ← 结果要到【写回段】才进 R1
      SUB R4, R1, R5     ← 但它在【执行段】就要用 R1
      ~~~

      按正常路径，$\texttt{SUB}$ 得等 $\texttt{ADD}$ 写回才能读到 —— 要停好几拍。

      **解决办法不是改控制器，而是==在数据通路上多接一根线==**：
      把 ALU 的输出==直接接回 ALU 的输入端==，
      结果还没写进寄存器就先"抄近路"送过去。这叫**转发 / 旁路（forwarding / bypassing）**。

      ==这件事的意义在于它说明了一个普遍规律==：

      > 很多性能问题的解法，==不是让控制器更聪明，而是让数据通路多一条路==。

      Cache、写缓冲、转发、分支目标缓冲，==全都是"加一条路"的产物==。
      这也是为什么说==数据通路划定了性能的上限==，
      控制器只能在这个上限内调度。

      转发的完整机制、两条转发通路，以及==它救不回来的那一种情况==，
      见[数据冒险与转发](#/co/cpu/pipeline?at=forwarding)。
    ` },

    { t: 'key', id: 'tristate', title: String.raw`④ $\texttt{Xout}$ 到底是个什么电路`, c: String.raw`
      教材里的 $\texttt{R0out}$ 画成一个方框，实际实现是==三态门（tri-state buffer）==：

      | 控制端 | 输出 | 含义 |
      |---|---|---|
      | $\texttt{R0out}=1$ | 把 $\texttt{R0}$ 的值驱动到总线上 | ==接通== |
      | $\texttt{R0out}=0$ | ==高阻态 Z==（相当于断开） | ==悬空==，不影响总线 |

      =="高阻态"这第三种状态，正是总线能被多个部件共享的物理基础==：
      没被选中的部件全部处于高阻，==就像根本不存在一样==。

      ==这也解释了为什么"一拍内只能有一个 $\texttt{Xout}$"==：
      两个三态门同时驱动总线，一个输出高电平一个输出低电平，
      ==就是一条从电源到地的低阻通路==，
      轻则电平不确定，重则烧毁芯片。这在硬件上叫**总线争用（bus contention）**。

      ==所以"总线冲突"不是一个抽象的规则，是一个真会冒烟的物理事故。==
    ` },

    /* ================================================================== */
    { t: 'h', id: 'pitfalls', c: '九、易错清单' },

    { t: 'warn', id: 'pitfall-list', title: '这一节的固定失分点', c: String.raw`
      1. **$\texttt{MAR}$ 和 $\texttt{MDR}$ 的位数搞反** ——
         ==$\texttt{MAR}$ 看单元个数，$\texttt{MDR}$ 看存储字长==，
         见[判据](#/co/cpu/datapath?at=mar-mdr-bits)。
      2. **算 $\texttt{MAR}$ 时忘了看编址单位** ——
         ==按字节编址和按字编址，单元个数差一个字长==。
      3. **同一拍写了两个 $\texttt{Xout}$** —— ==总线冲突==，
         这是大题最常见的扣分点，见[三条规矩](#/co/cpu/datapath?at=signal-rules)。
      4. **把访存的两拍合并成一拍** —— ==中间隔着真实的主存访问==。
      5. **忘了 $(\texttt{PC})+1$** —— 取指周期 $T_1$ 那一步，漏了就取不到下一条指令。
      6. **认为暂存器 $\texttt{Y}$、$\texttt{Z}$ 是必需的** ——
         ==只有单总线结构才需要==，多总线可以省掉，见[原因](#/co/cpu/datapath?at=why-yz)。
      7. **认为 $\texttt{MAR}$、$\texttt{MDR}$ 用户可见** —— ==不可见==，
         没有任何指令能直接写它们。
      8. **认为单周期 CPU 一定比多周期快** ——
         ==不一定==，见[这道计算题](#/co/cpu/datapath?at=ex-clock-calc)。
      9. **认为单周期 CPU 可以用单总线** ——
         ==不能==，单总线一拍只传一个数，凑不出一条指令的全部动作。
      10. **单周期 CPU 用 ALU 算 $\texttt{PC}+4$** ——
          ==ALU 那一拍正忙着，必须另配加法器==，见[为什么](#/co/cpu/datapath?at=why-harvard)。
      11. **把 CPU 内部总线和系统总线混为一谈** ——
          ==接口只有 $\texttt{MAR}$ 和 $\texttt{MDR}$ 两个==。
      12. **说执行周期的数据流是固定的** ——
          ==只有执行周期因指令而异==，其余三个周期都是固定的。
    ` },

    { t: 'md', c: String.raw`
      ---

      往下一步：这些微操作序列==由谁产生==，就是
      [控制器（硬布线 / 微程序）](#/co/cpu/control?at=the-whole-point) 那一页的内容；
      这条指令流被打断时会发生什么，见
      [异常与中断机制](#/co/cpu/exception?at=hidden-def)。
    ` },

  ],
});
