/* ==========================================================================
   计算机组成原理 / 6 总线 / 总线结构与性能指标
   —— 总线是"共享的连线"，它的所有优点和所有麻烦都来自"共享"这两个字。
      仲裁与定时（怎么分配这个共享资源、怎么在上面对时）在隔壁 bus-timing。
   ========================================================================== */

KM.page({
  path: 'co/bus/bus-basic',
  title: '总线结构与性能指标',
  subtitle: '总线用**共享**换掉了成堆的连线，代价是**同一时刻只能有一对部件说话**。这一页讲结构与带宽计算',
  tags: ['高频', '必考', '手算'],
  updated: '2026-08-14',

  blocks: [

    /* ================================================================== */
    { t: 'h', id: 'why', c: '一、为什么需要总线' },

    { t: 'code', id: 'point-to-point', title: '不用总线会怎样：连线数按平方爆炸', lang: '',
      c: String.raw`
        分散连接（每两个部件之间拉一条专线）
        ─────────────────────────────────────
             CPU ──────── 主存
              │  ╲      ╱  │
              │    ╲  ╱    │
              │     ╳      │
              │    ╱  ╲    │
             磁盘 ──────── 打印机

        n 个部件要 C(n,2) = n(n-1)/2 条连线：
             n = 4   →   6 条
             n = 10  →  45 条
             n = 20  → 190 条        ← 按 n² 增长，很快就走不通

        总线连接（所有部件挂在同一组公共线上）
        ─────────────────────────────────────
          CPU     主存     磁盘    打印机
           │       │       │       │
        ═══╧═══════╧═══════╧═══════╧═══  总线

        n 个部件只要 1 组线，增加一个部件只是【多挂一个】。
      ` },

    { t: 'key', id: 'tradeoff', title: '★ 总线的核心权衡（这一句能推出后面所有内容）', c: String.raw`
      $$\text{总线}=\textbf{用"分时共享"换掉了"连线数量"}$$

      **得到的**：连线数从 $O(n^2)$ 降到 $O(1)$；
      部件可以==标准化、模块化、随时插拔==（这才是 PC 能装显卡、插内存条的前提）。

      **付出的**：==同一时刻只能有一对部件通信==。
      于是立刻派生出这一章的全部问题：

      | 派生问题 | 在哪解决 |
      |---|---|
      | 多个部件同时想用总线，听谁的？ | ==总线仲裁==（[隔壁那页](#/co/bus/bus-timing?at=arbitration)） |
      | 发出去的信号，对方什么时候算收到？ | ==总线定时==（[同页](#/co/bus/bus-timing?at=timing)） |
      | 共享一条线，会不会成为瓶颈？ | ==总线带宽==（[本页](#/co/bus/bus-basic?at=bandwidth)），以及多总线结构 |

      ==记住"共享"是因，其余全是果==，这一章就不会散。
    ` },

    { t: 'key', id: 'features', title: '总线的四种特性（选择题偶尔考名词）', c: String.raw`
      | 特性 | 规定什么 | 例子 |
      |---|---|---|
      | **机械特性** | ==尺寸、形状、引脚数、排列顺序== | 插槽多长、多少个金手指 |
      | **电气特性** | ==信号传递方向、有效电平范围== | 高电平 $3.3\ \text{V}$，低电平 $0\ \text{V}$ |
      | **功能特性** | ==每根线的功能是什么== | 这根是地址线、那根是读命令 |
      | **时间特性** | ==信号在什么时刻有效==（时序关系） | 地址必须在命令前 $10\ \text{ns}$ 稳定 |

      **一个好记的顺序**：==插得进去（机械）→ 电压对得上（电气）→
      知道每根线干嘛（功能）→ 知道什么时候读它（时间）==。
      这四条全都规定死，不同厂商的部件才能互换 —— ==这就是"总线标准"的含义==。
    ` },

    /* ================================================================== */
    { t: 'h', id: 'classify', c: '二、总线的分类' },

    { t: 'compare', id: 'classify-table', title: '四个分类维度（别混着记）',
      cols: ['分类依据', '类型', '说明'],
      rows: [
        ['**连接部件**', '==片内总线==', 'CPU 芯片==内部==，连接寄存器与 ALU（就是[数据通路那条内部总线](#/co/cpu/datapath?at=bus-vs-bus)）'],
        ['', '==系统总线==', '连接 CPU、主存、I/O 接口，==本章主角=='],
        ['', '==通信总线==', '连接==计算机与计算机==、或与外部设备（USB、以太网）'],
        ['**传输格式**', '并行总线 / 串行总线', '多位同时传 / 一位一位传（[见下](#/co/bus/bus-basic?at=serial-win)）'],
        ['**时序控制**', '同步总线 / 异步总线', '有无统一时钟（[见 bus-timing](#/co/bus/bus-timing?at=timing)）'],
        ['**功能（系统总线内部）**', '地址总线 / 数据总线 / 控制总线', '见下表'],
      ] },

    { t: 'key', id: 'three-groups', title: '★ 系统总线的三组线（位数关系是考点）', c: String.raw`
      | 总线 | 传什么 | 方向 | 位数由什么决定 |
      |---|---|---|---|
      | **地址总线 AB** | 访问哪个单元 / 哪个 I/O 端口 | ==单向==（CPU 或主设备发出） | ==主存地址空间的大小== |
      | **数据总线 DB** | 真正要传的数据 | ==双向== | ==机器字长 / 存储字长== |
      | **控制总线 CB** | 读/写命令、时钟、中断请求、总线请求… | ==每根线各自单向，整体双向== | 由控制信号的种类决定 |

      **地址总线位数与寻址范围**：

      $$n\ \text{根地址线}\ \Longleftrightarrow\ 2^n\ \text{个可寻址单元}$$

      ==注意"单元"的大小取决于按什么编址==，
      这和 [$\texttt{MAR}$ 位数的算法](#/co/cpu/datapath?at=mar-mdr-bits)是同一回事：
      $32$ 根地址线、==按字节编址==时寻址范围是 $2^{32}\ \text{B}=4\ \text{GB}$。

      **和 CPU 内部的对应关系**：

      $$\texttt{MAR}\ \longrightarrow\ \text{地址总线},\qquad
      \texttt{MDR}\ \longleftrightarrow\ \text{数据总线}$$

      ==地址总线单向、数据总线双向==，这条判断题出过好几次。
      理由很直白：==没有哪个设备需要"把地址送给 CPU"==，
      但数据既要读进来也要写出去。
    ` },

    { t: 'warn', id: 'multiplex', title: '总线复用：省线，但慢', c: String.raw`
      **总线复用**指==地址线和数据线共用同一组物理线路，分时使用==：
      先在这组线上送地址，锁存之后再在同一组线上送数据。

      | | 好处 | 坏处 |
      |---|---|---|
      | 复用 | ==线数减半==，引脚少、成本低、体积小 | ==一次传输要分两拍==，速度下降 |

      ==算带宽时要小心==：题目说"地址/数据线复用，共 $32$ 根"，
      那么==数据宽度是 $32$ 位==（不是 $16$ 位），
      只是每次传输前要多花时间送地址。
      [这道真题](#/co/bus/bus-basic?at=ex-bandwidth)考的就是这个。
    ` },

    /* ================================================================== */
    { t: 'h', id: 'structures', c: '三、总线结构的演变' },

    { t: 'code', id: 'one-bus', title: '① 单总线结构', lang: '',
      c: String.raw`
          CPU        主存        I/O接口1     I/O接口2
           │          │            │            │
        ═══╧══════════╧════════════╧════════════╧═══  系统总线（一条）

        优点：结构简单、成本低、【易于扩充】
        缺点：★ 同一时刻只允许一对部件通信
              CPU 访存时，I/O 就不能传输 ──▶ 总线成为瓶颈
              且主存与外设【速度差异巨大】，慢设备会拖累整条总线
      ` },

    { t: 'code', id: 'two-bus', title: '② 双总线结构：把主存单独拉出来', lang: '',
      c: String.raw`
                    ┌──────────┐
          主存 ══════╡   CPU    ╞══════ 通道 / IOP
                    └──────────┘         │
            ↑                            │
        主存总线（快）                  I/O总线（慢）
                                         │
                                  ┌──────┴──────┬──────────┐
                                 磁盘          打印机      终端

        ★ 核心思想：把【高速的 CPU-主存 通路】和【低速的 I/O 通路】分开，
          不让慢设备拖累快通路。
        ★ 通道（IOP）是一个【专用处理器】，替 CPU 管理 I/O，
          CPU 只需下达一条 I/O 指令。
      ` },

    { t: 'code', id: 'three-bus', title: '③ 三总线结构：再给高速外设开一条直通主存的路', lang: '',
      c: String.raw`
                       主存总线
          CPU ═══════════╤═══════════ 主存
                         │                ▲
                     I/O总线              ║ DMA总线
                         │                ║
              ┌──────────┴───┐       ┌────╨─────┐
            打印机        终端       高速外设(磁盘)

        ★ DMA 总线让高速外设【绕过 CPU，直接和主存交换数据】
          —— 这正是 DMA 方式的物理基础，见 co/io/interrupt 的中断与 DMA 对比。
        ★ 三条总线【同一时刻只有一条能占用主存】，但彼此的传输可以重叠。
      ` },

    { t: 'compare', id: 'struct-compare', title: '三种结构对比',
      cols: ['', '单总线', '双总线', '三总线'],
      rows: [
        ['CPU 是否参与所有传输', '==是==', '否（通道分担 I/O）', '否'],
        ['并行度', '==最低==', '中', '==最高=='],
        ['成本 / 复杂度', '==最低==', '中', '==最高=='],
        ['主要解决的问题', '——', '==隔开快慢设备==', '==让高速外设直通主存=='],
      ] },

    { t: 'key', id: 'modern-bus', title: '略高一点：现代 PC 的总线组织是怎么走到今天的', c: String.raw`
      教材停在"三总线"，实际的演化又走了三步：

      **① 南北桥时代**（约 1995–2008）

      ~~~
      CPU ──前端总线FSB── 北桥 ──── 内存
                          │  └──── 显卡(AGP/PCIe)
                          │
                         南桥 ──── 硬盘、USB、网卡、声卡
      ~~~
      ==北桥接快的（内存、显卡），南桥接慢的==——
      这就是三总线"快慢分离"思想的工业版本。

      **② 内存控制器进 CPU**（约 2008 起）

      ==北桥最重要的功能"内存控制器"被搬进了 CPU 芯片==，
      于是 CPU 直连内存，前端总线消失，延迟大幅下降。
      剩下的北桥功能并进 CPU，南桥改名 PCH，用 DMI 链路连过来。

      **③ 点对点取代共享总线**

      ==这一步最关键==：PCIe 不再是"大家挂在一条线上"的共享总线，
      而是==每个设备一条独立的点对点链路==，通过交换芯片互联。

      ==于是"总线"这个词名存实亡==：
      既然每条链路只有两端，就==不再需要仲裁==，也不存在"谁占用总线"的问题。
      这一章讲的[仲裁方式](#/co/bus/bus-timing?at=arbitration)，
      在现代高速互连里==已经基本退出历史舞台==
      （详见 [bus-timing 的最后一节](#/co/bus/bus-timing?at=modern)）。
    ` },

    /* ================================================================== */
    { t: 'h', id: 'bandwidth', c: '四、性能指标与带宽计算' },

    { t: 'key', id: 'metrics', title: '★★ 概念先分清，再套公式', c: String.raw`
      | 指标 | 含义 | 注意 |
      |---|---|---|
      | **总线宽度** | ==数据线的根数==（位） | 算带宽时要 $\div 8$ 换成字节 |
      | **总线时钟频率** | 总线的工作时钟，$f_{\text{clk}}$ | ==不一定等于工作频率== |
      | **总线工作频率** | ==每秒钟传送数据的次数== | ==这个才是带宽公式里用的== |
      | **总线传输周期** | 完成==一次完整数据传送==所需时间 | 可能含申请、寻址、传数、结束多个阶段 |
      | **总线带宽** | ==每秒传输的字节数==（$\text{B/s}$） | 又叫总线数据传输率 |

      **核心公式**：

      $$\boxed{\ \text{总线带宽} = \frac{\text{总线宽度（位）}}{8}\times\text{总线工作频率}\ }$$

      **工作频率与时钟频率的换算 —— 这是全部难点所在**：

      | 题目怎么说 | 工作频率 |
      |---|---|
      | 每个时钟周期传送一次 | $f_{\text{工作}}=f_{\text{clk}}$ |
      | ==每个时钟周期传送两次==（上升沿+下降沿，即 DDR） | $f_{\text{工作}}=\mathbf{2}f_{\text{clk}}$ |
      | ==每 $k$ 个时钟周期传送一次== | $f_{\text{工作}}=f_{\text{clk}}/k$ |

      ==读题时先把"多少个时钟传几次"这句话找出来==，
      它决定了要乘 2 还是除以 $k$。==这一步错了，整题全错。==
    ` },

    { t: 'example',
      id: 'ex-bandwidth',
      title: '地址/数据复用 + 双沿传输的带宽',
      source: '真题 / 高频',
      level: 2,
      problem: String.raw`
        某同步总线采用==地址线和数据线复用==方式，其中==地址/数据线有 32 根==，
        总线时钟频率为 $66\ \text{MHz}$，
        ==每个时钟周期传送两个数据==（上升沿和下降沿各传送一个数据）。

        则该总线的数据传输率是（　）。

        **A.** $132\ \text{MB/s}$　　**B.** $264\ \text{MB/s}$
        　**C.** $528\ \text{MB/s}$　　**D.** $1056\ \text{MB/s}$
      `,
      idea: String.raw`
        题干里有两个词专门用来干扰：

        1. =="地址/数据线复用"== —— 它==不影响总线宽度==！
           复用的意思是这 32 根线==有时候用来送地址、有时候用来送数据==，
           但送数据的时候==32 根全都在送数据==，所以宽度就是 $32$ 位。
           ==很多人在这里除以 2，得到 A 或 B。==
        2. =="每个时钟周期传送两个数据"== —— 这是 DDR，
           ==工作频率是时钟频率的 2 倍==，要乘 $2$。

        把这两点定下来，剩下就是一次乘法。
      `,
      solution: String.raw`
        **总线宽度**：$32$ 位 $=4$ 字节（复用不改变宽度）

        **总线工作频率**：

        $$f_{\text{工作}} = 2\times f_{\text{clk}} = 2\times 66\ \text{MHz} = 132\ \text{MHz}$$

        **带宽**：

        $$\frac{32}{8}\times 132\times10^{6}
        = 4\times132\times10^{6}
        = 528\times10^{6}\ \text{B/s}$$

        $$\boxed{528\ \text{MB/s}\quad\text{选 C}}$$
      `,
      comment: String.raw`
        **四个选项各自对应一个错误**：

        | 选项 | 怎么算出来的 | 错在哪 |
        |---|---|---|
        | A. $132$ | $\frac{32}{8}\times\frac{66}{2}$ 或 $2\times66$ | ==把复用理解成"宽度减半"，又漏了双沿== |
        | B. $264$ | $\frac{32}{8}\times 66$ | ==漏了双沿==（忘记乘 2） |
        | **C. $528$** ✅ | $\frac{32}{8}\times 66\times 2$ | —— |
        | D. $1056$ | $\frac{32}{8}\times 66\times 2\times 2$ | ==乘了两次 2== |

        ==B 是最容易掉进去的==，因为"每个时钟周期传送两个数据"这句话
        很容易被当成背景描述读过去。

        **换个问法要会转**：

        - "若不采用双沿传输，带宽是多少？" $\Rightarrow 264\ \text{MB/s}$
        - "若总线宽度改为 $64$ 位？" $\Rightarrow 8\times132\times10^6=1056\ \text{MB/s}$
        - "若每 $2$ 个时钟周期才传一个数据？" $\Rightarrow 4\times33\times10^6=132\ \text{MB/s}$

        **一句话记住**：==复用管的是"线怎么用"，不管"一次传多宽"。==
      `,
    },

    { t: 'example',
      id: 'ex-burst',
      title: '一次总线事务含多个阶段时的实际带宽',
      source: '408 常见计算',
      level: 3,
      problem: String.raw`
        某总线时钟频率为 $100\ \text{MHz}$，总线宽度 $32$ 位。
        一次总线事务采用==突发（猝发）传输==方式：
        先用 $1$ 个时钟周期发送地址，==然后连续传送 $4$ 个 $32$ 位数据==，
        每个数据占 $1$ 个时钟周期。

        求：**(1)** 一次总线事务的时间　**(2)** 该总线的==实际==数据传输率
        　**(3)** 若改为每次只传 $1$ 个数据（非突发），实际传输率是多少
      `,
      idea: String.raw`
        ==这题问的是"实际"传输率，不是峰值==，两者的区别在于
        ==要不要把"发地址"那些不传数据的周期算进分母==。

        $$\text{实际传输率} = \frac{\textbf{有效数据量}}{\textbf{总时间（含开销）}}$$

        突发传输的意义就在这里：==一次地址开销，摊给 4 个数据==。
        第 (3) 问是同一个开销只摊给 1 个数据，==对比一下就知道突发为什么值钱==。
      `,
      solution: String.raw`
        时钟周期 $T=\dfrac{1}{100\ \text{MHz}}=10\ \text{ns}$

        **(1) 一次总线事务的时间**

        $$1\ (\text{地址}) + 4\ (\text{数据}) = 5\ \text{个时钟周期} = 5\times10 = \boxed{50\ \text{ns}}$$

        **(2) 突发传输的实际传输率**

        一次事务传送有效数据：$4\times32\ \text{位}=4\times4=16\ \text{B}$

        $$\frac{16\ \text{B}}{50\ \text{ns}} = \frac{16}{50\times10^{-9}}
        = 0.32\times10^{9} = \boxed{320\ \text{MB/s}}$$

        **(3) 非突发（每次传 1 个数据）**

        一次事务：$1+1=2$ 个时钟周期 $=20\ \text{ns}$，传送 $4\ \text{B}$

        $$\frac{4\ \text{B}}{20\ \text{ns}} = 0.2\times10^{9} = \boxed{200\ \text{MB/s}}$$
      `,
      comment: String.raw`
        **对照一下三个数字，突发传输的价值一目了然**：

        | 口径 | 传输率 | 说明 |
        |---|---|---|
        | ==峰值带宽== | $400\ \text{MB/s}$ | $4\ \text{B}\times100\ \text{MHz}$，==假设每拍都在传数据== |
        | 突发（4 个数据） | $320\ \text{MB/s}$ | 达到峰值的 $80\%$ |
        | 非突发 | $200\ \text{MB/s}$ | ==只有峰值的 $50\%$==，一半时间在发地址 |

        ==峰值带宽是买不到的==，它只是"数据线满负荷时"的理论上限。
        实际传输率永远低于它，差距就是==协议开销==。

        **突发长度再加长会怎样**：传 $8$ 个数据时
        $\frac{32\ \text{B}}{9\times10\ \text{ns}}\approx355\ \text{MB/s}$，
        ==越来越逼近峰值但永远达不到==（地址那一拍消不掉）。

        这正是==局部性原理==在总线上的体现 ——
        ==既然要付一次寻址的钱，就多搬一点走==。
        Cache 按"块"而不是按"字"从主存取数据，用的是同一个道理。
      `,
    },

    /* ================================================================== */
    { t: 'h', id: 'serial-win', c: '五、略高于考纲：为什么高速总线全都改成了串行' },

    { t: 'md', c: String.raw`
      教材讲"并行总线比串行快"，这在低速时代是对的。
      但今天所有高速接口 —— ==PCIe、SATA、USB、以太网 —— 全是串行的==。
      这个反转值得讲清楚，它是理解现代计算机互连的钥匙。
    ` },

    { t: 'key', id: 'skew', title: '★ 并行总线的死穴：信号偏斜', c: String.raw`
      $32$ 位并行总线意味着 $32$ 根线上的信号==必须同时到达==。
      但每根线的长度、阻抗、寄生电容都有微小差异，于是产生**偏斜（skew）**：

      ~~~
      理想                          实际
      线0  ──┐                     线0  ──┐
      线1  ──┤ 同时到达             线1  ───┐      ← 晚了一点
      线2  ──┤                     线2  ─┐        ← 早了一点
      线31 ──┘                     线31 ────┐     ← 更晚

                                   接收端必须【等最慢的那一根】
                                   ★ 时钟周期 ≥ 最大偏斜 + 建立时间
      ~~~

      ==频率越高，时钟周期越短，而偏斜几乎不随频率下降==。
      当偏斜接近一个时钟周期时，==并行总线就再也提不上去了==。

      **雪上加霜的还有两条**：

      - ==串扰（crosstalk）==：相邻线之间的电磁耦合，频率越高越严重；
      - ==成本与体积==：64 根线的排线又贵又粗，还要占大量芯片引脚。
    ` },

    { t: 'key', id: 'serial-fix', title: '串行怎么绕开这一切', c: String.raw`
      **① 只有一对线，就不存在"线之间"的偏斜。**
      这是最根本的一条 —— ==问题不是被解决了，是被消灭了==。

      **② 时钟嵌入数据流（嵌入式时钟）。**
      不再单独拉一根时钟线（那根线自己也会偏斜），
      而是把时钟信息==编码进数据里==，接收端用锁相环从数据流中==恢复==时钟。
      $\Rightarrow$ ==彻底告别时钟与数据之间的偏斜==。

      **③ 差分信号。**
      一对线传相反的电平，接收端只看两者的==差值==。
      共模噪声同时作用在两根线上，==相减就抵消了== $\Rightarrow$ 抗干扰强，可以把电压做低、频率做高。

      **④ 要更大带宽？多路串行并联（lane）。**

      ~~~
      PCIe x1  ──▶ 1 条 lane
      PCIe x4  ──▶ 4 条 lane   ← 每条独立，各自恢复时钟
      PCIe x16 ──▶ 16 条 lane     偏斜由协议层重新对齐，不要求物理上同时到达
      ~~~

      ==这就是显卡插槽写着 x16 的含义==。
      它看起来像"并行"，但==每条 lane 都是独立的串行链路==，
      对齐在协议层做，==没有物理层的偏斜约束==。
    ` },

    { t: 'key', id: 'encoding-overhead', title: '★ 一个容易被忽略的量：编码开销', c: String.raw`
      串行链路要靠数据流本身携带时钟，就==不能出现太长的连续 0 或连续 1==
      （否则接收端锁不住时钟）。解决办法是==线路编码==，代价是额外的位。

      | 标准 | 每 lane 速率 | 编码 | ==有效==数据率 / lane |
      |---|---|---|---|
      | PCIe 1.0 | $2.5\ \text{GT/s}$ | $8b/10b$（==$20\%$ 开销==） | $250\ \text{MB/s}$ |
      | PCIe 2.0 | $5\ \text{GT/s}$ | $8b/10b$ | $500\ \text{MB/s}$ |
      | PCIe 3.0 | $8\ \text{GT/s}$ | $128b/130b$（==仅 $1.5\%$==） | $\approx985\ \text{MB/s}$ |
      | PCIe 4.0 | $16\ \text{GT/s}$ | $128b/130b$ | $\approx1.97\ \text{GB/s}$ |

      **注意 PCIe 2.0 → 3.0 那一行**：
      速率只从 $5$ 提到 $8$（$1.6$ 倍），==有效带宽却几乎翻倍==，
      因为编码开销从 $20\%$ 降到了 $1.5\%$。

      ==$\text{GT/s}$（每秒千兆次传输）和 $\text{Gbps}$ 不是一回事==：
      前者是"线上跳变多少次"，后者要==扣掉编码开销==才是有效数据。
      这和上一节[峰值带宽 vs 实际传输率](#/co/bus/bus-basic?at=ex-burst)
      是同一种"标称值 $\ne$ 可用值"的关系。
    ` },

    /* ================================================================== */
    { t: 'h', id: 'pitfalls', c: '六、易错清单' },

    { t: 'warn', id: 'pitfall-list', title: '这一节的固定失分点', c: String.raw`
      1. **算带宽时忘了 $\div 8$** —— ==总线宽度给的是位，带宽要的是字节==。
      2. **把总线时钟频率当成工作频率** ——
         ==双沿传输要 $\times2$，$k$ 个周期传一次要 $\div k$==，
         见[换算表](#/co/bus/bus-basic?at=metrics)。
      3. **看到"地址/数据复用"就把宽度减半** ——
         ==复用不改变数据宽度==，见[这道真题](#/co/bus/bus-basic?at=ex-bandwidth)。
      4. **把峰值带宽当成实际传输率** ——
         ==实际要把发地址等开销算进分母==，见[这道题](#/co/bus/bus-basic?at=ex-burst)。
      5. **说地址总线是双向的** —— ==地址总线单向，数据总线双向==。
      6. **把片内总线和系统总线混为一谈** ——
         ==片内总线在 CPU 芯片内部==，接口只有 $\texttt{MAR}$ 和 $\texttt{MDR}$。
      7. **认为单总线结构不能扩充** ——
         恰恰相反，==单总线最容易扩充==，它的缺点是==带宽瓶颈==。
      8. **认为串行一定比并行慢** ——
         ==高速场合恰恰相反==，见[信号偏斜](#/co/bus/bus-basic?at=skew)。
      9. **$n$ 根地址线的寻址范围写成 $2^n$ 字节而不问编址单位** ——
         ==按字编址时是 $2^n$ 个字==。
      10. **总线宽度、总线带宽、总线传输周期三个词混用** ——
          ==宽度是"多宽"，带宽是"多快"，传输周期是"一次多久"==。
    ` },

    { t: 'md', c: String.raw`
      ---

      共享带来的另外两个问题 —— ==谁能用总线==、==怎么在总线上对时== ——
      在[总线仲裁与定时](#/co/bus/bus-timing?at=arbitration)。
    ` },

  ],
});
