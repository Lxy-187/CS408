/* ==========================================================================
   计算机组成原理 / 2 数据的表示和运算 / 浮点数表示（IEEE 754）
   —— 只管「一串位怎么解释成一个数」。加减乘除、对阶、舍入在隔壁 float-op。
   ========================================================================== */

KM.page({
  path: 'co/data/float',
  title: '浮点数表示（IEEE 754）',
  subtitle: '阶码定**范围**、尾数定**精度** —— 这一节所有结论都是这句话的推论',
  tags: ['高频', '必考', '手算', '概念辨析'],
  updated: '2026-08-11',

  blocks: [

    /* ================================================================== */
    { t: 'h', id: 'why-float', c: '一、为什么要有浮点数' },

    { t: 'key', id: 'fixed-dilemma', title: '定点数的两难：范围和精度抢同一批位', c: String.raw`
      32 位定点小数，小数点位置一旦钉死，==范围和精度就被同一批位锁死了==：
      小数点靠左则能表示的最大数很小，靠右则分辨不出小数。
      而实际计算里，$10^{-30}$ 和 $10^{30}$ 经常出现在同一个程序里。

      浮点数的做法是==拿出一部分位专门存"小数点在哪"==（阶码），
      剩下的位存有效数字（尾数）。于是：

      $$N=(-1)^S\times M\times 2^{E}$$

      - 阶码 $E$ 多一位，表示范围==指数级==扩大；
      - 尾数 $M$ 多一位，有效数字多一位，精度翻倍。

      ==这两句是本节的总纲==：后面所有"范围""精度"的题目，都只是在问
      "你动的是阶码还是尾数"。
    ` },

    { t: 'warn', id: 'range-vs-precision', title: '范围变大 ≠ 能表示的数变多', c: String.raw`
      32 位就是 32 位，一共只有 $2^{32}$ 种位模式。
      浮点数把范围撑到 $\pm 3.4\times 10^{38}$，==并没有凭空多出数来==，
      只是把这 $2^{32}$ 个"刻度"重新分布了：靠近 0 的地方刻度密，远处刻度疏。

      更极端一点：float 里还有大量位模式被 NaN 占着（$2^{24}-2$ 个），
      再加上 $\pm 0$、$\pm\infty$ 重复表示，
      ==32 位 float 能表示的不同数值实际上比 32 位 int 还少==。
      int 的优势是：在它的范围内，==每一个整数都精确==。
    ` },

    /* ================================================================== */
    { t: 'h', id: 'format', c: '二、IEEE 754 的格式' },

    { t: 'key', id: 'ieee-formula', title: '规格化数的通用公式', c: String.raw`
      $$V=(-1)^S\times (1+M)\times 2^{E-Bias}$$

      三个字段各司其职：

      | 字段 | 名字 | 作用 |
      |---|---|---|
      | $S$ | 符号位 | $0$ 正 $1$ 负，==只管符号，不参与大小== |
      | $E$ | 阶码 | 无符号整数，减去偏置 $Bias$ 才是真实指数 |
      | $M$ | 尾数 | 纯小数部分，真正的有效数字是 $1.M$ |

      注意公式里写的是 $1+M$：==存进去的 $M$ 是小数点后面那一截==，
      前面那个 1 是补出来的（见下面的隐藏位）。
    ` },

    { t: 'code', id: 'bit-layout', title: '单精度 32 位的排布', lang: '',
      note: '从高位到低位：符号 → 阶码 → 尾数，顺序是精心安排的',
      c: String.raw`
        31  30                23  22                                    0
        ┌─┬───────────────────┬───────────────────────────────────────┐
        │S│      E (8位)       │              M (23位)                  │
        └─┴───────────────────┴───────────────────────────────────────┘
         符号        阶码(移码)                   尾数(纯小数)

        例：12.375 = 1100.011₂ = 1.100011₂ × 2³
            S=0   E=3+127=130=10000010₂   M=100011000...0

        0 10000010 10001100000000000000000  =  0x41460000
        └┘└──────┘└─────────────────────┘
      ` },

    { t: 'compare', id: 'param-table', title: '两种精度的全部参数（必背）',
      cols: ['', '单精度 float', '双精度 double'],
      rows: [
        ['总位数', '32', '64'],
        ['符号 $S$', '1 位', '1 位'],
        ['阶码 $E$', '**8 位**', '**11 位**'],
        ['尾数 $M$', '**23 位**', '**52 位**'],
        ['偏置 $Bias$', '**127** $=2^7-1$', '**1023** $=2^{10}-1$'],
        ['有效位数（含隐藏位）', '24 位', '53 位'],
        ['真实指数范围', '$-126 \\sim +127$', '$-1022 \\sim +1023$'],
        ['十进制有效数字', '约 7 位', '约 16 位'],
        ['绝对值最大', '$\\approx 3.4\\times10^{38}$', '$\\approx 1.8\\times10^{308}$'],
      ] },

    { t: 'key', id: 'hidden-bit', title: '隐藏位：白捡的一位精度', c: String.raw`
      二进制规格化之后，有效数字必然形如 $1.xxxx$ ——
      ==最高位永远是 1，存它纯属浪费==。于是 IEEE 754 干脆不存，用的时候补回来。

      $$\underbrace{1}_{\text{隐藏，不占位}}.\underbrace{xxx\cdots x}_{M,\ 23\ \text{位}}$$

      结论：**单精度尾数字段 23 位，实际有效位数 24 位**；双精度 52 位字段、53 位有效。
      ==算精度、算能精确表示的最大整数时，用的都是 24 / 53，不是 23 / 52。==

      代价是 0 表示不出来了（$1.000\cdots$ 永远 $\ge 1$），
      所以 $E=0$ 被专门征用来表示 0 和非规格化数（见下一节）。
    ` },

    { t: 'key', id: 'bias-127', title: '★ 阶码为什么用移码，偏置为什么是 127 而不是 128', c: String.raw`
      **为什么用移码（无符号整数 + 偏置）而不是补码**：

      1. ==比大小时可以直接把整串位当整数比==。
         符号位在最高位、阶码紧随其后，于是对两个**非负**浮点数，
         位模式的无符号大小顺序==就是==数值的大小顺序，整数比较器可以直接复用。
         （负数那一侧顺序是反的：位模式越大值越小，所以硬件要先看符号位。
         如果阶码用补码，==符号位和阶码符号会打架==，就没这么整齐了。）
      2. 对阶时要比较两个阶码的大小、求差，移码下就是无符号减法，不用判符号。
      3. 全 0 的阶码天然对应最小的指数，==0 的表示可以做成全 0==（清零即得 0）。

      **偏置为什么是 $2^{n-1}-1=127$**：

      标准移码的偏置本该是 $2^{n-1}=128$。IEEE 754 特意少 1，是因为
      ==$E=0$ 和 $E=255$ 两个码点被征用去表示零 / 非规格化数 / 无穷 / NaN 了==，
      规格化数只剩 $E\in[1,254]$。取 $Bias=127$ 之后真实指数范围是
      $1-127=-126$ 到 $254-127=+127$，正负两侧大致对称。

      ==考试爱考的就是这个 127 / 1023，以及"真实指数 = $E-127$，而不是 $E-128$"。==
    ` },

    { t: 'warn', id: 'textbook-vs-ieee', title: '别把教材的"通用浮点格式"和 IEEE 754 搅在一起', c: String.raw`
      408 教材（唐朔飞 / 王道）里还讲一套==更一般的浮点格式==：

      $$\underbrace{\text{阶符}\ \text{阶码}}_{\text{补码或移码}}\ \ \underbrace{\text{数符}\ \text{尾数}}_{\text{原码或补码}}$$

      它和 IEEE 754 的区别，考试两套都会考：

      | | 教材通用格式 | IEEE 754 |
      |---|---|---|
      | 阶码 | 补码 / 移码都可能 | 移码，偏置 $2^{n-1}-1$ |
      | 尾数 | 原码或补码，**纯小数** $0.1xxx$ | 原码制，**$1.xxx$** |
      | 隐藏位 | ==没有== | ==有== |
      | 规格化判据 | 见下 | 尾数最高位恒为 1（隐含） |

      **教材体系里的规格化判据**（很爱考）：

      - 原码尾数：==最高数值位为 1==，即 $0.1xxx$ / $1.1xxx$，绝对值范围 $[0.5,\,1)$；
      - 补码尾数：==符号位与最高数值位相反==，即正数 $0.1xxx$、负数 $1.0xxx$，
        取值范围 $[-1,\,-0.5)\cup[0.5,\,1)$。

      ==补码那条的两个特例是送分题也是送命题==：
      $-1=1.000\cdots0$ **是**规格化数（符号 1、最高位 0）；
      $-0.5=1.100\cdots0$ **不是**规格化数（符号 1、最高位 1）。
    ` },

    /* ================================================================== */
    { t: 'h', id: 'special', c: '三、位模式的五种含义' },

    { t: 'key', id: 'five-kinds', title: '看阶码 E 分三档，再看尾数 M 分岔', c: String.raw`
      判断顺序永远是==先看 E 是不是全 0 或全 1，再看 M 是不是全 0==。以单精度为例：

      | 分类 | $E$ | $M$ | 数值 | 说明 |
      |---|---|---|---|---|
      | **规格化数** | $1\sim254$ | 任意 | $(-1)^S(1{+}M)2^{E-127}$ | 绝大多数情况 |
      | **非规格化数** | $0$ | $\neq0$ | $(-1)^S(0{+}M)2^{-126}$ | ==没有隐藏位，指数固定 $-126$== |
      | **零** | $0$ | $0$ | $\pm 0$ | 有两个 0 |
      | **无穷大** | $255$ | $0$ | $\pm\infty$ | 上溢 / 除以 0 的结果 |
      | **NaN** | $255$ | $\neq0$ | 非数 | $0/0$、$\infty-\infty$、$\sqrt{-1}$ |

      ==最容易错的一处==：非规格化数的指数不是 $0-127=-127$，而是 $\mathbf{1-127=-126}$。
      这是刻意设计的 —— 让最大的非规格化数和最小的规格化数==正好首尾相接==，
      中间不留缝。
    ` },

    { t: 'key', id: 'denorm-meaning', title: '非规格化数存在的意义：渐进下溢', c: String.raw`
      如果只有规格化数，最小的正数是 $2^{-126}$，
      而 $0$ 和 $2^{-126}$ 之间==是一片空白==。后果是
      $x\neq y$ 但 $x-y$ 算出来等于 $0$，
      于是 $\texttt{if (x != y) z = 1/(x-y);}$ 这种写法会当场除以 0。

      非规格化数把这段空白用等间隔的 $2^{-149}$ 刻度填满了，
      让数值==逐步==退化到 0，这叫 ==渐进下溢（gradual underflow）==。

      代价是精度逐位减少：越靠近 0，有效位数越少，最后只剩 1 位。
      可以在[位模式工具](#/tools/co/ieee754?at=widget)里点"最小规格化数"，
      再点一次"前一个可表示数"，==亲眼看阶码掉成全 0 的那一瞬间==。
    ` },

    { t: 'code', id: 'number-line', title: '正半轴上的刻度分布', lang: '',
      note: '刻度不均匀是浮点数一切怪事的根源',
      c: String.raw`
        0    非规格化数        规格化数（每跨一个 2 的幂，间隔翻倍）              +∞
        ├────┼────┼────┼───┼──────┼──────────────┼──────────────────────────┤
        0  2⁻¹⁴⁹  …  2⁻¹²⁶  2⁻¹²⁵   2⁻¹²⁴            …          (2-2⁻²³)×2¹²⁷

        等间隔一段 │←—— 每段内部等间隔，段与段之间间隔翻倍 ——→│

        指数每 +1，这一段里相邻两数的间隔就翻一倍：
          1.0 附近      间隔 = 2⁻²³ ≈ 1.2×10⁻⁷
          2.0 附近      间隔 = 2⁻²²
          10²⁰ 附近     间隔 = 2⁴³ ≈ 8.8×10¹²   ← 比 1 大得多，所以 10²⁰+1 = 10²⁰
      ` },

    /* ================================================================== */
    { t: 'h', id: 'range', c: '四、表示范围' },

    { t: 'formulas', id: 'range-table', title: '单精度的四个边界（记住怎么推，别死背）', items: [
      { label: '最大正规格化数（E=254, M 全 1）', tex: '(2-2^{-23})\\times 2^{127}\\approx 3.4\\times 10^{38}' },
      { label: '最小正规格化数（E=1, M 全 0）',   tex: '1.0\\times 2^{-126}\\approx 1.18\\times 10^{-38}' },
      { label: '最大正非规格化数（E=0, M 全 1）', tex: '(1-2^{-23})\\times 2^{-126}' },
      { label: '最小正非规格化数（E=0, M=1）',    tex: '2^{-23}\\times 2^{-126}=2^{-149}\\approx 1.4\\times 10^{-45}' },
    ] },

    { t: 'key', id: 'range-derive', title: '现场推这四个数的方法', c: String.raw`
      不要背，==按定义念一遍就出来了==：

      1. **最大正数**：阶码取最大的可用值 $254$（$255$ 被 $\infty$ 占了）
         $\Rightarrow$ 指数 $254-127=127$；尾数全 1 $\Rightarrow$ 有效值
         $1.\underbrace{11\cdots1}_{23}=2-2^{-23}$。
      2. **最小正规格化数**：阶码取最小可用值 $1\Rightarrow$ 指数 $1-127=-126$；
         尾数全 0 $\Rightarrow$ 有效值 $1.0$。
      3. **最小正非规格化数**：$E=0$ 指数锁定 $-126$，尾数只有最低位是 1
         $\Rightarrow$ 有效值 $2^{-23}$，相乘得 $2^{-149}$。

      双精度把 $127\to1023$、$23\to52$、$-126\to-1022$、$-149\to-1074$ 照抄一遍即可。
    ` },

    { t: 'code', id: 'overflow-zones', title: '数轴上的四个溢出区', lang: '',
      note: '这张图能直接回答"什么时候算溢出"',
      c: String.raw`
        负上溢          可表示的负数           负下溢  正下溢         可表示的正数          正上溢
      ←────────┼──────────────────────┼───────────┼───────────┼──────────────────────┼────────→
             -Max                   -min         0         +min                   +Max

        ┌ 上溢 (overflow)：|结果| > Max，阶码超出上界 → 置 ±∞，触发异常
        └ 下溢 (underflow)：0 < |结果| < min，阶码低于下界 → 退化为非规格化数，
                            再小则置 ±0（机器零），通常不当作错误

        Max = (2-2⁻²³)×2¹²⁷      min = 2⁻¹²⁶（规格化）/ 2⁻¹⁴⁹（含非规格化）
      ` },

    { t: 'warn', id: 'overflow-vs-underflow', title: '上溢是错误，下溢通常不是', c: String.raw`
      - **上溢**：结果超出表示范围，==信息完全丢失==，IEEE 754 置 $\pm\infty$ 并置溢出标志。
      - **下溢**：结果太接近 0，先退化成非规格化数（精度变差但还有值），
        再小才变成 $\pm 0$。==绝大多数场合把它当成"结果就是 0"，不报错==。

      考试里"判断该浮点运算是否溢出"，==只看阶码有没有越界，不看尾数==。
      尾数相加进位不是溢出，右规一下就好了 —— 详见
      [尾数溢出不是溢出](#/co/data/float-op?at=mantissa-overflow)。
    ` },

    /* ================================================================== */
    { t: 'h', id: 'precision', c: '五、精度' },

    { t: 'key', id: 'ulp-def', title: '精度 = 相邻两个可表示数的间隔（ulp）', c: String.raw`
      "精度"在浮点里==不是一个绝对误差，而是一个相对量==。
      对指数为 $e$ 的那一段，相邻两个可表示数的间隔是

      $$\text{ulp}=2^{e-23}\quad(\text{单精度})$$

      也就是说==有效数字的相对误差恒定在 $2^{-24}$ 上下，绝对误差随数值大小成比例放大==。

      这一条直接推出后面所有现象：大数吃小数、加法不满足结合律、
      $0.1+0.2\neq0.3$，本质都是"在大数那一段，刻度太粗了"。
    ` },

    { t: 'key', id: 'digits', title: '十进制有效数字：7 位 / 16 位', c: String.raw`
      $$\log_{10}(2^{24})\approx 7.22,\qquad \log_{10}(2^{53})\approx 15.95$$

      所以口径是==单精度约 7 位、双精度约 16 位十进制有效数字==，考试写这个就行。

      更严谨的说法（了解，偶尔出选择题）：

      - **保证正确的位数**：单精度 **6** 位、双精度 **15** 位
        （任何 6 位十进制数存成 float 再读回来都不变）；
      - **往返所需的位数**：单精度 **9** 位、双精度 **17** 位
        （要想任意 float 转成十进制再转回来位模式不变，得打印这么多位）。

      $7.22$ 和 $15.95$ 落在这两个数中间，正是这个原因。
    ` },

    { t: 'warn', id: 'int-exact-2p24', title: '★ float 只能精确表示 $|x|\\le 2^{24}$ 的整数', c: String.raw`
      有效位数只有 24 位，所以==超过 $2^{24}=16\,777\,216$ 的整数就开始存不全==：

      | 区间 | 相邻可表示整数的间隔 |
      |---|---|
      | $[0,\ 2^{24}]$ | $1$（全部精确） |
      | $(2^{24},\ 2^{25}]$ | $2$（只剩偶数） |
      | $(2^{25},\ 2^{26}]$ | $4$ |
      | $(2^{n},\ 2^{n+1}]$ | $2^{n-23}$ |

      经典结论：==$16777217$（即 $2^{24}+1$）存不进 float==，会变成 $16777216$。

      双精度的门槛是 $2^{53}=9\,007\,199\,254\,740\,992$，
      所以 ==32 位 int 转 double 一定无损==，转 float 则可能丢精度。
    ` },

    /* ================================================================== */
    { t: 'h', id: 'convert', c: '六、手算互转（必考题型）' },

    { t: 'method', id: 'dec-to-754', title: '十进制 → IEEE 754 的五步', c: String.raw`
      1. **定符号**：负数 $S=1$，然后==只管绝对值==；
      2. **整数、小数分别转二进制**：整数部分除 2 取余（倒着读），
         小数部分乘 2 取整（正着读）；
      3. **规格化**：小数点移到第一个 1 的右边，写成 $1.xxx\times 2^{e}$，
         ==记住移了几位就是 $e$==（左移为正，右移为负）；
      4. **算阶码**：$E=e+127$，写成 8 位二进制；
      5. **拼尾数**：把 $1.xxx$ 里==小数点后面==那一截抄进 23 位，右边补 0。

      最后按 $S\,|\,E\,|\,M$ 拼起来，每 4 位一组转十六进制。
    ` },

    { t: 'method', id: '754-to-dec', title: 'IEEE 754 → 十进制的三步', c: String.raw`
      1. **切三段**：$1+8+23$。十六进制先展开成二进制再切，
         ==注意 8 位阶码横跨了第 1 个和第 2 个十六进制位==，别切错；
      2. **判类型**：$E$ 全 0 或全 1 就走特殊值那一套（见
         [五种含义](#/co/data/float?at=five-kinds)），否则按规格化数算；
      3. **代公式**：真实指数 $e=E-127$，有效值 $1.M$，
         把小数点按 $e$ 移回去，读出真值。
    ` },

    { t: 'example',
      id: 'ex-dec2bits',
      title: '把 $-12.75$ 写成单精度机器数',
      source: '基础必会',
      level: 2,
      problem: String.raw`
        用 IEEE 754 单精度格式表示 $-12.75$，写出 32 位二进制和十六进制。
      `,
      idea: String.raw`
        照着五步走。==唯一容易错的是第 3 步的指数符号==：
        小数点从 $1100.11$ 往左移了 3 位才变成 $1.10011$，
        所以是乘 $2^{3}$（$e=+3$），不是 $-3$。

        自查的办法：$12.75$ 在 $8=2^3$ 和 $16=2^4$ 之间，那 $e$ 必然是 3。
      `,
      solution: String.raw`
        **① 符号**：负数，$S=1$，下面只算 $12.75$。

        **② 转二进制**

        - 整数 $12=1100_2$；
        - 小数 $0.75$：$0.75\times2=1.5$ 取 1，$0.5\times2=1.0$ 取 0 余 1，得 $0.11_2$。

        $$12.75=1100.11_2$$

        **③ 规格化**：小数点左移 3 位

        $$1100.11_2=1.10011_2\times 2^{3}\quad\Rightarrow\quad e=3$$

        **④ 阶码**：$E=e+Bias=3+127=130=10000010_2$

        **⑤ 尾数**：取 $1.10011$ 小数点后的 $10011$，右边补 0 到 23 位

        $$M=100\,1100\,0000\,0000\,0000\,0000$$

        **拼起来**

        $$\underbrace{1}_{S}\ \underbrace{10000010}_{E}\ \underbrace{10011000000000000000000}_{M}$$

        四位一组：$1100\ 0001\ 0100\ 1100\ 0000\cdots$

        $$\boxed{-12.75 = \texttt{0xC14C0000}}$$
      `,
      comment: String.raw`
        在[位模式工具](#/tools/co/ieee754?at=widget)的十进制框里输入 $-12.75$ 对答案，
        或者反过来在十六进制框里敲 $\texttt{C14C0000}$。

        **顺带记两个**：$12.375=\texttt{0x41460000}$，$1.0=\texttt{0x3F800000}$。
        $\texttt{0x3F800000}$ 值得单独记住 —— ==看到 $\texttt{3F80}$ 开头就知道是 1.x==，
        考场上能当"阶码算对没有"的锚点。
      `,
    },

    { t: 'example',
      id: 'ex-bits2dec',
      title: '把 $\\texttt{0x41360000}$ 还原成十进制',
      source: '基础必会',
      level: 2,
      problem: String.raw`
        某 32 位机器数为 $\texttt{41360000}_{16}$，按 IEEE 754 单精度解释，它是多少？
      `,
      idea: String.raw`
        先展开成二进制再切段。==切段时最容易错的是阶码的第 8 位==：
        它是第二个十六进制数字的最低位，不要按 4 位对齐随手切成 $1+8$ 之外的样子。
      `,
      solution: String.raw`
        **① 展开**

        $$\texttt{4}\ \texttt{1}\ \texttt{3}\ \texttt{6}\ \texttt{0}\ \texttt{0}\ \texttt{0}\ \texttt{0}
        =0100\ 0001\ 0011\ 0110\ 0000\ 0000\ 0000\ 0000$$

        **② 切三段**

        | | 位 | 值 |
        |---|---|---|
        | $S$ | $0$ | 正数 |
        | $E$ | $1000\,0010$ | $130$ |
        | $M$ | $011\,0110\,0\cdots0$ | — |

        **③ 代公式**

        $$e=E-127=130-127=3,\qquad 1.M=1.0110110_2$$

        $$V=1.0110110_2\times 2^{3}=1011.0110_2$$

        $$=8+2+1+\tfrac14+\tfrac18=\boxed{11.375}$$
      `,
      comment: String.raw`
        **反向自查**：$11.375$ 介于 $8$ 与 $16$ 之间 $\Rightarrow e=3\Rightarrow E=130$ ✓。
        ==凡是算完能用"这个数在哪两个 2 的幂之间"验一下阶码的，都要验==，
        一秒钟排掉最常见的错。
      `,
    },

    { t: 'example',
      id: 'ex-neg-frac',
      title: '选择题：$-0.4375$ 的单精度表示',
      source: '课后题 / 错题',
      level: 2,
      problem: String.raw`
        $-0.4375$ 的 IEEE 754 单精度浮点数表示为（　）。

        **A.** $\texttt{BEE0 0000H}$　　**B.** $\texttt{BF60 0000H}$

        **C.** $\texttt{BF70 0000H}$　　**D.** $\texttt{C0E0 0000H}$
      `,
      idea: String.raw`
        ==这题的全部难度都在"指数是几、符号是什么"上==，尾数只有两位 1，不会错。

        所以先把 $e$ 定死再动手：$0.4375<1$，
        ==绝对值小于 1 的数，指数必然是负的==。
        这一句话就能砍掉 D（后面会看到 D 恰好是指数写成 $+2$ 的产物）。

        然后数第一个 1 的位置：$0.4375=0.0111_2$，
        最高的那个 1 在 $2^{-2}$ 位上 $\Rightarrow e=-2$。
      `,
      solution: String.raw`
        **① 符号**：负数，$S=1$。

        **② 绝对值转二进制**

        $$0.4375=\tfrac{7}{16}=0.0111_2$$

        （$0.4375\times2=0.875$ 取 0，$\times2=1.75$ 取 1，$\times2=1.5$ 取 1，$\times2=1.0$ 取 1）

        **③ 规格化**：小数点==向右==移 2 位

        $$0.0111_2=1.11_2\times 2^{-2}\quad\Rightarrow\quad e=-2$$

        **④ 阶码**：$E=e+127=-2+127=125=01111101_2$

        **⑤ 尾数**：取 $1.11$ 小数点后的 $11$，补齐 23 位 $\Rightarrow M=110\,0000\cdots0$

        **拼起来**

        $$\underbrace{1}_{S}\ \underbrace{01111101}_{E}\ \underbrace{11000000000000000000000}_{M}$$

        四位一组：$1011\ 1110\ 1110\ 0000\ \cdots$

        $$\boxed{\texttt{BEE0 0000H}\quad\text{选 A}}$$
      `,
      comment: String.raw`
        **把四个选项全部反推一遍，比只算对答案值钱**：

        | 选项 | $E$ | $e$ | $1.M$ | 实际是多少 | 错在哪 |
        |---|---|---|---|---|---|
        | **A** | $125$ | $-2$ | $1.11$ | $\mathbf{-0.4375}$ | ✅ |
        | B | $126$ | $-1$ | $1.11$ | $-0.875$ | 第一个 1 的位置数错一位 |
        | C | $126$ | $-1$ | $1.111$ | $-0.9375$ | 把 $0.0111$ 抄成了 $0.1111$ |
        | D | $129$ | $\mathbf{+2}$ | $1.11$ | $\mathbf{-7}$ | ==指数符号写反== |

        ==三个干扰项全在打"指数"这一个点==，尾数一个都没改 ——
        出题人很清楚考生栽在哪。

        **注意 A 和 D 差了多少**：指数错了 $4$（从 $-2$ 变成 $+2$），
        结果就差 $2^4=16$ 倍，而 $0.4375\times16=7$ ✓。
        ==算完发现数量级离谱，回头查的第一个地方就是指数的符号==。
      `,
    },

    { t: 'method', id: 'exp-sign-check', title: '★ 指数符号的三道自查关卡', c: String.raw`
      $e$ 的符号是这一节唯一的高频失分点，写完立刻过一遍这三关：

      1. **先判量级**（最有用的一关）：
         $$|x|<1\Rightarrow e<0,\qquad 1\le|x|<2\Rightarrow e=0,\qquad |x|\ge2\Rightarrow e>0$$
         再精确一点：==$|x|$ 落在 $[2^{k},2^{k+1})$ 里，$e$ 就等于 $k$==。
         $0.4375\in[2^{-2},2^{-1})\Rightarrow e=-2$，一步到位。

      2. **看移动方向**：小数点==左移为正，右移为负==。
         $1100.11\to1.10011$ 是左移，$e=+3$；$0.0111\to1.11$ 是右移，$e=-2$。

      3. **反查阶码**：$e<0\Leftrightarrow E<127\Leftrightarrow$ ==阶码最高位为 0==。
         算出 $E$ 之后瞄一眼首位，和第 1 关的结论对得上才往下写。
    ` },

    { t: 'method', id: 'hex-quick-check', title: '看开头一个十六进制数字，直接砍掉一半选项', c: String.raw`
      选择题里连算都不用算的一招。机器数紧跟符号位的那一位就是==阶码的最高位==，
      而它为 1 $\Leftrightarrow E\ge128\Leftrightarrow e\ge1\Leftrightarrow|x|\ge2$。

      于是首字节的第一个十六进制数字直接暴露了量级：

      | $\|x\|$ | 正数开头 | 负数开头 |
      |---|---|---|
      | $\ge 2$ | $\texttt{4}\sim\texttt{7}$ | ==$\texttt{C}\sim\texttt{F}$== |
      | $<2$ | $\texttt{0}\sim\texttt{3}$ | $\texttt{8}\sim\texttt{B}$ |

      再细一档（负数，规格化）：
      $|x|\in[1,2)\to\texttt{BF8}\sim\texttt{BFF}$，
      $[0.5,1)\to\texttt{BF0}\sim\texttt{BF7}$，
      $[0.25,0.5)\to\texttt{BE8}\sim\texttt{BEF}$。

      回头看[上面那道选择题](#/co/data/float?at=ex-neg-frac)：
      $-0.4375\in[0.25,0.5)$，答案必然是 ==$\texttt{BE}$ 开头==。
      $\texttt{C0E0}$ 是 $\texttt{C}$ 开头 $\Rightarrow|x|\ge2$，==看一眼就该划掉==；
      $\texttt{BF60}$、$\texttt{BF70}$ 是 $\texttt{BF}$ 开头 $\Rightarrow|x|\in[0.5,1)$，也不对。
      四个选项，一个都不用算。
    ` },

    { t: 'insight', id: 'my-exp-sign-slip', title: '我在这道题上栽的跟头', c: String.raw`
      ==写 2 的指数的时候，本来应该是 $-2$，我写成了 $2$。==

      不是不会算 —— $0.0111_2\to1.11_2$ 这一步小数点==向右==移了 2 位，
      我把这个"2"顺手抄成了指数，负号丢在了半路上。
    ` },

    { t: 'md', c: String.raw`
      下面这个就是上面几道题的自动版 —— ==自己先手算，再用它对答案==，
      别反过来。完整用法见[工具页](#/tools/co/ieee754?at=how-to-use)。
    ` },

    { t: 'tool', id: 'inline-tool', use: 'ieee754',
      title: 'IEEE 754 位模式（就地可用）',
      note: '点位翻转 / 输十进制 / 输十六进制，三种方式互相验证' },

    /* ================================================================== */
    { t: 'h', id: 'c-cast', c: '七、C 语言里的类型转换' },

    { t: 'compare', id: 'cast-table', title: '四种转换的得失（高频选择题）',
      cols: ['转换', '范围', '精度', '结论'],
      rows: [
        ['`int` → `float`', '不会溢出', '**可能丢**', 'int 有效位最多 31 位 > float 的 24 位'],
        ['`int` → `double`', '不会溢出', '不会丢', '31 位 < double 的 53 位，==完全无损=='],
        ['`float` → `double`', '不会溢出', '不会丢', 'double 的范围和精度都严格覆盖 float'],
        ['`double` → `float`', '**可能溢出**', '**可能丢**', '范围和精度都可能不够'],
        ['`float` → `int`', '**可能溢出**', '**丢小数**', '向 0 截断，不是四舍五入'],
      ] },

    { t: 'warn', id: 'cast-traps', title: '转换题的四个固定陷阱', c: String.raw`
      1. ==float 转 int 是**截断**，不是四舍五入==：
         $\texttt{(int)(-1.9)}$ 得 $-1$，$\texttt{(int)1.9}$ 得 $1$。
      2. ==int 转 float 会丢精度但**不会溢出**==：这两件事经常被混着问。
      3. ==long（64 位）转 double 会丢精度==：64 位整数超过了 53 位有效位。
      4. ==转换链不可逆==：$\texttt{(int)(float)i}$ 不一定等于 $\texttt{i}$，
         $\texttt{(float)(int)f}$ 也不一定等于 $\texttt{f}$（小数没了）。
         ==只有 int → double → int 保证还原。==
    ` },

    { t: 'example',
      id: 'ex-int-float-cast',
      title: 'int 绕一圈变了个数',
      source: '408 高频 / CSAPP 经典',
      level: 3,
      problem: String.raw`
        在 32 位机器上执行：

        ~~~
        int   i = 123456789;
        float f = i;
        int   j = (int)f;
        ~~~

        问 $j$ 是多少？为什么？
      `,
      idea: String.raw`
        看到"int 转 float"就该条件反射：==31 位有效 → 24 位有效，多出来的低位要被舍入==。

        所以问题变成：$123456789$ 的二进制有多少位？超出 24 位的部分怎么舍？
        舍完再转回 int 时不会再丢东西（值已经是整数且没超范围），
        ==所以答案就是那个被舍入后的整数==。
      `,
      solution: String.raw`
        **① 转二进制**

        $$123456789=\texttt{0x75BCD15}=111\ 0101\ 1011\ 1100\ 1101\ 0001\ 0101_2$$

        一共 **27 位**，而 float 只有 **24 位**有效数字。

        **② 舍掉最低 3 位**

        $$\underbrace{111\ 0101\ 1011\ 1100\ 1101\ 0001}_{24\ \text{位，保留}}\ \underbrace{101}_{\text{丢弃}}$$

        丢弃部分是 $101_2=5$，而半个单位是 $100_2=4$，$5>4$ $\Rightarrow$ ==向上进位==，
        即在保留部分的最低位（权重为 $2^3=8$）上加 1：

        $$123456789-5+8=123456792$$

        **③ 转回 int**

        $123456792$ 本身就是可精确表示的浮点值，转 int 无损。

        $$\boxed{j=123456792\neq i}$$
      `,
      comment: String.raw`
        **验算技巧**：结果一定能被 $8=2^{3}$ 整除（因为最低 3 位被清掉了）。
        $123456792/8=15432099$ ✓。

        **同类问法**：
        - "$i==(int)(float)i$ 是否恒成立？" → ==否==（本题就是反例）；
        - "$i==(int)(double)i$ 是否恒成立？" → ==是==（32 位 int ≤ 53 位有效）；
        - "$f==(float)(int)f$ 是否恒成立？" → ==否==（小数部分被截断）。
      `,
    },

    /* ================================================================== */
    { t: 'h', id: 'pitfalls', c: '八、易错清单' },

    { t: 'warn', id: 'pitfall-list', title: '这一节的固定失分点', c: String.raw`
      1. **规格化时指数符号写反**：小数点==左移为正、右移为负==，
         $|x|<1$ 的数指数一定是负的。==本节头号失分点==，
         见[这道选择题](#/co/data/float?at=ex-neg-frac)和[三道自查关卡](#/co/data/float?at=exp-sign-check)。
      2. **偏置写成 128**：IEEE 754 是 $2^{n-1}-1$，单精度 ==127==、双精度 ==1023==。
      3. **非规格化数的指数写成 $-127$**：是 ==$1-127=-126$==。
      4. **精度按 23 位算**：要==算上隐藏位，是 24 位==。
      5. **把"范围"和"精度"的决定因素说反**：==阶码定范围，尾数定精度==。
      6. **$E$ 全 1 时还去套 $1.M$ 的公式**：那是 $\infty$ / NaN 的地盘。
      7. **认为浮点能表示的数比整型多**：见
         [范围变大 ≠ 数变多](#/co/data/float?at=range-vs-precision)。
      8. **把教材的补码规格化判据用到 IEEE 754 上**：两套体系，见
         [别搅在一起](#/co/data/float?at=textbook-vs-ieee)。
      9. **忘了 $\pm 0$、$\pm\infty$ 各有两个**，以及 ==$+0=-0$ 但 $\text{NaN}\neq\text{NaN}$==。
    ` },

    { t: 'md', c: String.raw`
      ---

      表示讲完了，接下来是==怎么算==：对阶、规格化、舍入、溢出判断，
      以及大数吃小数这类"算得出但不对"的现象 ——
      [浮点运算与规格化](#/co/data/float-op?at=five-steps)。
    ` },

  ],
});
