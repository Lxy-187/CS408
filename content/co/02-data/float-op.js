/* ==========================================================================
   计算机组成原理 / 2 数据的表示和运算 / 浮点运算与规格化
   —— 位怎么解释成数在隔壁 float；这一页只管「怎么算」以及「为什么算出来不对」。
   ========================================================================== */

KM.page({
  path: 'co/data/float-op',
  title: '浮点运算与规格化',
  subtitle: '对阶 → 尾数运算 → 规格化 → 舍入 → 判溢出，每一步都有一个固定考点',
  tags: ['高频', '必考', '手算', '综合应用'],
  updated: '2026-08-11',

  blocks: [

    /* ================================================================== */
    { t: 'h', id: 'add-sub', c: '一、加减法的五步' },

    { t: 'steps', id: 'five-steps', title: '浮点加减法的固定流程（背下来，考场按步给分）', items: [
      { title: '对阶：小阶向大阶看齐',
        c: String.raw`
          求阶差 $\Delta E=E_x-E_y$，==把阶码小的那个数的尾数右移 $|\Delta E|$ 位==，
          同时把它的阶码加上 $|\Delta E|$，直到两个阶码相等。
          右移出去的位不要立刻丢，先放进附加位（见[保护位](#/co/data/float-op?at=guard-bits)）。` },
      { title: '尾数求和 / 求差',
        c: String.raw`
          对齐之后，尾数就是普通的定点小数加减法。
          IEEE 754 下参与运算的是==补上隐藏位的 $1.M$==，不是存储的 $M$。` },
      { title: '规格化',
        c: String.raw`
          结果不满足规格化形式就调整：
          **左规**（尾数左移、阶码减，可能要移很多位）
          或 **右规**（尾数右移 1 位、阶码加 1，==最多一次==）。` },
      { title: '舍入',
        c: String.raw`
          对阶和右规都会把有效位挤出去，用附加位按规则舍入回 23 位（或 52 位）。
          ==舍入之后可能又不规格化了，要再规格化一次==。` },
      { title: '溢出判断',
        c: String.raw`
          ==只看阶码==：阶码上溢 $\Rightarrow$ 结果置 $\pm\infty$ 并报异常；
          阶码下溢 $\Rightarrow$ 退化为非规格化数，再小则置 $\pm 0$。` },
    ] },

    { t: 'warn', id: 'why-small-to-big', title: '★ 为什么必须"小阶向大阶看齐"', c: String.raw`
      这是本节最爱考的一问，答案要==从"丢哪一头"来说==：

      - **小阶向大阶**：小数的尾数**右**移，==移出去的是最低位==，
        只损失精度，数值的量级不变；
      - **大阶向小阶**：大数的尾数得**左**移，==移出去的是最高有效位==，
        直接把数值本身砍掉一大块，而且隐藏位那个 1 一移就没了，错得离谱。

      一句话版本：==右移丢的是"零头"，左移丢的是"本金"==。

      顺带一个细节：右移时空出来的高位，==原码补 0、补码补符号位==（算术右移）。
    ` },

    { t: 'code', id: 'align-demo', title: '对阶现场：$0.5+(-0.4375)$', lang: '',
      note: '注意 y 右移之后隐藏位的 1 掉到了小数点右边',
      c: String.raw`
        对阶前                              对阶后（都变成 2⁻¹）
        x =  1.000 × 2⁻¹                    x =  1.000 × 2⁻¹
        y = -1.110 × 2⁻²                    y = -0.111 × 2⁻¹   ← 尾数右移1位，阶码 -2→-1
                                                  ↑
                                            隐藏位的 1 右移进来了

        尾数相加：  1.000
                  - 0.111
                  ────────
                    0.001   × 2⁻¹     ← 最高位不是 1，要左规

        左规 3 位： 1.000 × 2⁻⁴  = 0.0625
      ` },

    { t: 'key', id: 'normalize-rule', title: '左规与右规', c: String.raw`
      | | 什么时候 | 怎么做 | 次数 |
      |---|---|---|---|
      | **左规** | 尾数最高有效位不是 1（如 $0.001\cdots$） | 尾数左移，==每移一位阶码减 1== | 可能多次，直到首位为 1 |
      | **右规** | 尾数溢出（如 $10.101\cdots$，两位整数） | 尾数右移 1 位，==阶码加 1== | ==最多一次== |

      **为什么右规最多一次**：两个规格化尾数都在 $[1,2)$ 内，
      和最大不超过 $4$，即 $10.xxx$ 形式，==右移一位必定回到 $[1,2)$==。

      **为什么左规可能很多次**：两个相近的数相减会发生==灾难性抵消==，
      前面一长串有效位全变成 0，得一路左移。本页的例题就左规了 3 位。

      教材体系（尾数是纯小数 $0.1xxx$）的判据不同，见
      [两套体系的区别](#/co/data/float?at=textbook-vs-ieee)。
    ` },

    /* ================================================================== */
    { t: 'h', id: 'rounding', c: '二、舍入' },

    { t: 'key', id: 'guard-bits', title: '附加位：保护位 / 舍入位 / 粘位', c: String.raw`
      对阶时右移出去的位如果直接扔掉，误差会大到离谱。
      硬件在尾数右边多留几位：

      - **保护位 G**（guard）：紧邻最低有效位的那一位；
      - **舍入位 R**（round）：再往右一位；
      - **粘位 S**（sticky）：==再往右所有位的"或"==——只要有过 1 就一直是 1。

      粘位的作用是==区分"正好一半"和"比一半多一点点"==：
      只有 $GRS=100$ 才是精确的一半（要按"到偶数"处理），
      $GRS=101$ 就是过半，直接进位。
    ` },

    { t: 'key', id: 'round-decision', title: '★ 就近舍入的三行判定表（背这个就够了）', c: String.raw`
      设丢弃部分的三个附加位是 $G\,R\,S$，==判定只有三行==：

      | 丢弃的部分 | 条件 | 怎么做 |
      |---|---|---|
      | $<$ 半个 ulp | $G=0$ | 直接舍去 |
      | $>$ 半个 ulp | $G=1$ 且 $R,S$ ==至少有一个 1== | **无条件进位**，==和末位奇偶无关== |
      | $=$ 半个 ulp | $G=1$ 且 $R=S=0$ | 正中间 $\Rightarrow$ **取偶**：末位是 0 就舍、是 1 就进 |

      ==「中间值取偶数」是第三行专属的规则==。
      题面上写了"就近舍入（中间值取偶数）"，==不等于这题要用取偶==，
      它只是在说明用的是哪种舍入模式。前两行根本轮不到取偶出场 ——
      [这道题](#/co/data/float-op?at=ex-round-121)考的就是这个。
    ` },

    { t: 'compare', id: 'rounding-modes', title: 'IEEE 754 的四种舍入方式',
      cols: ['方式', '规则', '$+1.5$ / $+2.5$ / $-1.5$ 舍到整数'],
      rows: [
        ['**就近舍入到偶数**（默认）', '取最近的可表示值；正好在中间时取==末位为偶==的那个', '$2$ / $2$ / $-2$'],
        ['朝 $+\\infty$ 舍入', '向上取（正数进位，负数截断）', '$2$ / $3$ / $-1$'],
        ['朝 $-\\infty$ 舍入', '向下取', '$1$ / $2$ / $-2$'],
        ['朝 $0$ 舍入（截断）', '直接砍掉多余位，绝对值只会变小', '$1$ / $2$ / $-1$'],
      ] },

    { t: 'key', id: 'tie-to-even', title: '"到偶数"是为了消掉系统性偏差', c: String.raw`
      如果正好一半时一律进位（小学的四舍五入），==每次都往上偏==，
      大量数据累加后误差会==单向累积==。

      改成"取末位为偶"之后，正好一半的情况一半向上、一半向下，
      ==长期统计的误差期望是 0==。这就是默认模式选它的唯一理由。

      教材里的三种简化舍入法，考试也会考：
      **截断法**（直接丢，误差单向且最大）、
      **0 舍 1 入**（丢弃部分最高位是 1 就进位，类似四舍五入）、
      **恒置 1 法**（末位强制为 1，正负误差抵消，硬件最省）。
    ` },

    { t: 'warn', id: 'round-then-renormalize', title: '舍入之后可能又不规格化了', c: String.raw`
      $1.111\cdots1$ 进位之后会变成 $10.000\cdots0$，==又溢出一位==，
      得再右规一次、阶码再加 1。

      所以标准流程里，==规格化和舍入之间是可以来回一次的==，
      写步骤时别漏掉最后那次"再规格化 + 再判溢出"。
    ` },

    { t: 'example',
      id: 'ex-round-121',
      title: '$12.1$ 的单精度机器数',
      source: '真题 / 高频',
      level: 3,
      problem: String.raw`
        已知用 IEEE 754 单精度浮点数表示浮点型变量，采用就近舍入（中间值取偶数）。
        若浮点型变量 $x$ 为 $12.1$，则 $x$ 的机器数是（　）。

        **A.** $\texttt{4141 9999H}$　　**B.** $\texttt{4141 999AH}$

        **C.** $\texttt{41E0 CCCCH}$　　**D.** $\texttt{41E0 CCCDH}$
      `,
      idea: String.raw`
        $0.1$ 在二进制里是==无限循环小数==，所以 23 位一定装不下，必然要舍入
        —— 这题真正问的是"舍还是入"。

        ==看到题面写"中间值取偶数"先别急着凑偶数==：
        那只是在告诉你用的是哪种舍入模式，
        [取偶只在丢弃部分正正好等于一半时才用](#/co/data/float-op?at=round-decision)。

        先把丢弃的头几位写出来，再对着三行判定表走。
      `,
      solution: String.raw`
        **① 转二进制**

        $$12=1100_2,\qquad 0.1=0.0\underbrace{0011\,0011\,0011\cdots}_{0011\ \text{无限循环}}{}_2$$

        $$12.1=1100.000110011001100110011001\cdots_2$$

        **② 规格化**：小数点左移 3 位 $\Rightarrow e=3$，$E=3+127=130=10000010_2$

        $$12.1=1.1000001100110011001100110011\cdots_2\times2^{3}$$

        **③ 取尾数的前 23 位**

        $$M=\underbrace{1000\ 0011\ 0011\ 0011\ 0011\ 001}_{23\ \text{位}}
        \ \big|\ \underbrace{1\,0011\,0011\cdots}_{\text{丢弃}}$$

        **④ 判舍入**

        - 保护位 $G=\mathbf{1}$（丢弃部分的第一位）
        - 舍入位 $R=0$
        - 粘位 $S=\mathbf{1}$（后面还有无穷多个 1）

        $G=1$ 且 $S=1$ $\Rightarrow$ ==丢弃部分严格大于半个 ulp，无条件进位==。
        （==没到中间值，取偶规则用不上==）

        $$M+1=1000\ 0011\ 0011\ 0011\ 0011\ 010$$

        **⑤ 拼接**

        $$\underbrace{0}_{S}\ \underbrace{10000010}_{E}\ \underbrace{10000011001100110011010}_{M}$$

        $$0100\ 0001\ 0100\ 0001\ 1001\ 1001\ 1001\ 1010$$

        $$\boxed{\texttt{4141 999AH}\quad\text{选 B}}$$
      `,
      comment: String.raw`
        **四个选项的来历**（每个都算过）：

        | 选项 | 实际代表的数 | 怎么来的 |
        |---|---|---|
        | A. $\texttt{41419999}$ | $12.099999427795410$ | ==直接截断==，没有进位 |
        | **B. $\texttt{4141999A}$** | $12.100000381469727$ | ✅ 就近舍入（进位） |
        | C. $\texttt{41E0CCCC}$ | $28.099998474121094$ | 阶码 $e=4$，==算的其实是 $28.1$== |
        | D. $\texttt{41E0CCCD}$ | $28.100000381469727$ | 同上，$28.1$ 的舍入版 |

        ==A / B 的差别只在最后一位，考的是舍入；C / D 整个阶码都不对。==

        **十秒排除 C、D**：$12.1\in[8,16)=[2^3,2^4)\Rightarrow e=3\Rightarrow E=130=10000010_2$，
        于是前 9 位是 $0\ 1000\ 0010$，==第三个十六进制数字的最高位必须是 0==（即 $0\sim7$）。
        $\texttt{41E0}$ 的第三位是 $\texttt{E}=1110$，最高位为 1 $\Rightarrow E=131\Rightarrow e=4$，
        直接出局。这就是[看开头几位判量级](#/co/data/float?at=hex-quick-check)那一招。

        **另一条自查**：舍入只会动最低位，所以正确答案与截断结果
        ==只可能差 1==（$\texttt{9999}\to\texttt{999A}$）。
        可以在[位模式工具](#/tools/co/ieee754?at=widget)里输入 $12.1$ 直接对答案。
      `,
    },

    /* ================================================================== */
    { t: 'h', id: 'overflow', c: '三、溢出判断' },

    { t: 'warn', id: 'mantissa-overflow', title: '★ 尾数溢出不是溢出，只有阶码溢出才是', c: String.raw`
      这是判断题的常客：

      - **尾数溢出**（相加进位成 $10.xxx$）：==右规一次就解决了==，
        代价只是低位精度，==不算溢出==；
      - **阶码上溢**（右规之后阶码超过最大值）：==这才是真溢出==，
        结果置 $\pm\infty$，置溢出标志。

      所以判断一次浮点运算是否溢出，==唯一的依据是最终阶码有没有越界==。
      范围的四个边界值见[表示范围](#/co/data/float?at=range-table)。
    ` },

    { t: 'compare', id: 'overflow-table', title: '四种越界情况的处理',
      cols: ['情况', '含义', 'IEEE 754 的处理'],
      rows: [
        ['正上溢', '结果 $>$ 最大正数', '置 $+\\infty$，==报异常=='],
        ['负上溢', '结果 $<$ 最小负数', '置 $-\\infty$，==报异常=='],
        ['正下溢', '$0<$ 结果 $<$ 最小正数', '退化为非规格化数，再小则置 $+0$，==一般不报错=='],
        ['负下溢', '最大负数 $<$ 结果 $<0$', '同上，置 $-0$'],
      ] },

    { t: 'example',
      id: 'ex-four-claims',
      title: '四句关于浮点加减的叙述，哪些对',
      source: '真题 / 错题',
      level: 3,
      problem: String.raw`
        下列有关浮点数加减运算的叙述中，正确的是（　）。

        Ⅰ. 对阶操作不会引起阶码上溢或下溢
        Ⅱ. 右规和尾数舍入都可能引起阶码上溢
        Ⅲ. 左规时可能引起阶码下溢
        Ⅳ. 尾数溢出时结果不一定溢出

        **A.** 仅 Ⅱ、Ⅲ　　**B.** 仅 Ⅰ、Ⅱ、Ⅳ　　**C.** 仅 Ⅰ、Ⅲ、Ⅳ　　**D.** Ⅰ、Ⅱ、Ⅲ、Ⅳ
      `,
      idea: String.raw`
        四句话其实在问同一件事：==这一步会让阶码往哪个方向动==。
        照着[五步流程](#/co/data/float-op?at=five-steps)一步步对：

        | 步骤 | 阶码怎么动 | 可能出什么事 |
        |---|---|---|
        | 对阶 | 小阶升到大阶 | ？ |
        | 右规 | $+1$ | ？ |
        | 左规 | 连续 $-1$ | ？ |
        | 舍入 | ==看似不动== | ？ |

        ==最后一行是这题的胜负手==：舍入真的不动阶码吗？
      `,
      solution: String.raw`
        **Ⅰ. 正确。** 对阶是"小阶向大阶看齐"，
        对齐后的阶码==就等于两个操作数中较大的那个阶码==。
        既然操作数本身合法，这个阶码当然也合法 —— ==凭空多不出溢出来==。
        （而且阶码只增不减，也谈不上下溢。）

        **Ⅱ. 正确 ——** ==这是最容易漏的一句==。

        - **右规**：两个同号尾数相加进位成 $10.\cdots$，右规一次、阶码 $+1$。
          原阶码已是最大值时，这一下就上溢。
        - **尾数舍入**：舍入进位可能让尾数从 $1.11\cdots1$ 变成 $10.00\cdots0$，
          ==又破坏了规格化，必须再右规一次，阶码再 $+1$==，于是同样可能上溢。
          见[舍入之后可能又不规格化了](#/co/data/float-op?at=round-then-renormalize)。

        **Ⅲ. 正确。** 两个相近的数相减会发生[灾难性抵消](#/co/data/float-op?at=ex-ieee-add)，
        高位一长串 0，左规要连着左移、阶码连着减，==减过头就是下溢==
        （退化为非规格化数或直接置机器零）。

        **Ⅳ. 正确。** 尾数溢出只是中间状态，==右规一次就化解了==；
        是否真溢出==只看最终阶码==，见
        [尾数溢出不是溢出](#/co/data/float-op?at=mantissa-overflow)。

        $$\boxed{\text{四句全对，选 D}}$$
      `,
      comment: String.raw`
        **把四句话压成一张表**，考场上比逐句回忆快：

        | 步骤 | 阶码 | 可能的后果 |
        |---|---|---|
        | 对阶 | 升到 $\max(E_x,E_y)$ | ==不会溢出==（本来就合法） |
        | 尾数运算 | 不动 | 尾数溢出 $\ne$ 结果溢出 |
        | 右规 | $+1$ | ==可能上溢== |
        | 左规 | 连续 $-1$ | ==可能下溢== |
        | 舍入 | 进位 $\to$ 触发再右规 $\to$ $+1$ | ==可能上溢== |

        ==规律很整齐：凡是让阶码变大的动作都可能上溢，让阶码变小的都可能下溢，
        唯独对阶是"往已有的合法值靠"，所以安全。==

        **Ⅱ 的连锁反应值得单独记**：舍入不是流程的终点，
        它后面还挂着"再规格化 + 再判溢出"。
      `,
    },

    { t: 'compare', id: 'special-ops', title: '特殊值参与运算的结果（选择题常客）',
      cols: ['运算', '结果'],
      rows: [
        ['$\\infty+$ 有限数', '$\\infty$'],
        ['$\\infty-\\infty$', '**NaN**'],
        ['$0\\times\\infty$', '**NaN**'],
        ['$0/0$、$\\infty/\\infty$', '**NaN**'],
        ['$x/0\\ (x\\neq0)$', '$\\pm\\infty$，==不是错误，是有定义的结果=='],
        ['NaN 参与的任何运算', '**NaN**（一路传播下去）'],
        ['$\\text{NaN}==\\text{NaN}$', '**假** ==（唯一一个不等于自己的值）=='],
        ['$+0==-0$', '**真**'],
      ] },

    /* ================================================================== */
    { t: 'h', id: 'mul-div', c: '四、乘法与除法' },

    { t: 'steps', id: 'mul-div-steps', title: '乘除法：不用对阶，但阶码要修偏置', items: [
      { title: '定符号',
        c: String.raw`$S=S_1\oplus S_2$，==符号单独异或，和大小无关==。` },
      { title: '阶码相加 / 相减',
        c: String.raw`
          乘法 $E=E_1+E_2-Bias$，除法 $E=E_1-E_2+Bias$。
          ==那个 $\mp Bias$ 是移码相加时必须做的修正==（见下）。` },
      { title: '尾数相乘 / 相除',
        c: String.raw`
          $1.M_1\times 1.M_2\in[1,4)$，$1.M_1\div 1.M_2\in(0.5,2)$。` },
      { title: '规格化 + 舍入 + 判溢出',
        c: String.raw`
          乘法结果落在 $[2,4)$ 时右规一次；除法结果落在 $(0.5,1)$ 时左规一次。
          ==两者都最多一位==，然后照常舍入、判阶码溢出。` },
    ] },

    { t: 'warn', id: 'bias-fix', title: '★ 移码相加为什么要减一个偏置', c: String.raw`
      移码存的是 $E=e+127$，两个移码直接相加：

      $$E_1+E_2=(e_1+127)+(e_2+127)=(e_1+e_2)+254$$

      ==偏置被算了两次==，所以要减掉一个 $127$ 才是正确的移码：

      $$E=E_1+E_2-127$$

      除法同理，两个移码相减会==把偏置减没==，要补回来：$E=E_1-E_2+127$。

      注意这是 IEEE 754（阶码移码）才有的修正。
      教材里阶码用**补码**表示时，==直接相加减即可，没有这一步==。
      偏置为什么是 127 见[这里](#/co/data/float?at=bias-127)。
    ` },

    /* ================================================================== */
    { t: 'h', id: 'anomaly', c: '五、算得出但不对：浮点运算的反直觉现象' },

    { t: 'key', id: 'big-eats-small', title: '大数吃小数', c: String.raw`
      两数量级相差很大时，对阶要把小数的尾数右移很多位，
      ==移出 24 位之后小数的有效位一个不剩==，加了等于没加：

      $$10^{20}+1=10^{20}\quad(\text{单精度下})$$

      **判据（会算比会背有用）**：设大数所在段的间隔是
      $\text{ulp}=2^{e-23}$，只要 $|\text{小数}|<\tfrac12\text{ulp}$，加上去就==原地不动==。

      $10^{20}$ 的 $e=66$，$\text{ulp}=2^{43}\approx8.8\times10^{12}$ ——
      别说加 1，加一万亿都未必动得了它。可以在
      [位模式工具](#/tools/co/ieee754?at=widget)里输入 $10^{20}$，
      直接读"相邻间隔"那一行。

      根源是[刻度不均匀](#/co/data/float?at=ulp-def)，不是硬件有 bug。
    ` },

    { t: 'key', id: 'no-assoc', title: '浮点加法不满足结合律', c: String.raw`
      $$(a+b)+c\ \neq\ a+(b+c)$$

      因为每一步都要舍入，==加的顺序变了，被舍掉的东西就变了==。

      推论（都考过）：

      - 浮点加法==不满足结合律，也不满足分配律==；
      - 但==满足交换律==（$a+b=b+a$，舍入只跟结果有关）；
      - 大量数据求和，==从小到大加==精度更好（先让小数互相攒起来）；
      - 编译器不能随便重排浮点加法的顺序，除非开了 $\texttt{-ffast-math}$。
    ` },

    /* ================================================================== */
    { t: 'h', id: 'examples', c: '六、例题' },

    { t: 'example',
      id: 'ex-ieee-add',
      title: 'IEEE 754 单精度加法全流程',
      source: '基础必会',
      level: 3,
      problem: String.raw`
        用 IEEE 754 单精度计算 $x+y$，其中 $x=0.5$、$y=-0.4375$，
        写出运算过程和结果的机器数。
      `,
      idea: String.raw`
        先把两个数写成 $1.M\times 2^{e}$ 的形式 ——
        ==这一步做对了，后面就是套流程==。

        $0.4375=\tfrac{7}{16}=0.0111_2$，第一个 1 在 $2^{-2}$ 位上，
        所以是 $1.11\times 2^{-2}$，==不是 $2^{-3}$==。
        这里数错一位，后面全错，所以要用"这个数在哪两个 2 的幂之间"验一下：
        $0.4375\in[0.25,0.5)=[2^{-2},2^{-1})\Rightarrow e=-2$ ✓。

        两个数很接近且异号，==要警惕左规会移很多位==。
      `,
      solution: String.raw`
        **① 写成规格化形式**

        | | 值 | 规格化 | $S$ | $E$ | 真实指数 |
        |---|---|---|---|---|---|
        | $x$ | $0.5$ | $1.000\times2^{-1}$ | 0 | $126=01111110$ | $-1$ |
        | $y$ | $-0.4375$ | $-1.110\times2^{-2}$ | 1 | $125=01111101$ | $-2$ |

        **② 对阶**：$\Delta E=126-125=1$，$y$ 的阶码小，==$y$ 的尾数右移 1 位==：

        $$y=-1.110\times2^{-2}\ \longrightarrow\ -0.111\times2^{-1}$$

        **③ 尾数求和**（注意 $x$ 的尾数要补上隐藏位）

        $$1.000-0.111=0.001$$

        结果为 $0.001\times 2^{-1}$。

        **④ 规格化**：最高位不是 1，左规。左移 3 位，阶码减 3：

        $$0.001\times2^{-1}=1.000\times2^{-4}$$

        **⑤ 舍入**：尾数是精确的，无需舍入。**判溢出**：$e=-4$ 远在范围内，正常。

        **结果**

        $$x+y=1.0\times2^{-4}=0.0625$$

        $$E=-4+127=123=01111011_2,\quad M=0$$

        $$\underbrace{0}_{S}\ \underbrace{01111011}_{E}\ \underbrace{00\cdots0}_{M}
        =\boxed{\texttt{0x3D800000}}$$
      `,
      comment: String.raw`
        **注意这道题的信息量在第 ④ 步**：$0.5$ 和 $0.4375$ 只差 $0.0625$，
        两个 24 位有效数字的数相减，==结果只剩 1 位有效数字==，
        其余全靠左规补 0 —— 这就是**灾难性抵消（catastrophic cancellation）**。

        本题因为原数精确所以没出错，但如果 $x,y$ 本身就是舍入过的近似值，
        ==相减之后误差会被放大到和结果同量级==。
        数值计算里"避免相近的数相减"就是这么来的。

        结果可以拿[位模式工具](#/tools/co/ieee754?at=widget)验：
        输入 $0.0625$，看是不是 $\texttt{3D800000}$。
      `,
    },

    { t: 'example',
      id: 'ex-textbook-add',
      title: '教材格式的浮点加法（阶码补码 + 尾数补码）',
      source: '408 经典题型',
      level: 4,
      problem: String.raw`
        已知 $x=2^{0010}\times 0.11011011$，$y=2^{0100}\times(-0.10101100)$，
        阶码用 4 位补码（含 1 位阶符）、尾数用 8 位补码（含 1 位数符）表示，
        求 $x+y$，结果用二进制表示。
      `,
      idea: String.raw`
        这是==另一套体系==：尾数是纯小数 $0.1xxx$、没有隐藏位、补码运算，
        [和 IEEE 754 的区别看这里](#/co/data/float?at=textbook-vs-ieee)。

        流程完全一样，但有两处必须小心：

        1. ==负数尾数要先转补码再运算==，不能拿真值直接加；
        2. ==规格化判据是"符号位与最高数值位相反"==，不是"最高位为 1"。

        右移时多留两位附加位，最后再舍入 —— 不然对阶丢掉的那两位会让答案偏一大截。
      `,
      solution: String.raw`
        **① 写成机器数**

        $$[x]_{\text{浮}}:\ E_x=0010\ (+2),\quad [M_x]_{\text{补}}=0.11011011$$
        $$[y]_{\text{浮}}:\ E_y=0100\ (+4),\quad [M_y]_{\text{补}}=1.01010100$$

        （$-0.10101100$ 的补码 $=10.00000000-0.10101100=1.01010100$）

        **② 对阶**：$\Delta E=(+2)-(+4)=-2$，==$x$ 的阶码小，$x$ 的尾数右移 2 位==
        （补码右移，高位补符号位 0），阶码变成 $0100$：

        $$[M_x]_{\text{补}}=0.0011011011\quad(\text{末尾 }11\text{ 是附加位})$$

        **③ 尾数求和**（补码直接加，符号位参与运算）

        $$\begin{aligned}
        &\ \ \ 0.0011011011\\
        +&\ \ \ 1.0101010000\\
        \hline
        &\ \ \ 1.1000101011
        \end{aligned}$$

        **④ 规格化**：符号位 $1$、最高数值位 $1$，==两者相同 $\Rightarrow$ 不是规格化数==，
        需要左规 1 位（数值位左移、末位补 0，阶码减 1）：

        $$[M]_{\text{补}}=1.0001010110,\qquad E=0100-1=0011\ (+3)$$

        现在符号位 $1$、最高数值位 $0$，==相反，规格化完成==。

        **⑤ 舍入**：尾数只留 8 位，多出来的 $10$ 要处理。按**截断法**直接丢：

        $$[M]_{\text{补}}=1.00010101$$

        **结果**

        $$x+y=2^{0011}\times 1.00010101_{\text{补}}=2^{0011}\times(-0.11101011)$$

        即 $2^{3}\times(-0.91796875)=-7.34375$。
      `,
      comment: String.raw`
        **验算**：$x=2^2\times0.85546875=3.421875$，$y=2^4\times(-0.671875)=-10.75$，
        精确和是 $-7.328125$。我们算出 $-7.34375$，差的部分正是舍入误差。

        **这里藏着一个大坑**：==补码的截断是朝 $-\infty$ 舍入，不是朝 0 舍入==。
        $1.0001010110$ 砍掉末两位得 $1.00010101$，对应的真值从 $-0.916015625$
        变成了 $-0.91796875$ —— ==绝对值变大了==。
        若按 IEEE 754 的默认"就近舍入到偶数"，
        丢弃部分 $10$ 正好是一半，末位取偶应进位成 $1.00010110$，
        结果是 $-7.3125$。==两种舍入夹着精确值 $-7.328125$，各偏 $0.015625$==。

        考试写哪个？==题目说了用什么舍入法就用什么，没说就用截断并注明==。
      `,
    },

    { t: 'example',
      id: 'ex-assoc',
      title: '换个加法顺序，结果就变了',
      source: '概念辨析 / 高频判断题',
      level: 3,
      problem: String.raw`
        在 32 位单精度下执行下面两段代码，$a$ 和 $b$ 分别是多少？

        ~~~c
        float a = (3.14f + 1e20f) - 1e20f;
        float b = 3.14f + (1e20f - 1e20f);
        ~~~
      `,
      idea: String.raw`
        别去算 $10^{20}$ 的位模式，==只需要问一句：$3.14$ 有没有资格影响 $10^{20}$==？

        看那一段的 ulp 就够了。
      `,
      solution: String.raw`
        **算 $a$**

        $10^{20}<2^{67}$，且 $10^{20}>2^{66}$，所以它落在 $e=66$ 那一段，间隔

        $$\text{ulp}=2^{66-23}=2^{43}\approx 8.8\times10^{12}$$

        $3.14\lll\frac12\text{ulp}$，于是 $3.14+10^{20}$ 舍入之后==还是 $10^{20}$==，
        再减去 $10^{20}$：

        $$a=0$$

        **算 $b$**

        括号里先算，$10^{20}-10^{20}=0$，于是

        $$b=3.14$$

        **结论**：$a=0\neq b=3.14$，==浮点加法不满足结合律==。
      `,
      comment: String.raw`
        **顺带澄清一个常见误解**：这不是"精度不够"造成的随机误差，
        而是==完全确定的、可预测的行为==。同样的代码在任何符合 IEEE 754 的机器上
        都得到同样的 $a=0$、$b=3.14$。

        **实际影响**：数值库里求和要用 Kahan 求和这类补偿算法；
        比较两个浮点数不能用 $\texttt{==}$，要判 $|x-y|<\varepsilon$。
      `,
    },

    /* ================================================================== */
    { t: 'h', id: 'pitfalls', c: '七、易错清单' },

    { t: 'warn', id: 'pitfall-list', title: '这一节的固定失分点', c: String.raw`
      1. **对阶方向搞反**：==小阶向大阶看齐，动的是小阶那个数的尾数（右移）==。
      2. **忘了补隐藏位**：IEEE 754 下参与尾数运算的是 $1.M$，不是 $M$。
      3. **把尾数溢出当成溢出**：右规一次就好，==只有阶码越界才是溢出==。
      4. **右规移了不止一位**：右规==最多一次==；能移很多次的是左规。
      5. **乘法忘了修偏置**：移码相加要 $-127$，相减要 $+127$。
      6. **舍入之后没有再检查规格化**：进位可能把 $1.11\cdots1$ 顶成 $10.0\cdots0$，
         ==于是再右规一次、阶码再 $+1$，可能就此上溢==，见
         [这道叙述题](#/co/data/float-op?at=ex-four-claims)。
      7. **看到"中间值取偶数"就去凑偶数**：==只有丢弃部分正好等于一半时才取偶==，
         判定表见[这里](#/co/data/float-op?at=round-decision)。
      8. **补码尾数右移时高位补 0**：应该==补符号位==（算术右移）。
      9. **认为浮点加法有结合律**：见[上面这道题](#/co/data/float-op?at=ex-assoc)。
      10. **拿 $\texttt{==}$ 比较浮点数**，以及忘了 ==NaN 不等于自己==。
    ` },

    { t: 'md', c: String.raw`
      ---

      往回看表示部分：[IEEE 754 的格式与范围](#/co/data/float?at=format)；
      想动手拨位：[IEEE 754 位模式工具](#/tools/co/ieee754?at=widget)。
    ` },

  ],
});
