# 408 专业课知识库 —— 工作手册

这是一个**纯静态、无构建**的个人复习站，和 `../Mathematics`（考研数学）是姊妹项目，
共用同一套引擎，内容体系各自独立。

用户的使用方式是：
把他和 AI 的对话（含他自己的思考）丢给我 → 我判断归属层级 → 我更新对应内容文件 → 他刷新页面看。

**用户不会在网页上编辑任何内容。所有更新都由我通过改文件完成。**

---

## 1. 目录结构

```
index.html                  页面外壳（新增内容页 / 新增工具都不需要改它）
assets/css/                 tokens 变量 / layout 布局 / typography 排版 / blocks 块样式 / tools 工具样式
assets/js/                  store 数据 / markdown 解析 / blocks 渲染 / tools 工具注册 / sidebar 导航 / search 搜索 / app 路由
vendor/katex/               本地 KaTeX，保证离线和 file:// 直接双击可用
content/manifest.js         ★ 知识树：学科 → 章 → 专题，全站唯一的结构定义
content/<学科>/<章>/<专题>.js  ★ 内容页
content/review/<科目>.js     ★ 错题卡（按科目一页，汇总页自动收集）
tools/<工具id>.js            ★ 交互工具（工具本体 + 它自己的说明页写在同一个文件里）
```

路由是 `#/<subject.id>/<chapter.id>/<topic.id>`，和 manifest 里的 id 一一对应。

学科 id：`ds` 数据结构 · `co` 组成原理 · `os` 操作系统 · `net` 计算机网络 ·
`threads` 跨科手法主线 · `review` 错题本 · `tools` 工具箱 · `notes` 随笔。

---

## 2. 新增 / 更新内容的标准流程

### 情况 A：专题已在 manifest 里且已有 `file`
直接编辑那个内容文件，往 `blocks` 数组里加块或改块。**改完把 `updated` 改成今天的日期。**

### 情况 B：专题在 manifest 里但没有 `file`（侧栏显示为灰色待补充）
1. 新建内容文件，路径用 `content/<学科id>/<章序号>-<章id>/<专题id>.js`
2. 回到 `content/manifest.js`，给那个 topic 补上 `file: '...'`

### 情况 C：manifest 里根本没有这个专题
先在对应章节的 `topics` 数组里按合适的位置插入一条（顺序 = 侧栏顺序 = 上一篇/下一篇顺序），再按情况 B 做。
**不要为了一段对话就新建一个章。** 章的划分已经对齐 408 考纲，优先往已有章里加专题。

### 归类时的判断顺序
1. 先定**科目**（数据结构 / 组成原理 / 操作系统 / 计算机网络）
2. 再定**章**（对齐考纲）
3. 再定**专题**（考点粒度，例如「页面置换算法与抖动」而不是「内存管理」）
4. 如果内容跨专题，拆开写进各自的页，不要堆在一页里

**408 特有的跨科重叠要小心**：

| 内容 | 归到哪 |
|---|---|
| Cache 的映射与替换 | `co/memory/cache`（组成原理） |
| 页面置换算法 | `os/memory/replace`（操作系统） |
| 虚拟地址转换的硬件部分（TLB） | `co/memory/virtual` |
| 请求分页的软件策略 | `os/memory/virtual` |
| B+ 树本身 | `ds/search/btree` |
| 文件索引结构用到 B+ 树 | `os/file/file-struct`，链接过去 |

判断不了归属时，问用户一句，不要瞎放。

---

## 3. 内容页文件格式

```js
KM.page({
  path: 'ds/string/kmp',                       // 必须等于 manifest 里的 学科/章/专题 id
  title: 'KMP 与 next 数组',                    // 纯文本，会进 document.title，不能写公式
  subtitle: '一句话说清这页解决什么问题',        // 可选，支持行内 Markdown 与 $公式$
  tags: ['高频', '必考', '手算'],                // 可选，见下方标签约定
  updated: '2026-08-10',                       // 每次改动都更新

  blocks: [ /* 见下 */ ],
});
```

**所有含反斜杠的字符串一律用 `` String.raw`...` ``**，否则 `\n`、`\t`、`\frac` 会被 JS 当转义字符吃掉。
代码块尤其要注意（C 代码里的 `\0`、`\n` 全会被吃）。
`String.raw` 里不能出现反引号和 `${`。

---

## 4. 块类型（这是排版的全部词汇）

| `t` | 用途 | 视觉 | 字段 |
|---|---|---|---|
| `h` | 分节大标题，同时进入右侧目录 | 衬线大标题 + 下划线 | `c` |
| `md` | 普通正文 | 无框 | `c` |
| `key` | 定义、原理、必背结论 | **蓝**框 | `title`, `c` |
| `method` | 方法论、解题套路、算法流程说明 | **紫**框 | `title`, `c` |
| `insight` | ★ **用户自己的思路、顿悟、私人口诀** | **绿**框 + 阴影 | `title`, `c` |
| `warn` | 易错点、陷阱、边界条件 | **红**框 | `title`, `c` |
| `quote` | 原始对话摘录、暂未消化的素材 | 灰框 | `title`, `c` |
| `steps` | 有序流程（带圆圈数字时间轴） | 紫色节点 | `title`, `items: [{title, c}]` |
| `code` | ★ **代码 / ASCII 图**（408 专用） | 深色卡片 + 语言标签 | `title`, `lang`, `note`, `c` |
| `example` | **例题卡** | 琥珀色卡片 | 见下 |
| `compare` | 对比表 | 数据表 | `title`, `cols: []`, `rows: [[]]` |
| `formulas` | 公式速查网格 | 蓝色格子 | `title`, `items: [{label, tex}]` |
| `tool` | ★ **嵌入一个交互工具** | 工具卡片 | `id`, `use`（工具 id）, `title`, `note` |
| `card` | ★ **错题卡 / 练习卡**（一个点） | 红顶白卡 + 折叠 | 见 4.7 节 |
| `cards` | 错题卡汇总（按时间自动收集） | 时间分档清单 | `id`, `only`（可选，只收某个路径前缀） |

### `code` 块（408 相对数学站新增的类型）

```js
{ t: 'code', id: 'get-next', title: '求 next 数组', lang: 'c',
  note: '可选的一句话说明',
  c: String.raw`
    void get_next(SString T, int next[]) {
        ...
    }
  ` }
```

- `lang` 会显示成标签（`c` / `pseudo` / `asm` / 留空）
- 内容会自动去掉模板字符串的公共缩进，**代码自身的层次缩进保留**
- `lang` 留空时适合放 **ASCII 示意图**（内存布局、树形结构、报文格式、时序图）
- 408 里画图很重要，==能用 ASCII 图说清的，不要写三段话==

### KaTeX 的两个坑

- ==`\texttt{}` 里不能放 `\cdots`、`\ldots` 这类命令==，会整块渲染失败。
  写成 `$\texttt{414}\cdots$`，别写 `$\texttt{414\cdots}$`。
- 机器数、十六进制、C 代码片段统一用 `$\texttt{...}$`，
  正文里出现不了反引号（见第 5 节）。

### `tool` 块 —— 把交互工具嵌进知识页

```js
{ t: 'tool', id: 'inline-tool', use: 'ieee754',
  title: '可选，默认取工具自己的标题', note: '可选的一句话说明' }
```

`use` 是 `tools/` 下注册过的工具 id。同一个工具可以嵌在多个页面上，
每次进页面都是全新实例，状态不会串。工具怎么写见第 4.6 节。

### 颜色语义不能混用
蓝 = 客观知识；紫 = 怎么做；**绿 = 用户自己说的话**；红 = 会丢分的地方。
用户复习时靠颜色区分「书上的」和「我自己的」，这是整站最重要的约定。

**不要替用户写绿块。** `insight` 里必须是他自己说过的话（可以精简、条理化，
但不能编）。整理对话时他没表达过的洞察，就放蓝块或紫块。

### 每个块都可以有 `id`（重要）

任何块都接受一个 `id` 字段，别处就能用 `?at=` 精确链过来：

```js
{ t: 'key', id: 'no-backtrack', title: 'KMP 只动 j', c: ... }
```

**命名规则**：短、ASCII、小写连字符、==描述内容而不是位置==。
`no-backtrack` / `three-way-handshake` / `banker-algorithm` / `ex-hand-compute` 都是好名字；
`block3`、`section-four` 是坏名字（改顺序就失真）。

**什么时候必须写 `id`**：
- 任何**可能被别的页引用**的块（定义、算法、例题、代码）；
- 所有 `h` 分节标题；
- 所有 `example` 和 `code`（不写就退化成位置编号，中间插一个就全错位）。

### example 块
```js
{ t: 'example',
  title: '这道题在考什么',      // 不要写「例题1」，编号自动生成
  source: '2019 真题',          // 可选
  level: 3,                     // 可选，1~5 星难度
  problem: String.raw`题面`,
  idea:     String.raw`思路——为什么想到这么做，默认折叠`,
  solution: String.raw`完整解答，默认折叠`,
  comment:  String.raw`点评/拓展/同类题，默认折叠`,
  openIdea: true,               // 可选，让某段默认展开
}
```
思路 / 解答 / 点评默认折叠，方便用户先自测。顶栏 `⇕` 或页内按钮可一键全展开。
**`idea` 要写「为什么会想到」，不是解答的复述** —— 这是这个站最有价值的部分。

---

## 4.5 页间链接必须精确到块

**链到页面开头是不合格的。** 用户点过去要能直接看到那个定义，而不是自己在长页面里找。

```
[next 数组的定义](#/ds/string/kmp?at=definition)     ✅ 落到那个块
[KMP](#/ds/string/kmp)                               ❌ 只到页面开头
[看这里](#definition)                                ❌ 会被路由当成页面路径，跳到「待补充」页
```

格式固定为 `#/<学科>/<章>/<专题>?at=<块id>`。页内链接也用同样的全路径写法。

链接文字要写清**落到的是什么**，因为跳过去之后用户看到的是页面中部，需要文字先建立预期。

写完链接后**一定要跑一次自检**（见第 8 节），断链在静态页面上是无声失败的。

---

## 4.6 交互工具（`tools/`）

有些考点「自己拨一下」比读三段话快得多：浮点的位、补码的溢出、Cache 地址划分、
子网掩码。这类东西写成工具，放在 `tools/<工具id>.js`。

**一个工具文件干两件事**：

```js
KM.tool({
  id: 'ieee754',                     // 工具 id，内容页用 use 引用它
  title: 'IEEE 754 位模式',
  subtitle: '一句话说清它能干嘛',     // 会显示在首页工具卡上
  icon: '⬚',
  path: 'tools/co/ieee754',          // 它自己那一页的路径，首页工具卡靠这个链过去
  mount(el) { /* 往 el 里塞 DOM、绑事件，每次进页面调一次 */ },
});

KM.page({ path: 'tools/co/ieee754', blocks: [ { t:'tool', id:'widget', use:'ieee754' }, ... ] });
```

**接进站里**：在 `manifest.js` 的 `tools` 学科下找一章（`co` / `net` / `ds`），
加一条 topic，`file` 指向这个工具文件即可 —— **不用改 index.html**。

**写工具时的约定**：

- 样式类名复用 `assets/css/tools.css` 里的零件（`.tool-row` `.tool-btn` `.tool-field`
  `.tool-line` `.bits` `.bit`），==不要在工具里写内联颜色==，深浅色主题会崩；
- 位格子的配色沿用全站语义：符号位红、阶码蓝、尾数紫；
- 工具算出来的东西**必须是对的**：能用整数 / `BigInt` 精确算的，不要用浮点近似
  （`ieee754` 的「精确值」就是用 `BigInt` 展开的）；
- `mount` 里不要依赖全局状态，用户来回切页面会反复调用。

**工具是配角**：它替代不了知识页。真正的考点、结论、易错点仍然写在内容页里，
工具只负责让人「亲眼看见」。内容页里嵌工具的位置，一般在讲完手算方法之后 ——
==先让人自己算，再给他对答案的东西==。

---

## 4.7 错题本（`content/review/`）

用户说「这题我做错了」时，除了把题目写进对应专题的 `example`，
还要==在错题本里加一张卡==。

```js
{ t: 'card',
  id: 'c-extend-order',            // 必写，汇总页和练习页靠它定位；习惯用 c- 开头
  date: '2026-08-11',              // 必写 YYYY-MM-DD，汇总页按它排序分档
  where: '类型转换 · 扩展',         // 短标签，会进右侧目录和卡片抬头
  q:     '题面（要能独立看懂，但别贴一整道大题）',
  options: [                       // 可选：选择题的选项，判断题就写「对 / 错」
    { k: 'A', c: '$2^{15}-1$' },
    { k: 'D', c: '$2^{32}-2^{15}+1$' },
  ],
  answer: 'D',                     // 可选：正确选项
  mine:   'B',                     // 可选：他选错的那个（做对的题就不写）
  wrong:  '他当时写的那一步',        // 写了就是「错点」卡，不写就是「练习」卡
  right:  '正确是什么',              // 翻面才看得到
  why:    '根因 + 一句话口诀',        // 翻面才看得到
  link:   'co/data/cast?at=ex-short-unsigned' }   // 回到知识点，必须带 ?at=
```

**三个使用面**（同一批数据，三种看法）：

| 页面 | 作用 |
|---|---|
| `review/index/deck` | ★ **卡片练习**：一次一张、点击翻面、可标「记住了」 |
| `review/index/all` | 按时间分档的清单 + 阶段性归纳 |
| `review/<科目>` | 卡面原文，可搜索、可打印，==卡片就写在这里== |

**卡片的要点**：

- ==一张卡只放一个点==，不要把整道题的完整解答搬进来。
  详细推导放在专题页的 `example` 里，卡片用 `link` 连过去。
- **正面只有题目**（`q` + `options`），==翻面才出现答案标记和 `wrong`/`right`/`why`==。
  所以 `q` 要写成"能直接作答"的样子，别在题面里剧透。
- 有 `options` 时尽量把 `answer` 和 `mine` 都写上 ——
  翻面时会把正确项标绿、他选的那项标红划掉，==一眼看出当时偏到哪去了==。
- `why` 里必须有==一句可执行的口诀或自查动作==，
  「要细心」不算，「补位看原类型、解释看目标类型」才算。
- **做对的题也可以做成卡**（不写 `wrong` 即可），会显示成蓝色的「练习」卡。
- 新增卡片只需要在 `content/review/<科目>.js` 里加一个 `card` 块，
  练习页和汇总页都会自动收进去，不用改别的地方。
- 新开一个科目的卡片页时，才需要动 manifest（在 `review` 学科下加一章）。

「记住了」的标记存在浏览器 localStorage（`km-deck-known`）里，==不进版本库==，
所以别把它当数据，它只是练习时的临时筛选。

**归纳比堆积重要**：卡片攒到一定数量，要回头在汇总页写一段
「这些错长得像不像」——==找出反复出现的失分模式==（例如「方向 / 顺序错」），
比多记十张卡有用。

---

## 5. Markdown 与公式

支持：`# ~ #####` 标题、`**粗**`、`*着重(紫色)*`、`==高亮(黄底)==`、`~~删除~~`、`` `代码` ``、
有序/无序列表（缩进嵌套）、`>` 引用、`---` 分割线、表格、代码块、`[文字](#/路径)`、`<br>`。

### ★ 正文里不能出现反引号，用这两个替代写法

正文一律写在 `` String.raw`...` `` 里，==反引号会直接把模板字符串截断，
报一个和内容毫不相干的 SyntaxError==（这是这个站最容易踩、也最难查的坑）。所以：

| 想写 | 在 `String.raw` 里写成 |
|---|---|
| 行内代码 `` `int` `` | `$\texttt{int}$` |
| 代码块 ` ``` ` | `~~~c` … `~~~`（等价，解析结果一样） |

`compare` 块的 `cols` / `rows` 是普通单引号字符串，==那里可以正常用反引号写行内代码==。

公式：行内 `$...$`，独立 `$$...$$`。408 里公式用得比数学站少，但这些地方需要：
组成原理的数制与浮点、Cache 命中率与平均访问时间、流水线加速比、
网络的时延与吞吐量、香农公式、复杂度记号。

### 排版习惯（直接影响「快速找重点」的体验）
- 结论句里的关键词用 `==高亮==`，一段里最多标 1~2 处，标多了等于没标
- 「必须记住的数字/边界」用 `**粗体**`
- 三行以上的并列信息用表格，不要用长列表
- **能画 ASCII 图就别用文字描述**（用 `code` 块，`lang` 留空）
- 每个 `h` 分节之间保持独立，用户经常只跳读某一节

### 自定义 KaTeX 宏（`assets/js/markdown.js` 里的 `MACROS`）
`\d`→d、`\dx \dt \du \ds \dy \dtheta \dxy`、`\R \N \Z \Q \C`、`\E \Var \Cov`、
`\rank \tr \diag`、`\abs{} \norm{} \set{}`、`\pd{f}{x}`、`\deriv{y}{x}`、`\xto{}`、
`\eps`、`\T`（转置）、`\inv`。
需要新宏就往 `MACROS` 里加（408 可能需要 `\oplus` 之类，KaTeX 原生已支持）。

---

## 6. 标签约定

`tags` 会自动上色：含「高频/必考/重点」→ 红；含「大题/证明/解答题/综合」→ 琥珀；
含「数一/数二/数三」→ 蓝（408 用不到）。

408 常用：`高频`、`必考`、`易错`、`真题`、`手算`、`综合应用`、`概念辨析`、`代码题`。

---

## 7. 处理用户对话的方法

用户会粘贴一大段他和 AI 的聊天记录。拆解方式：

- **他的提问和困惑** → 往往说明卡点在哪，写进 `insight` 或例题的 `idea`
- **他自己的推理尝试** → `insight` 块，==保留他的原话语气==，只做精简和条理化，不要改写成教科书腔
- **AI 给的方法总结** → `method` 或 `key` 块，**需要我核对正确性后再写**
- **具体题目** → `example` 块
- **代码 / 图** → `code` 块
- **他说「原来如此 / 我懂了」的地方** → 这就是最该记的，单独成块
- **他说「这题我做错了」** → 题目进对应专题的 `example`（要==把每个干扰项都反推一遍==，
  说清它是哪种错误的产物），错点进错题本（第 4.7 节）的 `card`，两边用 `?at=` 互相连上

**不要把整段对话原样贴进去。** 用户要的是能快速复习的成品，不是聊天记录归档。
确实需要保留原始上下文时用 `quote` 块，并放在该节末尾。

**核对正确性是硬要求。** 408 里 AI 常犯的错：
Cache 组相联的组号/块号位数算错、TCP 状态转换细节、
银行家算法的安全序列不唯一却说成唯一、
子网划分的可用主机数忘记减 2、KMP 的 next 两套约定混用。
写进去之前自己重算一遍。

写完给用户一句话说明：放到了哪个路径、加了哪些块。

---

## 8. 自检

改完内容后确认：
- [ ] `path` 和 manifest 里的 id 完全一致
- [ ] 含反斜杠的字符串都用了 `String.raw`（代码块尤其）
- [ ] **`String.raw` 里没有反引号**（行内代码用 `$\texttt{}$`、代码块用 `~~~`）
- [ ] 新增的块（尤其是例题、代码、会被引用的定义）都写了 `id`
- [ ] 跨页链接都带 `?at=`，没有只链到页面开头的
- [ ] `updated` 改成今天

先跑一遍语法检查，比在浏览器里猜快得多（`String.raw` 被反引号截断时，
控制台只会报一个位置莫名其妙的 SyntaxError）：

```bash
node --check content/co/02-data/float.js
```

起服务后在浏览器控制台跑这两句：

```
KM.audit()                                   // 断链、锚点不存在、重复 id
document.querySelectorAll('.math-error')     // 公式渲染错误，应为空
```

`KM.audit()` 会遍历全站所有页面，返回 `{total, broken}`，`broken` 必须是空数组。

本地预览（`.claude/launch.json` 已配好用 5181 端口，和数学站的 5180 错开；
也可以直接双击 `index.html`）：

```bash
python -m http.server 5181
```

**注意**：浏览器会缓存 js，改完刷新看不到变化时用 `Ctrl+Shift+R` 硬刷新。
